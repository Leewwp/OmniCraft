# OmniCraft

OmniCraft 是围绕原创、二创、内容发现和社区互动形成的全民创意分享平台。本词汇表统一产品设计、执行计划与实现讨论中使用的领域语言。

## Language

**推荐流**:
跨原创与二创展示算法推荐内容、且不向浏览者提供类型或排序筛选的发现界面。排序综合兴趣相关性、热度质量与新内容探索价值，点击率只是信号之一。
_Avoid_: 首页 Feed、原创推荐页、热门列表

**分区页**:
分别承载原创或二创领域结构、分类语义和定向浏览入口的内容界面。原创分区页按内容分类组织，二创分区页以 IP 为核心组织并承载 IP 库入口。
_Avoid_: 推荐页、频道页

**IP 库**:
展示全部 IP 的可检索浏览界面，卡片承载封面、简介、分类与创作数据；由二创分区页进入，点击单个 IP 进入 IP 详情页。
_Avoid_: IP 列表页、IP 索引

**IP 详情页**:
单个 IP 的完整页面，承载简介、关注与讨论区入口及按内容类型筛选的该 IP 二创作品集合；其中的内容卡片与所有内容卡片一样打开内容详情浮层。
_Avoid_: IP 主页、IP 空间

**Agent 工作台**:
由顶部导航进入、承载站内检索问答和受控任务协助的独立全页智能助手空间，不属于内容分区，也不以全局悬浮聊天入口存在。普通写操作必须由用户逐项确认，敏感账号操作不属于其能力范围。
_Avoid_: Agent 频道、AI 内容区、Agent 浮窗、全局聊天挂件

**Agent 会话全文搜索**:
在当前账号全部仍保留的 Agent 会话标题与消息正文中查找文本，不受侧边栏分页或已加载范围限制；每条命中消息都单独列出，按命中时间倒序，并显示会话标题作为来源、正文命中片段和日期。激活结果后进入对应会话、定位命中消息，并以短暂浅灰高亮提示位置。
_Avoid_: 最近对话标题筛选、侧栏列表过滤

**Agent 引用卡片**:
Agent 回答中指向经服务端重新校验的站内公开内容的可聚焦入口；打开后进入内容详情浮层，并在关闭时恢复原对话上下文。
_Avoid_: 外链引用、模型链接、Agent 内容弹窗

**全站搜索**:
顶部导航中的轻量关键词发现入口，负责建议、历史和热搜，不提供 Agent 模式切换；自然语言问答与受控任务协助进入 Agent 工作台。
_Avoid_: Agent 搜索模式、搜索 Agent、AI 搜索切换

**内容详情浮层**:
从任何站内内容卡片入口（推荐流、分区页、IP 详情页、Agent 引用卡片、浮层内关联区块）进入内容详情时，在保留原上下文的同时展示与完整详情页一致内容的覆盖式界面；浮层内访问关联内容形成逐层返回的导航栈，返回类手势（返回按钮、Esc、浏览器后退）逐层弹出，退出类手势（关闭按钮、背板点击）退出整个浮层，完全退出后恢复最初的触发入口、滚动位置和焦点；完整详情页仅服务直达链接。
_Avoid_: 弹窗详情页、推荐卡片弹窗

**用户资料浮层**:
在创作者主页以外，由桌面端悬停或聚焦用户身份入口触发的轻量资料摘要兼导航入口；展示头像、昵称、可选简介、内容发布数、累计获赞、关注者及关注/私信操作，移动端点击身份入口直接进入用户主页。创作者主页本身不重复显示该浮层，用户主页仍是完整资料与内容的权威界面。
_Avoid_: 用户卡片、迷你主页

**私信聊天浮层**:
从用户资料浮层进入、叠加在当前页面或内容详情浮层之上的完整但简化的私信会话界面；桌面默认在当前上下文视口居中，移动端全屏，保留消息历史、昵称标题、文本与单图发送能力，关闭后恢复下层上下文和触发点焦点。
_Avoid_: 私信编辑器、快捷发送框、消息中心跳转

**冷启动私信**:
接收方尚未关注发送方且从未回复时的受限私信状态；发送方只能发送一条纯文本消息，接收方关注或回复后永久解除限制并开放图片发送。
_Avoid_: 陌生人无限私信、首条图片私信
