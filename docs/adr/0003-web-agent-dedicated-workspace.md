# Web Agent 使用独立全页工作台

2026-07-26 接受此决策：Web Agent 由顶部导航进入受保护的独立 `/agent` 工作台，不再以 Root Layout 的全站右下角聊天挂件作为产品入口。全站搜索保持关键词发现职责，Agent 引用卡片与推荐流共用内容详情浮层并在关闭时恢复会话上下文。虽然现有代码采用 `AgentChatWidget` 全局挂载，但独立工作台能避免搜索/Agent 双重模式和两套入口心智，同时为会话导航、流式回答、工具状态、引用浏览及后续受控操作提供稳定页面边界。
