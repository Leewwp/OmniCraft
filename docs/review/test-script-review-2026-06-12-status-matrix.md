# OmniCraft Test Review Status Matrix - 2026-06-12

Status legend:

- `fixed`: required weakness replaced by real behavior evidence
- `partially fixed`: some credible coverage exists, but required repair is incomplete
- `not fixed`: weak test or support script still present as reviewed
- `intentionally deferred`: work is knowingly postponed because the owning contract or rollout path is not active yet

| ID | Severity | Status | Evidence / gap |
| --- | --- | --- | --- |
| TST-001 | Critical | fixed | `e2e/test_search_download.py` now requires `OMNICRAFT_DOWNLOAD_CONTENT_ID` + `OMNICRAFT_DOWNLOAD_ATTACHMENT_ID`, calls the real download route without auth, and fails unless it gets the exact unauthenticated contract (`status=401`, `code=UNAUTHORIZED`). |
| TST-002 | Critical | fixed | `e2e/test_search_download.py` now splits verified success, unverified rejection, low-reputation rejection, and missing-content rejection into separate real API checks with exact status/code assertions and signed-URL shape checks. |
| TST-003 | Critical | fixed | `e2e/test_search_download.py` no longer records warnings as passes; the unverified-user path now requires an explicit fixture token or JWT-backed fixture user and fails unless the real route returns `403 EMAIL_NOT_VERIFIED`. |
| TST-004 | High | fixed | `e2e/test_search_download.py` now captures the real `/api/v1/contents/search?q=...` browser request, asserts the returned payload contains the expected seeded title, and verifies the rendered UI shows that title. |
| TST-005 | High | fixed | `e2e/test_search_download.py` now treats screenshots as evidence only; pass/fail requires visible search input landmarks, successful request capture, expected result rendering, and forbidden-title absence checks. |
| TST-006 | Critical | fixed | `e2e/test_admin_journey.py` removed DB role/report fallbacks and now creates a real report fixture through `POST /api/v1/contents/:id/report`, resolves it only through `PATCH /api/v1/admin/reports/:id`, and verifies the follow-up audit row via the API. |
| TST-007 | High | fixed | `e2e/test_admin_journey.py` now creates required report and feedback fixtures up front and fails the run if either fixture cannot be created or discovered. |
| TST-008 | High | fixed | `e2e/test_admin_journey.py` now requires `/api/v1/admin/audit-logs` to return the exact `{items,total,page,page_size}` schema and validates matched audit entries for both `report_resolve` and `feedback_close`. |
| TST-009 | Medium | fixed | `e2e/test_admin_journey.py` no longer mutates user roles in the database; it now logs in real fixture accounts and fails setup unless `/api/v1/auth/me` confirms admin vs non-admin roles. |
| TST-010 | Medium | fixed | Root Python E2E is now explicitly labeled manual-only in `e2e/README.md`, runnable via `scripts/run-root-python-e2e.ps1`, and exposed through `frontend/package.json::test:e2e:release-manual` as a documented non-CI entry point. |
| TST-011 | High | fixed | Real runtime coverage now lives in `frontend/tests/register-captcha.test.tsx` and `frontend/tests/captcha-ticket-reset.test.tsx`, proving empty-token submit blocking, stable captcha/button wiring, and captcha remount after failed register/forgot-password/settings resend flows. |
| TST-012 | High | fixed | `frontend/components/verification/CaptchaWidget.test.tsx` now initializes the widget, executes the Aliyun callback, verifies `POST /api/v1/captcha/verify`, and proves only the server-issued `captcha_token` reaches `onToken`. |
| TST-013 | High | fixed | `frontend/tests/feedback-form-upload.test.tsx` now exercises failed OSS upload behavior and anonymous feedback retry/remount behavior with real runtime form submission, proving failed uploads do not retain attachment grants. |
| TST-014 | Critical | fixed | Real service and handler coverage now replaces skip-based source scans: `backend/internal/service/agent_publish_assist_test.go::TestUploadAssistInvalidJSONFallsBackToOriginalMetadata`, `TestUploadAssistReturnsProviderError`, `TestComplianceCheckParsesStructuredRiskLevel`, `TestComplianceCheckInvalidJSONFallsBackToSafeReason`, plus `backend/internal/handler/agent_test.go::TestUploadAssistHandlerReturnsStructuredResponse`, `TestComplianceCheckHandlerReturnsStructuredResponse`, `TestAgentHandlersRejectInvalidJSON`. |
| TST-015 | High | fixed | Real agent chat coverage now lives in `backend/internal/service/agent_chat_test.go::TestChatStreamStoresContentContextAndInjectsPageContext`, `TestChatStreamUsesPageContextTypeForRouteOnlyContext`, plus `backend/internal/handler/agent_test.go::TestChatStreamHandlerFiltersRolesTruncatesMessagesAndLimitsContextWindow`, proving page-context prompt injection, persisted `context_type`, invalid-role filtering, truncation, and context-window limiting against real provider requests. |
| TST-016 | Medium | fixed | `backend/internal/handler/routes_security_test.go` now exercises real routed behavior: credential endpoints hit `429 CREDENTIAL_RATE_LIMIT_EXCEEDED` after repeated requests, `/auth/csrf` stays unaffected, and `/agent/chat/stream` hits `429 AGENT_RATE_LIMIT_EXCEEDED` through the actual auth + interaction + route middleware chain. |
| TST-017 | High | fixed | Local-map schema test was replaced by real handler evidence in `backend/internal/handler/content_download_test.go::TestContentDownload_ResponseIncludesSignedURLAndExpiresIn`, which hits `DownloadContent` and decodes the actual JSON body. |
| TST-018 | Medium | fixed | Skip-based route checks were replaced by real router/middleware tests in `backend/internal/handler/content_download_test.go::TestContentDownloadProtectedRouteRequiresAuth`, `TestContentDownloadProtectedRouteRejectsUnverifiedUser`, and `TestContentDownloadProtectedRouteRejectsLowReputationUser`. |
| TST-019 | Medium | fixed | Primary evidence now comes from real `ValidateRelease()` behavior tests in `backend/config/config_test.go`, covering valid production config plus concrete rejection cases; `backend/cmd/server/main_test.go` remains only as a secondary source-order guard to catch accidental removal before external init. |
| TST-020 | Medium | fixed | `backend/internal/repository/search_repo_test.go` now keeps runtime visibility/search evidence as the primary proof and replaces the `tags.status` source scan with `TestSearchSuggestionsIncludesTagsWithoutRequiringMissingStatusColumn`; the weak migration text scan was removed so the file no longer treats static fragments as runtime evidence. |
| TST-021 | High | fixed | `frontend/e2e/helpers/mock-api-guard.ts` now fails unexpected `/api/v1/**` traffic in mocked suites, `frontend/playwright.config.ts` splits a dedicated `contract-smoke` project, and `frontend/e2e/contract-smoke.spec.ts` exercises real HTTP frontend/API compatibility for search and protected-route redirect without `page.route()` interception. |
| TST-022 | Medium | fixed | `frontend/e2e/search-filter-contract.spec.ts` is now explicitly labeled mocked request-shape coverage, while real backend semantics are covered by `backend/internal/repository/search_repo_test.go::TestSearchContentsFiltersByContentTypeAndTimeRange` and `backend/internal/handler/search_contract_test.go::TestSearchContentsRouteAppliesContentTypeAndTimeRangeFilters`. |
| TST-023 | High | fixed | `frontend/e2e/studio-publish-fanwork.spec.ts` is now explicitly a mocked payload/UI suite, and real fanwork source handling is covered by `backend/internal/service/content_source_test.go::TestValidateSourceOriginalLink` plus `backend/internal/handler/content_publish_route_test.go::TestCreateContentRoutePublishesFanworkAndReadsBackSourceRelation` and `TestCreateContentRouteRejectsInvalidSourceOriginal`. |
| TST-024 | Medium | fixed | Real publish-permission and protected-route behavior now live in `backend/internal/handler/content_publish_route_test.go::TestCreateContentRouteEnforcesPublishPermissions` plus `frontend/e2e/contract-smoke.spec.ts::protected publish route redirects unauthenticated users to login without mocked routes`. |
| TST-025 | Medium | fixed | `frontend/e2e/admin-control-semantics.spec.ts` is now explicitly mocked UI semantics coverage, while real admin-only create/persist/audit behavior is covered by `backend/internal/handler/admin_contract_test.go::TestAdminCreateCategoryRouteRequiresAdminRole`, `TestAdminCreateCategoryRoutePersistsCategoryAndAuditRow`, and `TestAdminCreateCategoryRouteRejectsInvalidZone`. |
| TST-026 | Medium | fixed | Real API schema coverage now lives in `backend/internal/handler/admin_contract_test.go::TestAdminEndpointsReturnExpectedSchemas`, and the mocked admin fixtures were reduced toward real response shapes for audit logs, feedback, reports, and public config/search payloads. |
| TST-027 | Low | intentionally deferred | Current UI layout checks remain useful mocked UI coverage, but they should be renamed/labeled rather than treated as functional E2E evidence. |
| TST-028 | Critical | fixed | `scripts/seed-test-data.sh` now obtains auth token via login API (`SEED_EMAIL`/`SEED_PASSWORD` env vars), uses `set -euo pipefail`, and every `api_post` helper checks HTTP status code — exiting on 4xx/5xx. Hard-coded JWT removed. |
| TST-029 | High | fixed | `scripts/browser_test.py` now calls `sys.exit(1)` when `errors` list is non-empty after the test loop, ensuring CI pipelines detect browser test failures. |
| TST-030 | High | fixed | `scripts/browser_test.py` now resolves page URLs dynamically via `api_get()` calls (IPs, original content, fanwork content) instead of hard-coding mutable database IDs. |
| TST-031 | High | fixed | All Python seed scripts (`seed_data.py`, `seed_extra.py`, `seed_last.py`) now include `api_fatal()` for critical operations (login, IP creation), which calls `sys.exit(1)` on failure instead of silently continuing. |
| TST-032 | Medium | fixed | `scripts/seed_data.py` now fetches existing IPs before creating and skips already-existing ones by name (idempotent rerun). A verification pass at the end queries the API to confirm data was written. |
| TST-033 | Medium | fixed | `scripts/beta/tests/Test-BetaScripts.ps1` now uses a structured `$destructiveGuards` array with explicit `# DESTRUCTIVE-GUARD: <category>` labels (git-safety, branch-protection, force-push, permission-bypass). Added AST-based Common.ps1 function export verification and beta-tasks.psd1 data integrity checks. |
| TST-034 | Low | fixed | `backend/config/config_test.go` now uses dual-layer assertions: `HasPrefix` on stable error prefix (`"release mode configuration error"`) plus `Contains` on field-path identifiers, replacing substring-only matching. Comment explains why structured error code assertion is not possible (plain `fmt.Errorf` return). |
| TST-035 | High | fixed | `backend/internal/pkg/captcha/verifier_test.go::TestAliyunV2VerifierRejectsCompatibilitySuccessShapes` now proves undocumented success-code/result compatibility shapes fail closed, alongside the existing real request-shape tests. |
| TST-036 | Medium | fixed | `backend/internal/pkg/llm/provider_real_test.go` now contains real provider construction tests (`TestNewOpenAICompatProvider_SetsFields`), factory routing tests (`TestNewProviderFromConfig_RoutesCorrectly`), and `httptest`-backed serialization tests (`TestOpenAICompatProvider_Chat_Serialization`, `TestOpenAICompatProvider_GetEmbedding_Serialization`) verifying Authorization headers, request paths, and JSON body shapes. |
| TST-037 | Medium | fixed | `backend/internal/pkg/queue/queue_test.go::TestRetryAndDLQ_PayloadAndMetadataPreserved` now creates a permanently-failing handler, waits for retries to exhaust, reads from the `omnicraft:dead-letter` stream, and verifies the DLQ entry contains original payload, `original_topic`, `error`, `failed_at` (RFC3339), and `attempts`. Also fixed `msg.Attempts` not being updated in the retry loop. |
| TST-038 | High | intentionally deferred | Desktop security Beta path is not yet active in this review stream; HMAC coverage remains legacy-only until Ed25519 contract work lands. |
| TST-039 | Medium | fixed | `tauri-client/src-tauri/src/commands/file_ops.rs` now includes `test_extract_archive_rejects_zip_slip` (malicious `../../../tmp/evil.txt` zip entry rejected), `test_move_file_overwrite_creates_backup_with_original_content` (backup in `.omnicraft_backup/` preserves original), and `test_backup_file_internal_fails_gracefully_on_readonly_dir` (returns `Err`, no panic). |
| TST-040 | Low | fixed | `tauri-client/src-tauri/src/commands/env_detect.rs` now includes `test_detect_environment_with_custom_home` (custom USERPROFILE/HOME reflected in home_dir), `test_detect_environment_handles_empty_vars` (empty vars → Ok with fallback), and `test_detect_environment_steam_paths_is_vec` (steam_paths always a valid Vec). |
| TST-041 | Medium | fixed | Real typed config and PostgreSQL migration evidence now exists in `backend/config/config_test.go::TestDefaultConfigDeclaresCreatorSupportDisabled`, `TestDefaultConfigHasAbuseControlLimits`, `backend/internal/model/content_migration_test.go::TestContentSourceMigrationAddsColumnsIndexAndForeignKey`, and `backend/internal/service/review_remediation_test.go::TestRehabStartedAtMigrationBackfillsColumnAndRelaxesNullability`. |
| TST-042 | Medium | fixed | `backend/internal/service/agent_service_test.go::setupAgentVisibilityDB` now asserts `AutoMigrate` owns `deleted_at` columns instead of hand-patching them into the schema. |
| TST-043 | Medium | fixed | Real rendered download behavior is now covered by `frontend/tests/sheet-music-viewer-download.test.tsx` with authorized API calls for download-only, previewable, and mixed attachment sets. |
| TST-044 | Low | fixed | Browser-style cookie integration is now covered by `frontend/lib/api.cookie.test.ts::api.post uses csrf-token cookie without bootstrapping a csrf fetch first` and `api.post decodes __Host-csrf cookie values before sending the csrf header`. |
| TST-045 | High | fixed | All seed scripts (`seed_data.py`, `seed_extra.py`, `seed_last.py`, `seed-test-data.sh`) now read credentials from `SEED_EMAIL`/`SEED_PASSWORD`/`SEED_BASE_URL` environment variables instead of hard-coding them. |
| TST-046 | Medium | fixed | `backend/testdata/search_seed.sql` now uses `ON CONFLICT (id) DO UPDATE SET` for users, ips, and content_items tables, ensuring fixture data is always updated to match on rerun. Header comment documents reserved ID ranges (9001–9099, 8001–8099, 7001–7099). |
| TST-047 | Medium | fixed | `frontend/playwright.config.ts` now defines separate `mocked` project (contract-smoke + *.mock.spec.ts, frontend-only server), `cross-stack` project (*.integration.spec.ts, dual backend+frontend servers), and `desktop`/`mobile-chrome` projects for standard E2E. |
| TST-048 | Medium | fixed | Source-scan tests replaced with runtime tests: `frontend/tests/error-handler-i18n.test.ts` imports `handleApiError` and verifies 429→`common.rateLimited` translation key, 401→no toast, 500→error toast; `frontend/tests/content-upload-grant.test.tsx` uses React Testing Library to verify `grant_id` passthrough and graceful missing-grant handling. |

## Current fixed set with real evidence

All items except TST-027 and TST-038 (intentionally deferred):

- `TST-001` through `TST-026`
- `TST-028` through `TST-037`
- `TST-039` through `TST-048`

## Intentionally deferred

- `TST-027` (Low): UI layout checks should be renamed/labeled
- `TST-038` (High): Desktop security Ed25519 contract not yet active
