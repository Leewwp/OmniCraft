# 2026-09-02 四批次整合裁决与执行总序（唯一执行顺序权威）

> 创建日期：2026-09-02 ｜ **预计失效日期**: 2026-10-31（全部批次落地后即失效；预计 10 月中旬完成）
> 来源：用户 2026-09-02 整合指令（本会话）。输入 = 四个规划会话产物：
> ① 全站设计审计（sess_71b4…，96+21 条发现 → 44 项修复 → SP-00~11 = #209~#220 → T01~T55 = #221~#275）
> ② UI 精细化 SP-12（sess_95d3…，#276 + U-01~U-05 = #277~#281）
> ③ Agent 工作台重造 SP-13（sess_1910…，#282 + A-01~A-07 = #283~#289）
> ④ IP 详情页贴吧式重构（sess_9ebb…，#290 + handoff 文档 + 交互原型）
> 权威关系：本文档只裁决**跨批次顺序与重叠裁剪**；各批次内部范围以各自 spec / 票面为准；车道与验证门以 AGENTS.md 为准。冲突时：宪法 > 生产代码 > 各批次 spec > 本文档。

---

## 0. 整合裁决声明（冻结解除）

四个批次原先全部「执行冻结，等用户整合所有方案后统一下达」。**2026-09-02 用户下达整合指令，冻结就此解除**：按本文档 §3 总序执行，不再需要逐批二次确认。简历投递门槛维持 2026-09-01 裁决：**U-01~U-05 + 全部 T 票 + 两个重构（#290、SP-13）全部落地**后才启动投递。

本文档与配套 idle goal 的执行范围固定为本页列出的执行单元：T01–T55（#221~#275）、U-01~U-05（#277~#281）、A-01~A-07（#283~#289）和 #290；#276/#282 仅为协调父票，不实现、不关票。活计划中的 #204（面试证据收口）与 #32（简历/live-demo 叙事）仍是独立下游工作，不因本总序完成而自动关闭或代办。

## 1. 四批次事实基线（2026-09-02 核验）

- **T 票启动基线（整合裁决时）**：#221~#275 全部 OPEN，0 张已执行。按票内 `## Blocked by` 统计，35 张无内部前置（#221~#237、#239~#256）；#238 blocked_by #278，#257~#275 为下游票。#237 虽无内部前置，按本总序固定在第 5 段；下游边见 fix-plan §3.1 与票面。硬前置约定包括 FIX-19（T03 #223）先于 FIX-29①（T45 #265）。
- **U 批次**：#277 → #278 → {#279, #280, #281}；T18 #238 blocked_by #278。U-03/U-04 共享 `IPBrowseClient.tsx` 需串行。
- **A 批次**：A-01 #283 / A-02 #284 / A-03 #285 / A-05 #287 均为 frontier；A-04 #286 ← A-03；A-06 #288 ← A-01 + A-02；A-07 #289 ← A-06。A-01 heavy（迁移），其余 light。
- **#290**：单票大重构（45 user stories，迁移 073+，heavy）。无票内依赖；spec 自述吸收 FIX-25 / F-064 / 类目页死排序 / mod label 错映射，不吸收 FIX-18（T12）/FIX-23（T15）。
- 外部输入：DashScope API Key 待用户开通（A-03/A-04 真实数据用；本地 fail-open 不阻塞）；GREEN / MiniMax 已恢复可用。

### 1.1 当前执行 checkpoint（2026-09-02，U-04 收口后）

- 固定 goal 范围共 **68 个执行单元**：T01–T55（55）+ U-01~U-05（5）+ A-01~A-07（7）+ #290（1）。其中 **17 个已完成并在 GitHub 以 `CLOSED/COMPLETED` 收口**：T01~T09（9）、T18~T20（3）、T22（1）、U-01~U-04（4）；剩余 51 个，#276/#282 仍为协调父票不计入。
- 第 3 段已收口；第 4 段进行中：U-03（a39b0fc）与 U-04（60accf0）已收口，剩 **U-05（#281）第二批 sweep：admin+studio**——完成后第 4 段收口并跑段边界 `verify-project.sh --full`。
- 下一张可执行票是 **U-05（#281）**；注意 T41~T44 与 U-05 共享 studio 页面（T 票在第 8 段，当前无冲突）。
- 用户 checkpoint 摘要中的“14/45”不是本总序固定范围的分母；后续 agent 以本节 68/17/51 和 GitHub 实际状态计数，不据此提前宣称总序或投递门槛完成。

## 2. 冲突整合裁决（重叠裁剪，逐项）

| # | 冲突 | 裁决 | 落点 |
|---|------|------|------|
| C-01 | U-04 含「Agent 工作台」逐页样式 sweep，而 A-06 将 1:1 重建工作台 UI | **U-04 剔除 Agent 工作台范围**（先扫后拆=废弃工作）；该范围由 A-06 重建时直接按 U-01/U-02 token 实现 | #280 票面已修订；A-06 验收吸收 |
| C-02 | mod label 错映射双重归属：SP-12 会话归 U-04，#290 会话归 #290 | 归 **U-04**（先执行，一行修复 `frontend/components/ip/ipCategory.ts:12` `home.other`→`home.mod`，已核实）；#290 继承修复后映射，仅校验不重修 | U-04；#290 已注记 |
| C-03 | T17（FIX-25：IP 详情可见性 + admin 缓存失效）被 #290 吸收，但 #290 的生效链只覆盖提案采纳路径的缓存失效，不覆盖 admin 审核操作路径 | **拆分**：IP 详情可见性部分并入 #290（spec 已吸收）；T17 **收窄为「admin IP 状态变更缓存失效（含 E2E B-004 admin.go:66 折叠）」**，独立保留在后端路径 | #237 票面已修订 |
| C-04 | T18（气泡/引用卡组件对比度修复）与 A-06 重建重叠 | **保留并前置**：U-02 后立即执行（F-007 为 P0，当前 demo 亮色主题用户气泡 1.09:1 不可读）；A-06 重建时**继承语义**（重建后对比度不得回退），不视为废弃 | #238 维持 blocked_by #278；A-06 验收注记 |
| C-05 | T19（SSE 跨 chunk 行缓冲）/ T20（429/用量/markdown/配置 TTL）与 A-02 契约 v2 / A-06 重建重叠 | **保留为行为基线**：均为当前演示路径上的真实功能缺陷（T19 事件静默丢弃为 P1），在快赢段先修；A-02/A-06 的新 SSE 客户端与用量 UI **必须继承**这些行为语义 | #239/#240 不动；A-02/A-06 验收注记 |
| C-06 | A-06 跨批次依赖（spec 明示「由用户整合裁决」） | A-06 的完整前置 = **A-01 #283 + A-02 #284 + U-02 #278（token 基座）+ T18/T19/T20（#238/#239/#240 行为基线）** | #288 票面已增补 |
| C-07 | T21（搜索建议/Users tab/图标语义）、T22（热搜契约）与 A-07（搜索页 Agent 入口统一）同页不同层 | **不裁剪**：T21/T22 是功能修复，A-07 只统一 Agent 入口；T21 先于 A-07 执行，A-07 重排入口时继承 | 无票面改动 |
| C-08 | T24 含「/ips 排序词表补 most_contents 别名」，#290 吸收「类目页死排序」 | **两者不同物**：#290 吸收的是 IP 详情类目子路由（旧 `/ip/[id]/[category]`，将被 tab+query 取代）的死排序；T24 修的是全局 `/ips` 库页排序词表。各自保留 | 无票面改动 |
| C-09 | U-05（studio 样式 sweep）与 T44（studio 功能：全状态/编辑/删除/申诉入口）同域不同层 | **不裁剪**：样式与功能分层；U-05 先行，T44 新增 UI 遵循 U-01 定稿规范 | 无票面改动 |
| C-10 | #290 与 U 批次的前后关系 | #290 排 U 批次之后：消费 U-02 token、U-03 FilterPills 与 U-01 规范；TagBadge 按 R2 裁决保留 | 本文 §3 |
| C-11 | A-04 消融评测需要真实 DashScope 凭证 | 非阻塞：A-03/A-04 本地 fail-open 实现先行；key 缺失期间消融数据标注「外部输入待补」，**禁止以 mock 数据冒充消融结论** | 执行纪律，无需票面改动 |

**裁剪净效果**：唯一实质「优化不进行」= C-01（U-04 的工作台 sweep 项移交 A-06）；其余重叠均以「行为基线前置 + 重建继承语义」处理，不丢弃当前 demo 路径上的真实缺陷修复。

## 3. 整合执行总序

串行主线（单 agent 默认；多 agent 并行注记见 §3.1）。段内无特别说明时顺序自由，但必须遵守票内 `## Blocked by`。

### 第 1 段 视觉基座（阻塞一切视觉工作）
1. **U-01（#277）** 规范定稿：design-system.md + ui-spec.md 修订 + token 差异清单 ✅ 2026-09-02 完成（commit 518463f；design-system v3.0 分层画布/高度三档/形状语义/选中态/动效 + FIX-05 dark token 定稿，ui-spec FilterPills/IP hero/类目并入，差异清单 docs/working/2026-09-02-u02-token-diff-list.md）
2. **U-02（#278）** token 换血：表面分层 + dark token 达标（FIX-05/F-111 收口）+ Button 三档高度 ✅ 2026-09-02 完成（globals.css 两对画布 token + dark 三 token + radius-md 8px；Button/Input/Select 三档高度；15 处 bg-canvas-default 卡片面改 bg-card；verify-project.sh --full 全过；20 张亮暗×双断点截图 screenshots/u02-*；对比度复算全对 ≥4.5:1）——**第 1 段完成**（段边界 verify-project.sh --full 通过 2026-09-02）

### 第 2 段 Agent 行为基线（保当前 demo 正确，A-06 继承）
3. **T18（#238）** 工作台气泡/引用卡代码修复（blocked_by #278 恰在此满足） ✅ 2026-09-02 完成（气泡 bg-accent+白→bg-primary+primary-foreground 双主题 6.29:1；引用卡经 U-02 token 已达标仅验证；双主题截图 t18-bubble-*）
4. **T19（#239）** SSE 跨 chunk 行缓冲 ✅ 2026-09-02 完成（前端行缓冲+2MiB 上限+流尾 flush；4 例分块单测；本地抽查 SSE 管线无丢事件，引用路径待真实 LLM key）
5. **T20（#240）** 429 文案 / 用量 / markdown / 配置 TTL ✅ 2026-09-02 完成（AgentStreamError 透传 code+429 专属文案+窗口内隐藏重试；start/usage 消费+本轮详情折叠区；助手消息受控 markdown（无 raw HTML 通道）；public config 5min TTL+gate 错误态重试）——**第 2 段完成**（段边界 verify-project.sh --full 通过 2026-09-02）

### 第 3 段 审计快赢批次 A（FIX-01~09，解锁演示主线）
6. **T03（#223）** email model 层收敛（隐私 P0，最先） ✅ 2026-09-02 完成（model json:"-" 全 Preload 出口收口 + public/self 两档 + login//auth/me 显式回填；7 例 TDD + curl 三角色 7 出口 + 两阶段审查；heavy worktree b2820bb 合并 3bfd5f9）
7. T01（#221）判官考试字段对齐 ✅ 2026-09-02 完成（answer_key→answer 一行修复 + 契约 e2e 红→绿 + 本地题库播种后真实全对提交 10/10 资格授予；heavy 22bf4b1 合并 a426d52） → T02（#222）stats 状态词 ✅ 2026-09-02 完成（published→approved 一行修复 + TDD 单测红→绿；live ips=16 复核 + 首页 16 活跃 IP 视觉验证）→  → T05（#225）admin reports 契约 ✅ 2026-09-02 完成（{status,action_taken} 契约 + action_taken 展示 + 错误红字透传弹窗保持；维持/驳回/400 错误三路径浏览器验证）→  → T04（#224）本地 worker 启动 ✅ 2026-09-02 完成（Notify(0) 幽灵删除 TDD 红→绿 + 9 条积压清理重建 group + AGENTS Step 2 worker 行与 lag 检查；闭环实测评论通知落库 lag=0）→  + 积压清理（D 批触达前提）→ T06（#226）收藏错链 ✅ 2026-09-02 完成（两处侧栏 href+redirect 修正、StudioSidebar 收藏集入口、IP 入口保留；浏览器三态验证）→  → T07（#227）admin 导航缺口 ✅ 2026-09-02 完成（Header role=admin 菜单项 + ADMIN_NAV 补广播页；admin 可见可跳转/普通用户不可见）→  → T08（#228）封禁理由全链路 ✅ 2026-09-02 完成（model 字段+作者/admin 出口+admin {reason} 发送+studio banned 行原因展示；TDD 5 例+浏览器全链路+两阶段审查；heavy e81cb93 合并 a3de204）→  → T09（#229）内容可见性 scope ✅ 2026-09-02 完成（详情 viewer-aware 404 + 列表/热榜/推荐统一 scope + 缓存与 view_count 门；TDD+curl+延迟实测+联合审查 P1 已修；heavy 1f62c53 合并 ef16ae6）→  → T22（#242）热搜契约 ✅ 2026-09-02 完成（GetTrending join content_items 解析真实标题 + content_id 契约 + ApplyContentVisibilityScope 公开口径过滤 banned/审核中/私密/已删/封禁作者；前端读 trending 契约渲染真实标题+深链 /content/{id}+空榜单降级；废弃 home.trendingN 假榜单键；TDD 契约测试红→绿 + curl 混合成员池实测 + 浏览器深链/降级验证 + 亮暗×1440/375 截图 4 张；light 直接 main 提交 4375a3e）——**第 3 段完成**（段边界 verify-project.sh --full 通过 2026-09-02：75/75 + 全部工程门；附带修复两处既有 flaky e2e = 593c527：judge-exam 徽章改 exact 匹配、t97 设置保存 mock 加 300ms 延迟使乐观 UI 中间态可观测）

### 第 4 段 UI 扩散
8. **U-03（#279）** FilterPills 共享组件 + 全站筛选就地化 ✅ 2026-09-02 完成（`ui/filter-pills.tsx` 按 ui-spec 定稿；IP 库消费；IP 详情类目就地切换+URL replace+SSR 首屏+旧路由 redirect+删除 IPCategoryTabs；/original 类目/排序就地化（OriginalFeedClient）+删除旧 CategoryTabs/SortSelect；ContentTypeFilter 收敛药丸形态；mock e2e 77/77 含新增 ip-detail-category 契约（stub 18080 供 SSR+浏览器）；verify --full EXIT=0；真机 URL/滚动/刷新恢复/redirect 验证 + 截图 8 张 u03-*；light 直接 main 提交）
9. **U-04（#280，已剔除工作台范围）** 第一批 sweep：公开页 + 用户侧 + 进入讨论区对齐 + mod label 修复 ✅ 2026-09-02 完成（hero 操作行同排同高 28px 档 + 8px 矩形（DOM 实测 28=28 同排 8px）；mod→home.mod 类目唯一化；非主 CTA active:scale 全清 + 筛选 chip 药丸化 + 药丸形主操作 CTA 矩形化（ContentSidebar/RelatedFanworks/IPBrowseClient 创建按钮）+ 部署 CTA 36px 档；22 页逐页清单核销；tsc/单测/build/mock e2e 77/77 绿；截图 u04-*×4；light 直接 main 提交）
10. **U-05（#281）** 第二批 sweep：admin + studio

### 第 5 段 重构一：IP 详情页（#290）
11. **#290** 贴吧式社区枢纽（heavy：迁移 073+、共治提案域、单页三模块、IP 内搜索；吸收可见性/F-064/类目页死排序；mod label 由 U-04 已修则仅校验继承；消费 U-02/U-03 成果；原型 `/prototype/ip-detail-hub` 为脚手架，落地后移除该原型路由）
12. **T17（#237，收窄版）** admin IP 状态变更缓存失效 + B-004（admin.go:66）折叠——同域紧随 #290

### 第 6 段 重构二后端链（SP-13）
13. **A-01（#283，heavy）** 会话模型重构（title/pinned_at 迁移、续写、自动标题、停止保留）
14. **A-02（#284）** SSE 契约 v2（真流式 + 思考块 + 工具步骤事件；同步 web-agent-v0.4-mvp.md；继承 T19 行缓冲语义）
15. **A-05（#287）** 护栏补口（chat 输入前置 Green + 输出事后异步审核）
16. **A-03（#285）** 检索管线升级（v4@1536 零 DDL + hybrid on Postgres + 查询扩展 + qwen3-rerank 三开关）
17. **A-04（#286）** 评测消融与门禁（blocked_by A-03；真实数据待 DashScope key，缺 key 时实现+mock 管线先行、结论标注待补）

### 第 7 段 重构二前端收口
18. **T21（#241）** 搜索建议 + Users tab + 图标语义（先于 A-07 稳住搜索页功能面）
19. **A-06（#288）** 工作台 DeepSeek 化（前置已全部满足：A-01/A-02/U-02/T18/T19/T20；吸收 U-04 剔除的工作台 sweep 范围；对比度与行缓冲语义不得回退）
20. **A-07（#289）** 搜索页 Agent 入口统一（blocked_by A-06）

### 第 8 段 审计长尾（批次 B→C→D→E，票内 DAG 不变）
按 fix-plan §1.3 总序 + 票面 Blocked by 执行剩余 T 票：
- **B 审核链路**：T29（#249 申诉五步）、T30（#250 注销软删除，触及迁移按 heavy）、T31（#251）、T32（#252）、T33（#253）、T10（#230）、T11（#231）→ T41（#261）→ T42（#262）→ T43（#263）→ T44（#264）
- **C PR/IP 协作**：T13（#233）→ T14（#234）→ {T48（#268）、T49（#269）、T50（#270）、T51（#271）}；T12（#232）、T15（#235）、T16（#236）→ T52（#272）
- **D 触达反馈**：T45（#265，前置 T03 已在第 3 段完成）→ T46（#266）→ {T47（#267）、T54（#274）}；T23（#243）、T25（#245）、T35（#255）、T36（#256）、T53（#273）、T55（#275）
- **E admin/判官/打磨**：T26（#246）、T27（#247）、T28（#248）、T34（#254）、T37~T39（#257~#259）、T40（#260）、T24（#244）

### 3.1 并行注记（仅当多 agent 同时在场）
- **Lane F（前端视觉线）**：第 1 段 → T18 → 第 4 段 → #290 → T17
- **Lane B（后端/行为线）**：T03 等 P0 快赢 → 第 2 段（T19/T20）→ 第 6 段
- 汇合点：A-06 = Lane F 的 U-02 ∪ Lane B 的 A-01/A-02 + T18/T19/T20；A-07 之后进入第 8 段长尾。
- 共享文件红线：U-03/U-04 共享 `IPBrowseClient.tsx`；T41~T44 与 U-05 共享 studio 页面——跨 lane 时串行。

## 4. 票据处置清单（2026-09-02 已执行）
- #280（U-04）：范围修订——剔除 Agent 工作台 sweep（移交 A-06），保留公开页+用户侧+进入讨论区+mod label。
- #237（T17）：范围修订——可见性部分并入 #290，收窄为 admin IP 状态变更缓存失效 + B-004 折叠。
- #288（A-06）：Blocked by 增补 #278/#238/#239/#240；范围增补「承接 U-04 剔除的工作台 sweep + 继承 T18/T19/T20 行为语义」。
- #290：注记 mod label 由 U-04 先修、此处仅校验；原型路由在落地时移除。
- #276/#282/#290 及全部 U/A 票：冻结横幅更新为「已解除（2026-09-02），按本文档总序执行」；#276/#280/#237 的旧范围段已同步清理，避免与整合裁决冲突。
- 不新增票据：全部执行单元已有票面（#221~#290），本次整合只做范围修订与排序，无需 to-tickets 立新票。

## 5. 执行纪律（引用 AGENTS.md，不重复展开）
- 车道：A-01、#290、T30 及一切触及迁移/安全/auth 的票按 heavy（TDD、一任务一 commit、两阶段审查）；其余 light。
- 每票验证门：`go build/vet/test` + `npm run build/lint` + UI 浏览器验证截图存 `screenshots/`；UI 票追加亮暗 × 1440/375 截图。
- 批段边界跑 `bash scripts/verify-project.sh --full`；改 config.go/migrations/routes.go 后跑 doc-validator `--fix`。
- 阻塞协议、本地开发模式例外、progress.txt 记录、`git add` 精确文件列表——均按 AGENTS.md 原文。
- 每完成一票：勾票（close as completed）+ progress.txt 条目；段完成时在本文档 §3 对应条目后追加 ✅ 与 commit 区间。
