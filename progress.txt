# OmniCraft 万象工坊 - 开发进度日志

> **轮换规则**：本文件仅保留最近约 30 天的条目；更早记录已归档至 `docs/archive/progress/`。每月或文件超过 64KB 时轮换一次（上次轮换：2026-07-23）。

## [2026-07-02] - Community browse-history: review repair

### What was done:
- Verified `codex/community/browse-history` against the community design spec, plan file, UI spec, and current validation gates.
- Fixed `/history` so it initializes `content_type`, `start_date`, and `end_date` from URL query parameters.
- Added inline validation for invalid date ranges (`start_date > end_date`) so the page does not send an invalid request.
- Fixed mobile history filter chips so labels stay on one line and scroll horizontally instead of wrapping into vertical text.
- Added focused frontend tests for URL-query initialization and invalid date range behavior, plus localized error text.

### Testing:
- `node --import tsx --test tests/history-page.test.tsx`: passed.
- `npm run test`: passed.
- `npm run lint`: passed.
- `npm run build`: passed.
- Prior review verification in this session also passed backend `go test ./...`, `go vet ./...`, `go build ./...`, focused browse-history backend tests, and `tools/doc-validator --fix`.

### Notes:
- Existing screenshot artifacts remain under `screenshots/community-browse-history-*.png`; this repair changes query/date validation behavior and does not alter the primary layout.
- `task.json` and Beta roadmap checkboxes were not modified.

## [2026-07-02] - Community Docs: Review Follow-up Repairs

### What was done:
- Reviewed the attached community documentation audit against the current `codex/community-integration` branch.
- Repaired true documentation gaps in the community development guide, community design spec, and six community plan files.
- Added explicit guidance for migration numbering checks, `tsx` focused-test readiness, focused gates, BLOCKED output, rollback flow, shared error-code registry, UI spec coverage, performance expectations, migration rollback comments, pagination defaults, compatibility windows, and concurrency/transaction edge cases.

### Testing:
- Verified changes with targeted `git diff` and `rg` checks across the community docs.
- No code tests were run because this task only changes Markdown documentation.

### Notes:
- Several audit findings were already covered locally or were terminology mismatches; most notably `content_type` is a content format enum and not the public category taxonomy from `AGENTS.md`.
- `task.json` and Beta roadmap checkboxes were not modified.

## [2026-06-29] - Task P3-1/P3-2: Implement doc-validator core tool

### What was done:
- Created tools/doc-validator/ Go CLI tool with --fix and --check modes
- rules/config_sync.go: Parse config.go with Go AST, extract 90+ config fields with mapstructure tags, flatten nested structs to dot paths, generate markdown table for architecture.md section 7
- rules/schema_sync.go: Parse 56 migration SQL files, extract CREATE TABLE statements with columns/constraints/references, generate documentation tables for section 4
- rules/route_sync.go: Parse routes.go with Go AST, extract 130+ routes with method/path/handler, generate route table for section 3.2
- rules/token_refs.go, rules/cross_refs.go: Stub implementations (full in Task 16)
- Added AUTO-GENERATED HTML comment markers to architecture.md around sections 3.2, 4, and 7
- README.md with usage and check descriptions

### Testing:
- go build ./...: passed
- go vet ./...: passed
- go run . --fix: Regenerated all three auto-generated sections
- go run . --check: "All checks passed" (6 checks)
- Second go run . --fix: "no changes needed" (idempotent)
- go run . --check after second fix: still "All checks passed"

### Notes:
- Config parsing uses Go AST (go/ast, go/parser) for accurate struct field extraction
- Route parsing walks AST call expressions to find Gin .GET/.POST/.PUT/.PATCH/.DELETE calls
- Schema parsing uses regex-based SQL extraction with parenthesized block matching
- Token refs, cross refs, and expired docs checks are stubs for Task 16
- All tool logic uses only Go standard library (no external dependencies)

## [2026-06-29] - 文档治理：TT-001 catch-up — Task 151-169 progress.txt 记录补全

### What was done:
- 补全 Task 151-169 的 progress.txt 记录缺口
- 确认以下任务已完成 (passes: true) 但此前未在 progress.txt 中单独记录：
  Task 151: 全局 err.Error() 脱敏
  Task 152: 信誉分 < 3 权限限制补全 + 信誉分规则实现
  Task 153: Auth 中间件 Redis fallback + Agent usage-guide HTTP 方法修复
  Task 154: 数据库 Schema 修复 — Comment 缺失列 + 索引补全 + 字段类型对齐
  Task 155: 中文全文检索修复 + 标签变更触发搜索索引更新
  Task 156: Redis ZSet 内存回收 + 硬编码 TTL 迁移到配置 + Goroutine 安全补全
  Task 157: Config override 环境变量一致性 + Docker 健康检查 + OptionalAuth 补全
  Task 158: 前端 Shadow 规范修复 + OriginalSidebar 硬编码搜索词迁移
  Task 159: 集成验证 — 安全 + 数据库 + 搜索修复全流程回归测试
  Task 160: 配置结构补全 — AgentConfig/PublishConfig struct + .env.example 补全
  Task 161: Redis key 规范化 + 推荐引擎硬编码参数配置化
  Task 162: Docker/Nginx 加固 + SSL 证书配置
  Task 163: pg_trgm GIN 索引 + 计算列排序优化
  Task 164: CSRF rand.Read 检查 + Agent ChatStream 孤立记录清理 + 维度不匹配日志
  Task 165: 前端 Rehab 子组件提取 + FollowerAnalytics 命名对齐 + console 清理
  Task 166: E2E 残留 API 修复 — reactions 端点 + my/contents 路由 + stats NULL 处理
  Task 167: 数据库迁移注释补充
  Task 168: 集成验证 — 第二轮审查修复全流程回归测试
  Task 169: 代码审查 — Spec Task 151-168 全面系统性审查

### Testing:
- 不适用（本记录为治理审计，不涉及功能改动）

### Notes:
- 上述任务在 task.json 中均已标记 passes: true，但 progress.txt 未记录执行详情
- 历史任务精确执行日期不可追溯，以治理审计日期 (2026-06-29) 标注
- 如需查看各任务完整 scope，请参考 task.json 中对应 ID 的 description 和 steps 字段

## [2026-06-29] - Task 9: Update CLAUDE.md and AGENTS.md with document authority table and rules

### What was done:
- Inserted document authority quick-reference table before "## Key Rules" in both CLAUDE.md and AGENTS.md
- Inserted file naming conventions section after project structure in both files
- Added Rule 3 at end of Step 1: document conflict resolution guideline
- Added Rule 1 at end of Step 3: doc-validator / manual confirmation rule for config/migrations/routes changes
- Added Rule 2 before Step 6: new .md file naming and placement rule

### Testing:
- Verified all 5 insertions in CLAUDE.md: authority table, naming conventions, 3 agent rules
- Verified all 5 insertions in AGENTS.md: same changes mirrored

### Notes:
- Document authority table references docs/superpowers/specs/2026-06-29-omnicraft-documentation-governance-design.md §4 as full authority
- File naming conventions require new .md files in docs/working/ with YYYY-MM-DD-<scope>-<type>.md format
- This is a pure documentation task (no build/test needed)

## [2026-06-29] - Task 12: Fix FS-001~003 + CF-001 — Feature spec gaps and config alignment

### What was done:
- FS-001 (CLAUDE.md): Added implementation status note to collections section — `> **实现状态**: 设计已确认（DEC-010），代码尚未实现。collections 和 collection_items 表 DDL 见 architecture.md §4.13。`  — architecture.md §4.13 already had this note, CLAUDE.md was missing it
- FS-002 (CLAUDE.md): Fixed search_suggestions description — was incorrectly referencing a `search_suggestions` table that does not exist. Replaced with accurate description based on architecture.md §15.5: suggestions come from tags/content_items prefix matching, trending from Redis hot_rank
- FS-003 (CLAUDE.md): Fixed judge error_rate_window value — was incorrectly stating `N > 10`, changed to `N >= 10 (从 config.yaml > judge.error_rate_window 读取)` to match config.yaml's `error_rate_window: 10`
- CF-001: Verified features registry is consistent — all three flags (`payment_enabled`, `creator_support_enabled`, `desktop_deploy_enabled`) exist in backend/config.yaml, architecture.md §7, Go config struct, and frontend code with matching defaults. No changes needed.

### Testing:
- No code changes — documentation alignment only
- Verified all three CLAUDE.md changes via Read

### Notes:
- config.yaml is authoritative for runtime defaults per document authority table
- architecture.md §4.13 already had correct implementation status note for collections — CLAUDE.md was out of sync
- No `search_suggestions` migration table exists; suggestions are computed from prefix matching on tags and content_items titles

## [2026-07-01] - Community messages-notifications: completed

### What was done:
- Added DM cold-start protection, stable message DTO contracts, and `/api/v1/messages` frontend usage.
- Added migration 057 for `notifications.channel = broadcast`.
- Added broadcast notification repository/service support with active-recipient filtering, transactional batch creation, and safe admin audit metadata.
- Added `POST /api/v1/admin/notifications/broadcast` and the admin broadcast page with Markdown editor, safe preview, confirmation modal, and localized toasts.
- Updated `/messages` notification/message UI with broadcast styling, safe Markdown rendering, valid-target navigation, stable unread counts, and mobile chat behavior.
- Added focused backend/frontend tests, mocked Playwright verification, screenshots, and generated `architecture.md` route documentation.

### Testing:
- Backend: `go test ./...`, `go vet ./...`, `go build ./...` all passed.
- Frontend: `npm run test`, `node --import tsx --test tests/messages-components.test.tsx`, `node --import tsx --test tests/admin-notifications-page.test.tsx`, `npm run lint`, and `npm run build` all passed.
- Doc sync: `cd tools/doc-validator && go run . --fix` passed and updated `architecture.md` with the admin broadcast route.
- Browser verification: used Playwright MCP against local dev server with mocked APIs; verified admin broadcast form, Markdown preview, ConfirmModal Esc/confirm flow, recipient-count toast, broadcast notification rendering, mobile chat, and `DM_REPLY_REQUIRED`; saved four screenshots under `screenshots/community-messages-notifications-*.png`.

### Notes:
- `npm run test:e2e -- messages-notifications.spec.ts` could not start because existing `frontend/playwright.config.ts` waits for backend `/health`, while the backend exposes `/healthz`; browser verification was completed with Playwright MCP instead.
- This is Mode C community plan work. No `task.json` or Beta roadmap checkbox was changed.

## [2026-07-01] - Community messages-notifications: review repair

### What was done:
- Fixed the final review finding where concurrent first DMs for a new sender/recipient pair could create duplicate conversations and bypass the cold-start reply guard.
- Added a Postgres concurrency regression test that aligns concurrent conversation creation attempts and requires exactly one successful first DM.
- Added transaction-scoped Postgres advisory locking before conversation lookup/creation, with an in-process pair lock fallback for non-Postgres tests/dev.
- Addressed review follow-up by holding the non-Postgres fallback mutex around the full transaction and replacing the optional second-arrival race probe with an advisory-lock wait test against `pg_locks`, filtered to the exact pair advisory key.

### Testing:
- Confirmed the new regression test failed before the fix: 5 successful concurrent sends instead of 1.
- `go test ./internal/repository -run "TestSendWithColdStartGuard" -count=1 -v -timeout=120s` passed.
- `go test ./internal/repository -count=1 -v -timeout=120s` passed.
- `go test ./internal/handler -run TestMessageColdStart -count=1 -v -timeout=120s` passed.
- `go test ./internal/handler -run TestAdminNotificationBroadcast -count=1 -v -timeout=120s` passed.
- `go test ./internal/service -run TestBroadcast -count=1 -v -timeout=120s` passed.
- Backend gates passed: `go test ./...`, `go vet ./...`, `go build ./...`.
- Frontend gates passed: `npm run test`, `node --import tsx --test tests/messages-components.test.tsx`, `node --import tsx --test tests/admin-notifications-page.test.tsx`, `npm run lint`, `npm run build`.
- Doc sync passed: `cd tools/doc-validator && go run . --fix` reported no changes needed.

### Notes:
- The repair keeps Mode C tracking only; no `task.json` or Beta roadmap checkbox was changed.

## [2026-07-02] - Community browse-history: completed

### What was done:
- Added `browse_history.retention_days` and `cleanup_time` config mapping/defaults and synced generated config docs in `architecture.md`.
- Enhanced browse history repository and handler contracts with config-driven retention, `items` + legacy `history` compatibility, `content` + legacy `content_item` aliases, content-type/date filters, selected-delete and clear-all semantics, and unavailable-content placeholders.
- Added daily browse-history cleanup scheduler using Asia/Shanghai wall-clock time, `time.AfterFunc` self-rescheduling, and graceful shutdown `Stop()`.
- Refactored `/history` UI with filter chips, date range fields, batch delete, clear-all confirmation, retained data on refresh errors, localized copy, and gray non-clickable unavailable records.
- Added focused backend and frontend tests for config, repository, handler, scheduler, and history page behavior.

### Testing:
- Focused: `go test ./config -run TestBrowseHistoryConfig -v`; `go test ./internal/repository -run TestBrowseHistory -v`; `go test ./internal/handler -run TestBrowseHistory -v`; `go test ./internal/pkg/scheduler -run TestBrowseHistoryCleanup -v`; `node --import tsx --test tests/history-page.test.tsx`.
- Full gates: `go test ./...`; `go vet ./...`; `go build ./...`; `npm run test`; `npm run lint`; `npm run build`.
- Doc sync: `cd tools/doc-validator && go run . --fix` passed and updated `architecture.md` config mapping.
- Browser verification: logged into local dev as `admintest@omnicraft.com`, verified `/history` rendered retained items plus unavailable placeholder, `content_type=video` filtering, same-day `start_date`/`end_date` query behavior, selected delete, clear-all empty state, and saved desktop/mobile screenshots under `screenshots/community-browse-history-*.png`.

### Notes:
- Mode C tracking only: no `task.json` or Beta roadmap checkbox was changed.
- Subagent review/implementation attempts were made, but the local subagent service repeatedly returned `503 Service Unavailable`; controller completed local review and verification gates instead.

## [2026-07-03] - Community collections: completed

### What was done:
- Added migration 058 plus collection models, repository, service, and `/api/v1/collections` handlers for zone-scoped collection folders, default original/fanwork collections, item notes, public/private visibility, owner-only mutations, and fixed `contains_item` list response shape.
- Preserved old `/favorites` rollback-window behavior with transactional double-write/delete against default collections, default collection creation on registration, and recommendation profile compatibility using a de-duplicated favorites + collection_items union.
- Added `CollectionPicker`, `CollectionCard`, `CollectionInfoCard`, `ContentTypeFilter`, public `/collections/[id]`, public `/user/[userId]/collections`, and updated `/studio/favorites` and content detail entry points to use the new collection flow with localized copy.
- Synced generated architecture route docs and fixed Playwright readiness to use backend `/healthz`.
- Completed local two-stage review after subagent review attempts failed with `503 Service Unavailable`; no blocking spec or code-quality findings remained.

### Testing:
- Backend focused: `go test ./internal/model -run TestCollectionMigration -v`; `go test ./internal/repository -run TestCollectionRepo -v`; `go test ./internal/service -run 'Test(Collection|FavoriteCompatibility|Recommendation|AuthServiceCreateUserFromPending)' -v`; `go test ./internal/handler -run TestCollection -v`.
- Backend full gates: `go test ./...`; `go vet ./...`; `go build ./...`.
- Frontend focused: `node --import tsx --test tests/collection-picker.test.tsx`; `node --import tsx --test tests/collection-detail.test.tsx`; `node --import tsx --test tests/user-collections-page.test.tsx`.
- Frontend full gates: `npm run test`; `npm run lint`; `npm run build`; `npx playwright test e2e/collections.spec.ts` (12 passed across desktop/mobile).
- Doc sync: `cd tools/doc-validator && go run . --fix` passed with no remaining route/schema/config changes.
- Browser screenshots saved locally under `screenshots/community-collections-desktop.png`, `screenshots/community-collections-owner-mobile.png`, `screenshots/community-collections-picker-desktop.png`, `screenshots/community-collections-picker-mobile.png`, `screenshots/community-collections-user-list-desktop.png`, and `screenshots/community-collections-user-list-owner-mobile.png`.

### Notes:
- Mode C tracking only: no `task.json` or Beta roadmap checkbox was changed.
- Subagent spec and code-quality review attempts both returned local `503 Service Unavailable`, so controller completed local review and fresh verification gates instead.
- `frontend/test-results/.last-run.json` is a generated Playwright artifact and must not be staged.

## [2026-07-06] - Community collections: merged into main and docs synced

### What was done:
- Fast-forward merged `codex/community/collections` into `main`, bringing in community plan 3 commit `0c74996`.
- Updated `docs/working/2026-06-30-community-features-development-guide.md` to mark plan 3 Collections as complete and merged, and to set plan 4 Content Series as the next step.

### Testing:
- Verified `main` branch merge state with `git merge --ff-only codex/community/collections`.
- Reconfirmed fresh validation evidence from the plan 3 worktree: focused backend tests, `go build ./...`, `npm run build`, and `npx playwright test e2e/collections.spec.ts` all passed before merge.
- Verified post-edit repository diff only contains the intended doc-state updates.

### Notes:
- This follow-up sync is status maintenance on `main`; no `task.json` or Beta roadmap checkbox was changed.

## [2026-07-17] - Hardening Task 1: Make Frontend Unit Tests Discoverable And Complete

### What was done:
- Added a cross-platform Node test runner that recursively discovers all supported `*.test.*` unit files, excludes generated/E2E directories, normalizes and sorts paths, and separates native from TS/TSX execution.
- Added runner contract tests for discovery, exclusions, grouping, failure propagation, serial TS/TSX execution, and unknown-mode rejection.
- Replaced the fixed frontend test list with `test:unit`, `test:contracts`, `verify`, and `verify:release` scripts while preserving existing package scripts.
- Serialized TS/TSX test files after the newly complete concurrent gate exposed a resource-contention flake in JSDOM-heavy tests.

### Testing:
- TDD RED: `node --test scripts/run-tests.test.mjs` failed with `ERR_MODULE_NOT_FOUND` before the runner existed; the serial-execution contract later failed before its implementation.
- Focused GREEN: `node --test scripts/run-tests.test.mjs` passed 6 tests.
- Frontend gate: `npm.cmd run verify` passed TypeScript lint, 82 discovered unit assertions, and the Next.js production build.
- Backend regression: `go test ./...` passed after PostgreSQL and Redis were healthy; `go vet ./...` and `go build ./...` passed with VCS stamping disabled for the external worktree environment.
- Review: specification compliance reviewer returned `APPROVED`; local code-quality review, syntax checks, package-script completeness check, and `git diff --check` found no blocking issues.

### Notes:
- The complete default unit gate now surfaces pre-existing React `act(...)` and intentional error-path console output from previously omitted tests; these tests pass and cleanup belongs to a separate focused task.
- Mode D tracking only: no `task.json`, Beta roadmap, or community-plan completion state was changed.

## [2026-07-17] - Hardening Task 2: Split Doc Validator Profiles Without Hiding Current Debt

### What was done:
- Added explicit `release`, `archive`, and `all` validation profiles with `release` as the default and unknown-profile rejection.
- Kept config, schema, route, token, active cross-reference, and working-document expiry checks in the release gate while excluding only `docs/archive/**` cross-reference debt.
- Added archive-only cross-reference checking and an aggregate all profile, with the active profile printed in command output.
- Replaced the permissive bare-file-URI regex with strict absolute Windows/POSIX file URI parsing that ignores bare prose and preserves balanced parentheses in paths.
- Added profile, scope, file URI, Markdown-anchor, and archive isolation regression tests; documented profile usage and release semantics.

### Testing:
- Reproduced the baseline: 122 issues, consisting of 121 archive findings and one active false bare `file:///` match.
- TDD RED: cross-reference tests failed because `CrossRefOptions` did not exist; profile tests failed because `resolveProfile` did not exist; balanced-parentheses URI test failed before the scanner fix.
- `go test ./... -v`, `go vet ./...`, and `go build ./...` passed in `tools/doc-validator`.
- Default and explicit `release` profiles passed with zero issues; unknown profile rejection passed.
- `archive` and `all` remained non-zero with 115 visible historical issues. Six former findings were invalid partial-path matches around `(protected)`, not hidden debt.
- Remote specification reviewer timed out; local specification and code-quality reviews found no blocking issues.

### Notes:
- `docs/working/2026-06-30-community-features-development-guide.md` already had the exact required metadata, so no redundant rewrite was made.
- Mode D tracking only: no `task.json`, Beta roadmap, or community-plan completion state was changed.

## [2026-07-17] - Hardening Task 3: Establish A Dedicated HTTP Composition Root

### What was done:
- Moved the sole `RegisterRoutes` owner from `backend/internal/handler/routes.go` to `backend/internal/router/routes.go`, updated `main.go`, and preserved trusted-proxy, CORS, global middleware, route, authentication, and interaction-guard ordering.
- Added container ownership for `StatsService` and reused the existing `IPStatsService`, `SearchService`, `PRService`, and `SocialService`; added thin service-backed PR/social handler constructors so the router creates no repositories or domain services.
- Moved route security/deploy tests into the router package and added sole-owner, composition, representative route, credential/agent rate-limit, and disabled desktop/payment 503 contract coverage.
- Updated doc-validator route extraction to the new owner, added legacy-marker replacement coverage, and regenerated the `architecture.md` route source marker/table.

### Testing:
- TDD RED: the main ownership test failed on the missing router import; router tests failed on missing `RegisterRoutes`; container and handler-constructor tests failed on their missing seams; route-sync failed while looking for the deleted handler owner.
- Focused GREEN: `go test ./internal/container ./internal/handler ./internal/router ./cmd/server -v` passed, including the existing credential/agent middleware behavior and exact disabled-route 503 contracts.
- Backend full gates: `go test ./...`, `go vet ./...`, and `go build ./...` passed.
- Doc gates: `go test ./...`, `go run . --fix`, and `go run . --check --profile release` passed; release reported zero issues.
- Project regression: `npm.cmd run verify` passed frontend TypeScript lint, all discovered unit tests, and the Next.js production build.
- Mechanical route audit compared all 193 registration lines in order; after the required PR-handler container reuse equivalence, URL and middleware arguments were identical. Specification and code-quality reviewers both returned `APPROVED`.

### Notes:
- Route composition remains behavior-preserving; no route method/path was added or removed, and the old handler route owner no longer exists.
- Mode D tracking only: no `task.json`, Beta roadmap, or community-plan completion state was changed.

## [2026-07-17] - Hardening Task 4: Centralize Interaction Policy Archetypes

### What was done:
- Audited all current interaction guards and confirmed the only policy-value difference is publishing's additional no-publish-freeze requirement; standard interactions require verified email and minimum reputation.
- Added fresh-value `standardVerifiedInteractionPolicy` and `publishingInteractionPolicy` archetypes, with publishing composed from standard plus the freeze check.
- Kept all 12 route-specific guard variables and preserved operation-specific upload, comment-edit, Agent rate limits, middleware ordering, and handler/service business rules.
- Added tests for exact archetype values, every guard-to-policy mapping, and representative real route attachments.

### Testing:
- TDD RED: `go test ./internal/router -run "TestInteractionPolicy|TestRoutePolicy" -v` failed because both named archetype functions were missing.
- Focused GREEN and router regression: the same focused command and `go test ./internal/router -v` passed.
- Mechanical audit compared all 193 route registration lines before and after; order and arguments were identical.
- Full gates passed: backend `go test ./...`, `go vet ./...`, `go build ./...`; frontend `npm.cmd run verify`; doc-validator `go test ./...`, `go run . --fix`, and strict release profile.
- Specification and code-quality reviewers both returned `APPROVED`.

### Notes:
- This is behavior-preserving policy deduplication, not a replacement for route-specific or handler/service authorization rules.
- Mode D tracking only: no `task.json`, Beta roadmap, or community-plan completion state was changed.

## [2026-07-17] - Hardening Task 5: Add Project-Level Verification Commands

### What was done:
- Added a PowerShell project verifier with explicit default, full, and release tiers plus an orthogonal Tauri option.
- Default runs backend `test/vet/build`, frontend `unit/lint/build`, doc-validator tests, and strict release profile as separate fail-fast child commands.
- Full adds mocked Playwright contracts; release adds the complete Playwright suite; Tauri adds its frontend build and Rust tests. Full and release are mutually exclusive.
- Added isolated fake-command contract tests for exact command order, tier additions, first-failure stop, caller-location restoration, standalone non-zero exit propagation, and default repository resolution under Windows PowerShell `-File`.
- Documented tier prerequisites, archive hygiene, and evidence limits in README; synchronized the stable command entry in AGENTS.md and CLAUDE.md.

### Testing:
- TDD RED: contract tests initially failed because `verify-project.ps1` did not exist.
- Contract GREEN: `powershell -ExecutionPolicy Bypass -File scripts/verify-project.tests.ps1` passed all default/full/release/Tauri and failure-path checks.
- Real default gate: `powershell -ExecutionPolicy Bypass -File scripts/verify-project.ps1` passed all backend, frontend, and release-document subcommands.
- Systematic regression: the first real CLI run reproduced an empty `$PSScriptRoot` during parameter binding; a standalone `-File` regression test failed, then passed after moving default `RepoRoot` resolution into the script body.
- Full/release command plans were verified deterministically with fake commands; real browser suites remain subject to their documented service, browser, data, and external-input prerequisites. Tauri was not executed because Task 5 does not modify desktop files.
- Specification and code-quality reviewers both returned `APPROVED`.

### Notes:
- The aggregate verifier is intended as the stable CI entry for later Ops work; it does not replace screenshots, real-provider smoke, package signing, or release evidence.
- Mode D tracking only: no `task.json`, Beta roadmap, or community-plan completion state was changed.

## [2026-07-18] - Ops-00: Production Readiness 规格与执行计划

### What was done:
- Added the independent Production Readiness design covering CI, forward-only migrations, backup/recovery, observability, alerting, security scans, SBOM/provenance, load testing, production configuration, deployment rollback, and desktop signing.
- Added the Ops-00～Ops-09 execution plan with exact files, serial branch/dependency rules, TDD/verification commands, evidence summaries, acceptance criteria, and release blockers.
- Registered independent Mode E in `AGENTS.md` and `CLAUDE.md`; explicitly excluded Hardening Task 6 and protected `task.json`, Beta/community state, and Web Agent state.
- Coordinated the UI Polish plan: U-01/U-05 may run after UI-plan registration; U-11 depends on this coordination input; U-12 waits for Ops-01 because it shares verifier/package paths.

### Testing:
- `go test ./...` in `tools/doc-validator`: passed.
- `go run . --check --profile release`: passed with all checks passed.
- `git diff --check`: passed.
- Three rounds of specification/plan review were completed; third-round findings were incorporated before user approval on 2026-07-18.

### Notes:
- This is a planning-only Ops-00 commit; no Ops-01～Ops-09 implementation has started.
- No production database snapshot or real provider credentials were required for Ops-00.
- `task.json`, Beta roadmap, community completion state, and Web Agent completion state were not modified.

## [2026-07-18] - Local test validation repair: documentation sync

### What was done:
- Synchronized `docs/working/2026-07-18-local-test-validation-repair-plan.md` with the implementation already merged on `main`.
- Marked the preflight/readiness/layer-runner implementation and contract verification complete, along with the originating session’s Docker recovery and isolated-layer execution evidence.
- Kept the full repository regression checkbox open because the fresh `scripts/verify-project.ps1` run exceeded 240 seconds in `go test ./...` and did not produce a PASS result.

### Testing:
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test/preflight.tests.ps1`: passed.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test/readiness.tests.ps1`: passed.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test/run-test-layer.tests.ps1`: passed.
- `node scripts/playwright-config.test.mjs`: 3 tests passed.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-project.ps1`: timed out after 240 seconds in the `go test ./...` child process; the process tree started by this check was stopped and no PASS was recorded.

### Notes:
- Implementation commits already present on `main`: `9006a18`, `fe34360`, and `7d297f8`.
- This maintenance entry changes documentation only; it does not modify `task.json`, Beta/community/Web Agent completion state, or the Production Readiness plan.
## [2026-07-18] - Community Browse History Task 7 Follow-Up: Harden Clear And Cleanup

### What was done:
- Replaced implicit destructive DELETE behavior with explicit `{ "ids": [...] }` and `{ "clear_all": true }` modes; missing/empty bodies and empty IDs now fail safely, while conflicting modes return a dedicated error.
- Added a transaction-scoped PostgreSQL advisory leader lock around expired-history deletion so concurrent API replicas do not run the same cleanup transaction.
- Updated the History page to preserve confirmation before clear-all and send the explicit clear tag; added desktop/mobile Playwright contracts and deterministic screenshots.

### Testing:
- TDD RED confirmed missing body, empty object, empty IDs, unknown-length stripped body, and conflicting modes were previously accepted or mapped incorrectly; focused handler tests pass after the fix.
- PostgreSQL concurrency tests prove one of two cleanup instances executes while the other skips, and prove lock release after both successful commit and forced rollback.
- Backend `go test ./...`, `go vet ./...`, and `go build -buildvcs=false ./...` passed.
- Frontend `npm.cmd test` (75 TS/TSX assertions plus native runner tests), `npm.cmd run lint`, and `npm.cmd run build` passed.
- Playwright desktop/mobile verification passed 4/4 for selected deletion, cancelled clear-all, confirmed clear-all, and stripped-body rejection; screenshots were visually inspected.

### Notes:
- The advisory lock ID is stable and held only inside the delete transaction; it is never retained between scheduler runs.
- Existing frontend next-intl environment and React `act(...)` test warnings remain baseline debt for the UI hardening plan; no new warning class was introduced.
- Only Browse History Task 7 follow-up state was updated; `task.json`, Beta roadmap, and unrelated community plan state were not changed.

## [2026-07-18] - Community Collections Task 11 Follow-Up: Reconcile Legacy Cutover

### What was done:
- Added owner-only lazy repair so authenticated implicit collection lists idempotently restore both default zones, while every explicit `owner_id` read remains mutation-free.
- Added safe structured repair logs shared by own-list and add-to-default paths, with `user_id`, `zone`, and bounded error codes only.
- Added the `collection-reconcile` maintenance command with per-user/zone drift reports, read-only exit semantics, only-add idempotent repair, duplicate preservation, explicit `DB_DSN` precedence, and a mandatory stopped-writer maintenance-window confirmation for `--apply`.
- Replaced the user-profile legacy favorites API tab with a semantic link to `/user/[userId]/collections`, added localized labels, documented the five legacy cutover exit criteria, and saved browser evidence for the source and destination pages.

### Testing:
- TDD RED/GREEN covered own-list repair, public-read immutability, add-to-default safe logging, four reconciliation drift classes, read-only non-mutation, only-add/idempotent apply, duplicate preservation, invalid arguments, explicit DSN precedence, and the maintenance-window write gate.
- Real PostgreSQL drills confirmed read-only drift totals, unconfirmed apply made zero changes, confirmed apply repaired 1 default collection/2 collection items/1 favorite, and the second confirmed apply reported zero repairs; both temporary databases were removed.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-project.ps1` passed backend test/vet/build, 76 frontend unit tests, TypeScript lint, Next.js production build, doc-validator tests, and the release documentation profile.
- In-app Playwright verified the localized Collection link, pointer navigation to `/user/19/collections`, successful public empty state after migration 058, and zero browser console errors; specification and code-quality re-reviews both returned PASS.

### Notes:
- `--apply` is intentionally refused unless all application writers are stopped and `--maintenance-window-confirmed` is supplied; this prevents reconciliation from racing a user's favorite/unfavorite transaction.
- Remaining `/studio/favorites` strings are the collection manager's historical route name, not calls to the legacy favorites API.
- Only Collections Task 11 follow-up state was updated; `task.json`, Beta roadmap, and unrelated community plans were not changed.

## [2026-07-18] - Community Content Series Tasks 1-10

### What was done:
- Added migration 059 and content-series models, repository/service rules, owner/contributor validation, visibility filtering, concurrent append locking, and transactional reorder.
- Added series API handlers/routes, full content-detail memberships, SeriesNav, public series detail, and Studio series management with create/edit/delete/add/remove/reorder flows.
- Added mocked and real cross-stack Playwright coverage, including desktop/mobile screenshots in the repository root.

### Testing:
- `mocked browser contract PASS` — `npm.cmd run test:contracts` (8/8).
- GoUnit runner PASS.
- PostgreSQL integration runner PASS after Docker daemon access was restored; an initial unprivileged run was truthfully BLOCKED by Docker access and was not counted as PASS.
- CrossStack runner PASS (4/4) through `run-test-layer.ps1`, including seeded real public series detail and SeriesNav requests; the initial final-commit run without temporary `node_modules` was truthfully FAIL and was rerun after dependency installation.
- Frontend unit PASS (96), TypeScript lint PASS, Next.js production build PASS.
- `tools/doc-validator`: `go test ./...` PASS; `go run . --check --profile release` PASS.

### Notes:
- The mocked suite previously exposed an environment/permission failure and was rerun to a complete 8/8 PASS after the runner could write its generated result files. Series item mutations now refresh `content_series.updated_at`, management preloads soft-deleted content summaries, and CrossStack exports its temporary compose root for deterministic fixtures.
- No `task.json`, Beta roadmap, or UI polish plan state was changed.

## [2026-07-23] - Content Series strict real-browser follow-up BLOCKED

### What was done:
- Added an uncommitted CrossStack fixture and browser flow covering multiple memberships, first/last navigation, and authenticated Studio create/add/reorder/remove/delete.
- Added CSRF-aware login setup and deterministic fixture password/content rows.

### Testing:
- `mocked browser contract PASS` — 8/8.
- CrossStack with the fixture previously reached 3/5 PASS; the remaining failures were corrected in the test (role-tab ambiguity and CSRF login), but the verification could not be rerun after correction because Docker Desktop stopped starting.
- Latest required CrossStack invocation is `BLOCKED`: `docker compose up` reports `Docker Desktop is unable to start`; daemon polling remained unavailable for 60 seconds.

### Notes:
- The uncommitted fixture/test changes are intentionally preserved for the next Agent; do not discard them.
- Do not amend or merge the Content Series commit until CrossStack reruns with the authenticated Studio flow and all 5 tests pass.
- To resume: repair/restart Docker Desktop, run `docker compose up -d --force-recreate postgres redis`, then rerun the mandated CrossStack runner with the content-series worktree and ComposeRoot.

## [2026-07-23] - Content Series strict real-browser follow-up UNBLOCKED

### What was done:
- Docker Desktop was restarted and the preserved CrossStack fixture was rerun through the mandated runner.
- The real browser suite now covers public 200 series detail, ordered chapters, multiple-series switching, first/last boundaries, and authenticated Studio create/add/reorder/remove/delete.

### Testing:
- `mocked browser contract PASS` — 8/8 (mock-only evidence; not used as a substitute for CrossStack).
- GoUnit — PASS.
- PostgresIntegration — PASS.
- CrossStack — PASS, 5/5.
- Frontend unit tests — 96 passed.
- Frontend lint — PASS (`npm.cmd run lint`; standard cache-writing invocation passed under elevated permissions).
- Frontend production build — PASS (`npm.cmd run build`, elevated due temporary worktree ACL).
- Go vet/build — PASS via `go vet ./...` and `go build ./...` with `GOFLAGS=-buildvcs=false` because the temporary worktree cannot provide VCS stamping metadata.
- `tools/doc-validator`: tests PASS; `go run . --check --profile release` PASS.

### Notes:
- The prior Docker BLOCKED record remains above as historical evidence; this entry records its resolution on 2026-07-23.
- The Content Series plan browser-verification checkbox and self-check are now complete.
- No `task.json`, Beta roadmap, or UI polish plan state was changed.

## [2026-07-23] - 文档瘦身阶段 0+1：清场与文档体系重构

### What was done:
- 阶段 0：验证并提交会话遗产批次（5 个主题 commit：SSR API URL 分离、公开页 SEO、hot rank 修复、迁移 060 搜索回退、本地 seed 工具）；修剪 9 个 codex worktree（4 个长路径残留已强删）；删除 10 条已全部并入 main 的 codex 分支。
- 死账处置：progress.txt 轮换为 30 天窗口（259KB→39KB，127 条旧记录归档至 docs/archive/progress/2026-04-to-2026-06.md）；task.json 移至 docs/archive/task-ledger-mvp.json，历史任务模式废止。
- 旧计划归档：铁证完成 6 份 + 验证完成 2 份（web-beta-review-repair、documentation-governance）移入 docs/archive/plans/ 并加归档说明。
- AGENTS.md 重写：46KB→14KB；五模式废止，改为活计划注册表 + 双车道（light/heavy）；业务规则抽取至 docs/reference/business-rules.md；CLAUDE.md 变为 @AGENTS.md 指针。
- architecture.md 拆分：128KB→37.7KB 概述；§4/5/7→docs/reference/{schema,api,config}.md；§10-13→docs/specs/；§14-15 实现笔记→docs/reference/implementation-notes.md（因含活实现引用，改归档为参考）。
- ui-spec.md 小手术：删除 3 个死节（/publish、/dashboard 两个 301 页 + Deprecated 组件），顶部新增章节索引（46 页 + 63 组件）。
- doc-validator 同步：schema/config 检查路径指向 docs/reference/，修复迁移后断链。

### Testing:
- 提交前验证：前端 100/100 单测 PASS、lint(tsc) PASS、go build/vet PASS。
- doc-validator --check（含 release profile）：All checks passed。
- go test ./tools/doc-validator/... PASS。

### Notes:
- 常驻上下文（AGENTS.md + architecture.md）从 ~420KB 降至 51.7KB。
- 下一步按注册表优先级：近完成项清零（消息通知 11 / 卓越加固 9 / 发布验证 6），随后 UI 工程（U-01~U-05 地基 + 首页/详情页视觉样板）。

## [2026-07-23] - 阶段2 近完成清零：4 子系统 + 卓越加固归档，R-02 定阻塞

### What was done:
- Beta 子系统×4（foundation F-01~F-06 / admin-operations A-01~A-05 / agent-entrypoints G-01~G-05 / verification-feedback V-01~V-06）经 roadmap 状态表与 progress 归档双重验证为 100% 完成（注册表余项为过期计数），加归档说明后移入 docs/archive/plans/，注册表行移除。
- 卓越加固 Review Checklist 8 项逐项核验全部满足：run-tests.mjs 无 shell glob 全扩展名发现；doc-validator release profile 仅豁免 archive；bare-file-URI 中文散文误报已有回归测试；router 组合根由测试强制不构造 repo/service；policies_test 覆盖 archetype 与路由挂载；verify-project.ps1 全外部命令 fail-fast（有 tests.ps1）；计划无 Task 6 标题；五个执行提交未翻转 roadmap/community/task.json 完成状态（be6db7e 仅新增未勾选后续项）。勾选后归档，注册表行移除。
- 发布验证 R-02 确认阻塞：Step 1 前置要求 D-01~D-05 全部勾选 + 真实 Ed25519 私钥 + HTTPS 基址，而 D-02~D-05 属优先级 6 heavy 车道未开始。注册表备注已改为显式依赖阻塞，不产出模拟证据。

### Testing:
- 核验方法：代码/脚本/测试文件逐一比对（见上），git log --follow 审查五个执行提交触及面。
- doc-validator --check --profile release 在阶段1已全绿，profile 语义未变。

### Notes:
- 剩余优先级 1 工作：消息通知 Task 9（广播幂等，10 项，实现型）。
