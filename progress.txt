## [2026-08-12] - Task 4 [heavy]: Handler Routes And User Setting Contract (#97)

### What was done:
- **Step 1/2（TDD red）**：先写 `backend/internal/handler/collab_invite_test.go`（15 个顶层测试：send happy path 201 + invite/conversation/message 计数、不存在内容 404 CONTENT_UNAVAILABLE 且 0 记录、author/not-owner 校验、INVITE_NOT_ACCEPTING、重复邀请 409 ALREADY_EXISTS + decline 后 409 DUPLICATE_USER、invalid body 400 VALIDATION_ERROR + 非数字 id 400 INVALID_ID、send 路由守卫 401/USER_BANNED/EMAIL_NOT_VERIFIED/INSUFFICIENT_REPUTATION、accept happy path + contributor 行、非 invitee 403、非 pending 409、expired 400、not found 404、decline happy path + 无 contributor 行、decline 守卫、PATCH 设置契约 false→true + 他人 403 + NO_FIELDS + /auth/me 返回字段）+ 501 stub handler（可编译），`go test ./internal/handler -run TestCollabInvite -v` 20 个子测试红（守卫子测试因中间件独立于 handler 而先绿，属预期）。
- **Step 3（handler）**：新建 `backend/internal/handler/collab_invite.go`——`CollabInviteHandler{svc}` + `NewCollabInviteHandler(svc)`；`SendInvite`（GetUserID、`{"invitee_id":42}` binding required、`:id` 为 content_item_id、201 `gin.H{"invite": invite}`）、`AcceptInvite`/`DeclineInvite`（`:id` 为 invite id、200 `gin.H{"invite": invite}`）；`mapCollabInviteError` 按 `errors.Is` 映射 18 个 sentinel → 规格表 HTTP/错误码（含用户批准的 INVITE_NOT_INVITEE 403、INVITE_NOT_PENDING 409）。
- **Step 4（路由 + 容器）**：`backend/internal/router/routes.go`——新增 `collabInvitesGuard := middleware.InteractionRequired(cfg, db, rdb, standardVerifiedInteractionPolicy())`（无匿名 policy 字面量）；`contents.POST("/:id/collab-invites", authReq, collabInvitesGuard, collabInviteHandler.SendInvite)`、`v1.POST("/collab-invites/:id/accept|decline", authReq, ...)`；无 repository.New/service.New。`backend/internal/container/container.go`——`CollabInviteService` 字段 + `service.NewCollabInviteService(c.ContentRepo, repository.NewCollabInviteRepository(db), c.MessageRepo, c.UserRepo, rdb, cfg)` 构造。
- **用户设置契约**：`backend/internal/handler/user.go` UpdateUser 请求结构新增 `AcceptCollabInvites *bool json:"accept_collab_invites"` + updates 白名单映射；`sanitizeUser` 输出新增 `accept_collab_invites`。`auth.go` 无需改动（Me 直接序列化 model.User，json tag 已含该字段，仅测试契约）。
- 路由表同步：`cd tools/doc-validator && go run . --fix` 把 3 条新路由写入 `architecture.md` §3.2 路由清单（二次运行 no changes needed，幂等）。

### Testing:
- 红→绿：red 20 子测试失败（501/NO_FIELDS 等）→ 实现后 `go test ./internal/handler -run TestCollabInvite -v` 全绿（修 1 个测试自身 bug：INVITE_AUTHOR_NOT_ALLOWED 需邀请者是已确认 contributor 才可达——service 链先判 NOT_CONTENT_OWNER）。
- 全量：`go build ./...` / `go vet ./...` / `go test ./...`（24 包）全绿；routes_test.go/policies_test.go/container_test.go 保持绿；改动文件 gofmt 干净（ip.go/version.go 为既有 gofmt 违规，未触碰）。

### Notes:
- 测试基建：ephemeral postgres（testutil.OpenEphemeralPostgres）+ 手写 base schema（users 含 role/email_verified_at，ResolveRuntimeUserStatus 需要）+ 065 迁移 + miniredis；handler 包无法注入 svc.now，断言避开 expires_at/Redis 日期；守卫用内联 `middleware.InteractionPolicy{RequireVerifiedEmail: true, RequireReputation: true}`（不能 import router，循环依赖）。
- 提交：`feat(collab): invite routes, handler and accept_collab_invites setting contract (#97)`。

## [2026-08-12] - Task 3 [heavy]: Implement Accept, Decline, And Expiry (#97)

### What was done:
- **Step 1/2（accept 事务）**：`CollabInviteService.AcceptInvite(ctx, inviteID, userID)`（`collab_invite_service.go`）——单事务内：`FindByIDForUpdate` 锁 invite 行 → 不存在 `ErrInviteNotFound` → 非 invitee `ErrInviteNotInvitee` → 非 pending `ErrInviteNotPending` → `ExpiresAt <= now` `ErrInviteExpired` → 锁 content 父行（`FindByIDForUpdate`，nil → `ErrContentUnavailable`）→ 非贡献者时锁下 `CountContributors >= max_contributors_per_item` → `ErrContributorLimitReached`（事务回滚 = 邀请保持 pending）→ `InsertContributorIfAbsent`（`pr_count=0`、`first_at=注入 now`，`ON CONFLICT DO NOTHING` 幂等，不触碰既有行的 pr_count/first_at）→ `UpdateStatus(accepted, responded_at)` → 返回变更后的 invite DTO。锁序 invite→content（SendInvite 只锁 content 行 + 插 invite 行，无环）。
- **Step 3（decline 事务）**：`DeclineInvite(ctx, inviteID, userID)` 同骨架（FOR UPDATE → notfound → invitee → pending → expiry → `UpdateStatus(declined, responded_at)`），不锁 content 行（无容量检查）。
- **Step 4/5（expiry 调度器）**：`backend/internal/pkg/scheduler/collab_invite_expiry.go` 克隆 BrowseHistoryCleanup 结构（db/cfg/repo/注入 now/mu/timer/stopped；`Start()`、幂等 `Stop()`、`time.AfterFunc` 自重排 24h 固定间隔、Asia/Shanghai location + FixedZone 兜底、skipped-run slog.Info「leader lock not acquired」）；repo 侧 `CollabInviteRepository.ExpirePendingIfLeader(expireDays, now)`：事务内 `pg_try_advisory_xact_lock(0x4f4d4e494e564954 /* "OMNINVIT" */)`，未获锁提交即跳过，`UPDATE status='expired' WHERE status='pending' AND created_at < now.AddDate(0,0,-expireDays)`，非 postgres 方言跳过锁。
- **Step 6（接线）**：`backend/cmd/server/main.go` 具名实例 `collabInviteExpiry := scheduler.NewCollabInviteExpiry(db, &cfg.Collaboration)` + `Start()`（browseHistoryCleanup 之后），shutdown `stopWorkers()` 后 `collabInviteExpiry.Stop()`（rdb.Close 之前），非匿名。
- **错误码决策（用户批准）**：设计规格错误码表未定义的两个码按用户指示新增——`ErrInviteNotInvitee = errors.New("INVITE_NOT_INVITEE")`（403，非被邀请人调用 accept/decline）、`ErrInviteNotPending = errors.New("INVITE_NOT_PENDING")`（409，非 pending 重复操作）；既有 `INVITE_EXPIRED`/`INVITE_NOT_FOUND` 按规格表实现。

### Testing:
- TDD 红→绿：stub 阶段 `TestCollabInviteAccept|Decline` 13 个测试编译/行为红（undefined AcceptInvite/DeclineInvite）→ 实现后 13/13 PASS（`go test ./internal/service -run "TestCollabInviteAccept|TestCollabInviteDecline" -v`，含非 invitee、expired 边界 `expires_at == fixed now`、已响应拒绝、幂等贡献者 pr_count 不增 first_at 不变、容量满留 pending、并发容量 1 成功 1 拒绝、NotFound）。
- 调度器：`go test ./internal/pkg/scheduler -run TestCollabInviteExpiry -v` 4/4 PASS（配置天数边界 -3d、过期/接受/拒绝分支、Stop 取消 timer 回调、双实例同一 DB 经 GORM Update callback 阻塞断言第二实例 `AcquiredLeader=false Expired=0`、释放后首实例完成、第三实例重获锁）。
- 全量：`go build ./...` / `go vet ./...` / `go test ./...`（24 包）全绿；改动文件 gofmt 干净。

### Notes:
- 未碰 config/config.yaml/routes/migrations（main.go 非三者）→ 无需 doc-validator；未碰前端与主 worktree。
- 遗留：INVITE_NOT_INVITEE/INVITE_NOT_PENDING 为设计规格错误码表外新增，Task 4 handler 映射（403/409）与设计规格表同步待记录；Task 5 卡片渲染消费 ListConversationSummaries 时需补 msg_type/metadata（Task 2 遗留）。
- 提交：`feat(collab): accept, decline and expiry for collaboration invites (#97)`。

## [2026-08-12] - Task 2 [heavy]: Invite Service Anti-Abuse Chain (#97)

### What was done:
- **Step 1/2（TDD red）**：先写 `backend/internal/service/collab_invite_service_test.go`（23 个顶层测试全矩阵）+ service stub（sentinels + 构造器 + `SendInvite` 返回 nil），`go test ./internal/service -run TestCollabInviteSend -v` 23 个全红（行为红，先修 1 个 miniredis Get 双返回值编译错）。
- **Step 3（Redis 反滥用）**：`collab_invite_count:{inviter_id}:{YYYY-MM-DD}` 与 `collab_invite_user:{inviter_id}:{invitee_id}:{YYYY-MM-DD}`，TTL 86400s，日期为注入时钟的 Asia/Shanghai 日历日（午夜边界测试 23:59+08 → 00:00+08）；预留用单 Lua 脚本（`inviteReserveScript`：limit 检查/重复 invitee 检查都在写之前，拒绝不产生部分预留；token 存入 per-invitee key）；DB 失败后补偿用有界 Lua（`inviteCompensateScript`：计数永不为负、归零即删、per-invitee key 仅当 token 匹配才删）；Redis 不可用 fail-closed（`ErrInviteRateLimitUnavailable`，先于任何 DB 写）。
- **Step 4（typed send）**：`MessageRepository.SendTyped(senderID, convID, body, msgType, metadata)` + `Send` 改为 `msgType="text"`/空 metadata 的包装；新增 tx 内变体 `SendTypedTx` 与 `FindOrCreateConversationTx`（复用 conversationPairAdvisoryKey 事务级 pair 锁，不开新事务）；`sendInTx` 支持 msg_type/metadata，unread_count 逻辑不变。
- **Step 5（service 事务）**：`SendInvite` 链 = self/author/contributor/unavailable 内容与 invitee 检查 → 信誉门（复用 `minScoreForInteraction`）→ 双向拉黑（`IsBlockedByEither`）→ `accept_collab_invites` → active duplicate（pending/accepted）→ Redis 预留 → 单事务（content row `FOR UPDATE` 权威复核 + 容量 = 已确认 contributor + 非 contributor 的 active pending invitee 去重计数，超 `max_contributors_per_item` 拒）→ 建 invite（expires_at = now + invite_expire_days）→ find/create 1:1 会话 → `collab_invite` typed message（metadata 仅 5 键：invite_id/content_id/content_title/inviter_id/inviter_username，body「联合创作邀请」）→ 回填 message_id；23505 部分唯一索引冲突映射 `ErrInviteAlreadyExists`；DB 失败 best-effort 补偿 + 记日志。
- 新文件：`backend/internal/repository/collab_invite_repo.go`（CreateInvite/UpdateMessageID/IsBlockedByEither/HasActiveInvite + Transaction/DB）；`backend/internal/service/collab_invite_service.go`；`backend/internal/service/collab_invite_service_test.go`。修改：`backend/internal/repository/message_repo.go`、`backend/internal/repository/content_repo.go`（FindByIDForUpdate/IsContributor/CountContributors/CountPendingInviteesNotContributors）。

### Testing:
- 红→绿：red 23/23 失败 → 实现后 `go test ./internal/service -run TestCollabInviteSend -v` 23/23 绿（修 1 个测试自身 bug：UnavailableContent 子测试内容 id 撞主键）。
- 关键覆盖：owner/contributor 可邀、pending 内容可邀、9 个错误码路径、并发容量 9+2 并发 → 1 成功 1 `CONTRIBUTOR_LIMIT_REACHED`、pending 去重容量语义（已 contributor 的 pending invite 不占位）、expired 可重新邀请、typed message 豁免冷启动守卫但普通文本仍 `DM_REPLY_REQUIRED` 直至回复、Redis 不可用零 DB 写、DB 失败（plpgsql 触发器）后补偿删 key、Asia/Shanghai 午夜 key 切换、TTL 86400s。
- 全量门：`go build ./...`、`go vet ./...`、`go test ./...`（24 包全 ok）绿；改动文件 `gofmt -l` 干净（分支上另有 6 个与本任务无关的 pre-existing 未格式化文件，未触碰）。

### Notes:
- 未改动 config.go/config.yaml/routes.go/migrations → 无需 doc-validator；未触碰前端与主 worktree。
- 遗留（供后续任务）：`ListConversationSummaries` 最后消息 raw SQL 不含 msg_type/metadata，Task 4/5 卡片渲染需补；容器/handler/路由接入为 Task 4 范围。
- **两阶段审查 DONE_WITH_CONCERNS 修复**（commit `fix(collab): address Task 2 review concerns...`）：① `SendInvite` 在 Redis 预留前新增廉价容量预检（spec §5.3 链第 2 步「先做容量预检」），事务内权威复核保留；新增 `TestCollabInviteSendCapacityPreCheckFiresBeforeRedis`（Redis 宕机时容量已满仍返回 `CONTRIBUTOR_LIMIT_REACHED`，证明预检先于 Redis）+ `TestCollabInviteSendRejectsContributorLimit` 断言 `mr.Keys()` 为空；② 新增 `TestCollabInviteSendCompensationFailureIsLogged`（关闭 miniredis 直接驱动补偿路径，slog JSON handler 断言 ERROR 级结构化日志含 count/user key，沿用 content_media_gallery_test 日志捕获先例）；③ 删除 `CollabInviteRepository.Transaction()`/`DB()`（零调用者，service 用 `NewCollabInviteRepository(tx)`）；④ `ErrInviteRateLimitUnavailable` 文案改 UPPERCASE 码 `INVITE_SERVICE_UNAVAILABLE`（标识符不变，Task 4 handler 映射 503）；⑤ `CountPendingInviteesNotContributors` 的 `'pending'` 字面量参数化为 `model.CollabInviteStatusPending`。接受不修：banned inviter → `ErrNotContentOwner`（spec 无 INVITER_UNAVAILABLE 码，Task 4 路由守卫先返回 USER_BANNED，service 为纵深防御）。修复后 `-run TestCollabInviteSend` 25/25 绿、`go build/vet/test ./...` 全绿、gofmt 干净。

## [2026-08-12] - Ticket 10（light）：相关内容块与到底提示（桌面/web）（#90）

### What was done:
- **组件（Step 3）**：
  - `frontend/components/content/RelatedContents.tsx`（新，ui-spec:2761 #80/#90 权威）：桌面/web（≥1100px，组件内 matchMedia 自判定，移动端不渲染不请求）相关内容块 = 关联原创/二创行（复用 RelatedFanworks 组件，embedded 嵌入单容器）+ 相似内容行（固定复用列表 API：同 zone/content_type/category，fanwork 有 IP 再带 ip_id，sort=hot&page_size=12；不新增临时 similar endpoint，不用带筛 selected recommended）+ 底部「已经到底了」提示（media.related.endReached，与 #89 移动端 media.continue.endReached 语义分开）；客户端去重（排除当前内容 id 与关联行 prop 摘要 + RF onData 实际行 id）后最多 8 条；空分支（两行都无数据）只渲染到底提示不渲染空块标题；similar 错误行内错误 + 重试。
  - `RelatedFanworks.tsx` 小扩展（复用优先）：可选 props `onOpenDetail`（浮层栈内打开卡片）、`onData`（行数据落定回报，RelatedContents 去重用）、`embedded`（嵌入模式不渲染自身 bordered 容器，宿主提供外层单容器）；默认行为不变。
  - `ContentDetail.tsx`：新可选 props `onOpenRelatedDetail`/`relatedFanworksSummary`；新增 isDesktop（matchMedia ≥1100px）——桌面端行内 RelatedFanworks 降级隐藏（由评论区之后的 RelatedContents 统一承载，避免同页重复关联作品），<1100px 保持原行内行；评论区之后渲染 RelatedContents（slot 的 titleKey 覆写为 media.related.relatedTitle「相关创作」）。
  - `ContentDetailOverlayLayer.tsx`：桌面端（≥1100px）向 ContentDetail 传关联行 slot（复用 RF 合同）+ relatedFanworksSummary（layer 已为侧栏拉取的数据，去重用）+ onOpenRelatedDetail（onPush 压栈，栈深 ≤5 沿用）；<1100px 不传（移动端关联入口维持 #89 语义）。
  - `ContentDetailOverlayHost.tsx`：`onOpenRelatedDetail` 接入既有 useContentDetailOverlay（source=zone-page），相关内容卡片浮层栈内打开。
  - i18n：`media.related.*`（relatedTitle/similarTitle/endReached/error.*/a11y.similarList）zh/en 同步。
- **放置裁决**：spec（2 处）与 ui-spec Key Constraints + 截图检查点均写「正文与评论区之后」；ui-spec「与现有组件关系」括号内的扩展区顺序（RelatedContents 在 CommentSection 前）与之矛盾——按多数（含 issue #90 正文「在正文和评论区之后组装」）实现为评论区之后，PR 描述记录该 ui-spec 内部矛盾供 #65 修订。
- **测试（Step 4）**：
  - `frontend/tests/related-contents.test.tsx`（新 10 例）：固定 list 合同精确 URL 断言（fanwork+ip 带 ip_id、original 不带、无 category 省略、sort=hot 且非 recommended）、双行渲染 + 去重 ≤8、嵌入单容器（RF 行无嵌套 border）、空分支仅到底提示、错误 + 重试、移动端不渲染不请求、onOpenDetail 回调。
  - `#88/#89` 三个受影响测试适配（jsdom 桌面视口下相关块新增 h2 标题行）：openOverlay/openContext 等待改按名称区分；#89 AC4 桌面断言由 queryByText("You've reached the end") 改为 `[data-slot="media-continue-end"]` 不存在（#90 块提示与 #89 移动连续浏览提示语义分开）。
  - `frontend/e2e/t90-related-contents.mock.spec.ts`（新 3 例契约测试）：桌面双行 + 固定合同请求断言（zone/content_type/category/ip_id/sort=hot/page_size=12）+ 去重 10→8 + 卡片浮层栈内打开/返回 + 到底提示；空分支仅 hint；移动端不渲染不发起相似请求。

### Testing:
- `npm run lint`（tsc strict）、`npm run lint:ui`、`npm run build` 全绿；`npm run test:unit` 411/411 全绿（含新增 10 例）；`npm run test:contracts` 37/37 全绿。
- 浏览器实测（Playwright mocked contracts，zh）：桌面双行块（相关创作 1 张 + 你可能也喜欢 8 张、当前内容与关联行去重）、空分支（仅「已经到底了」）、卡片在浮层栈内打开（612→613 压栈、返回逐层回退）、移动端 375px 无块无请求。
- 截图 4 张（`git add -f` 入库，同 #85/#88 先例）：`screenshots/t90-related-contents-{desktop,empty,mobile,overlay-stack}.png`。

### Notes:
- 未触碰：后端文件、config/routes/migrations、MediaGallery/MediaViewer/SourceAttribution/PublishForm/SourceContentPicker、主 worktree；doc-validator 无需运行（无 config/routes/migrations 改动）。
- 遗留：ui-spec RelatedContents「与现有组件关系」段与 Key Constraints/截图检查点的放置矛盾待 #65 修订（本票按多数实现为评论区之后）；桌面 overlay 侧栏相关列表（#88）与块内关联行并存为既有面与新面并存，未做归并。

## [2026-08-12] - Task 8（closure）：全量验证与文档同步（#96）

### What was done:
- **Step 1 后端聚焦门**：`go test ./internal/model -run TestContentSourceMigration -v` 2/2、`go test ./internal/service -run TestValidateSource -v` 9/9、`go test ./internal/handler -run "TestCreateContentRoute|TestListRelatedFanworks" -v` 全绿（含 original/fanwork 两向 related 行为、不可见来源 404、content_type 单值/多值/非法、sort/limit 语义）。
- **Step 2 后端全量门**：`go test ./...`（32 包全 ok）、`go vet ./...`、`go build ./...` 全绿。
- **Step 3 前端门**：`npm run test` 375/375、`npm run lint`（tsc strict）、`npm run build` 全绿；`npx playwright test e2e/studio-publish-fanwork.spec.ts` 2/2 绿（默认配置 testMatch 仅 `*.integration.spec.ts`，沿用 t6 先例用等价临时 baseURL :3000 配置跑同一批 spec，跑完删除临时配置，不入库）。
- **Step 4 doc-validator**：`go run . --fix` 仅更新 `docs/reference/schema.md`（content_items 表补 `source_fanwork_id BIGINT -> content_items.id` 1 行）；config/route sync 无变化；再跑一次幂等（no changes）。
- **Step 5 浏览器验证（ego-browser, zh, :3000 → :8088 真实后端）**：7 项全过——original/137 相关二创行（卡片 229 + 开始创作 ?source_original_id=137）；original/26 无 fanwork 时整行隐藏；content/229 归因「灵感来源：…」→ /original/137；content/17481 二创来源归因 → /content/229；content/229 「衍生作品」行（17481 卡 + ?source_fanwork_id=229）；publish 页三源（IP 可选 + 原创来源 + 二创来源）搜索/互斥正常，选原创来源后提交按钮解除 disabled；无 IP/来源提交被本地化校验「发布二创需要 IP 或灵感来源」拦截（提交钮 disabled）。
- **截图 6 张** `screenshots/community-{source-picker-desktop,source-picker-mobile,source-attribution-desktop,source-attribution-unavailable,related-fanworks-desktop,derivatives-mobile}.png`（screenshots/ 被 .gitignore 排除，`git add -f` 入库，同 #85/#88 先例；像素抽样验证非空白）。
- **Step 6 收口**：确认 Task 0–7 checkpoint commits 均在 main（t1 0b85f25/#157、t2 785355f/#158、t3 1e07199/#159、t4 e407340/#160、t5 2cc68a1..#161、t6 f3ec98c..#162、t7 347b862..#163，t0 为验证-only 无独立提交）；仅 stage Task 8 证据（doc-validator 生成物 + 截图 + progress.txt + 计划勾选），单 commit，未重写 Task 0-7 实现代码。

### Testing:
- 见各 Step；Plan Self-Check 15 项逐项核对全部通过（错误码 6 个与条件、无陈旧单来源文档、历史无源行不受阻、SourceContentPicker canonical、搜索复用可见性、多值 content_type 契约、SOURCE_IMMUTABLE 创建期不可变、无「三创」、不可用来源行为、双来源互斥、浏览器覆盖等）。

### Notes:
- 环境：docker postgres/redis 复用主 worktree 已有健康容器（omnicraft-postgres/omnicraft-redis）；后端 `SERVER_PORT=8088` 起在 8088（config.go 支持 SERVER_PORT env 覆盖）；前端 `NEXT_PUBLIC_API_URL=http://localhost:8088` 起在 :3000；验证数据沿用 Task 7 fixture（137/229/17481/17482/17483）。
- 截图暗色为主分布与已入库 t7 截图一致（浏览器环境默认暗色渲染），像素抽样确认含内容非空白。
- 未触碰：主 worktree、Task 0-7 已提交实现代码；doc-validator 仅改 schema.md 生成节。
## [2026-08-12] - Task 7（light）：来源归因与相关/衍生作品行（#96）

### What was done:
- **ui-spec 确认（Step 1）**：`design/ui-spec.md` 两节均存在（`## Component: SourceAttribution` :2635、`## Component: RelatedFanworks` :2697），按视觉权威实现，克制视觉处理。
- **组件（Step 3/4）**：
  - `frontend/components/content/SourceAttribution.tsx`（新）：仅 fanwork 且存在内容级来源时渲染；source_original → `/original/:id`，source_fanwork → `/content/:id`；仅 IP 不渲染；有 id 无摘要渲染灰色不可点击「灵感来源：内容已下架」（`sourceAttribution.unavailable`）；`inline-flex gap-1 text-xs text-fg-muted` + lucide GitBranch w-3.5 + line-clamp-1 + py-2 触控命中区，aria-label 区分来源类型。
  - `frontend/components/content/RelatedFanworks.tsx`（新）：props 按计划原文；`GET /api/v1/contents/:id/related-fanworks?page=1&page_size=8`；loading 骨架（标题 + 4 卡）→ total=0 隐藏 → 横向滚动卡片行（148/160/180px 三档响应式，复用 ContentCard）；「查看全部」仅 total>8 且 viewAllHref 存在时渲染；错误态内联 + 重试；外层单容器 `border border-border-default bg-canvas-default p-4`。
  - i18n：`sourceAttribution.*`（original/fanwork/unavailable/a11y.*）与 `relatedFanworks.*`（original.title/derivatives.title/actions.viewAll/actions.create/error.loadFailed/error.retry）zh/en 同步；衍生标签「衍生作品」，无「三创」硬编码（组件源码 + messages 双重断言）。
- **接线（Step 5）**：`ContentDetail` 新增可选 props `sourceOriginal`/`sourceFanwork`（标题元信息下、正文上方）与 `relatedFanworks`（SeriesNav 同级、评论区上方）；original 详情页传 viewAllHref=既有 `/original/[contentId]/fanworks` + createHref=`?source_original_id=`；fanwork 详情页（ContentDetailOverlayHost）传归因 + 衍生行（首版不传 viewAllHref）；Overlay Layer 两处 ContentDetail 传 source 摘要，避免浮层内 fanwork 误渲染「内容已下架」。不做递归来源链。
- **TYPE_FILTERS 修正（Task 4 遗留协调点）**：`fanworks/page.tsx` 的 `text,article,prompt` 在 Task 4 严格 allowlist 下会 400；改为「文本」→ `article,prompt`、「全部」→ 不发送 content_type（后端空值=不过滤，否则漏 audio 等类型）。浏览器验证全部/文本过滤均 200。
- **验证数据**（dev DB）：T7 衍生测试 17481（source_fanwork_id=229，published）、下架来源对 17482/17483（17482 已软删，17483 呈现 unavailable 态）；迁移 064 直接应用至共享 dev DB（schema_migrations 账本本就残缺，未触碰账本）。

### Testing:
- 红→绿：先写 `frontend/tests/source-linkage-components.test.tsx`（11 条：计划 7 断言 + createHref 两向 + viewAll 边界），组件不存在时 ERR_MODULE_NOT_FOUND 红；实现后绿（缺 i18n key 时 MISSING_MESSAGE 红 → 补 key 绿）。`node --import tsx --test` 11/11。
- 门禁：`npm run lint`（tsc strict）/`npm run lint:ui`/`npm run build` 全绿；`npm run test:unit` 375/375；`npm run test:contracts` 34/34（18080 端口先确认空闲）。
- 浏览器实测（ego-browser, zh, :3001 → :8088）：original/137 相关二创行（卡片 229 + 开始创作 ?source_original_id=137，无查看全部 total=1）；content/229 归因链接「灵感来源：…」→ /original/137 + 衍生作品行（17481 卡 + ?source_fanwork_id=229，无「三创」）；content/17481 二创来源归因 → /content/229；content/17483 灰色「灵感来源：内容已下架」不可点击；fanworks 页全部/文本过滤 200（文本正确空态）；移动端 390px 衍生行横向滚动。截图 7 张 `screenshots/t7-*.png`。

### Notes:
- 未触碰：主 worktree、后端文件、SourceContentPicker.tsx/PublishForm.tsx；无 config/routes/migrations 改动（064 仅本地应用，无需 doc-validator）。
- 首版范围裁剪遵循计划：衍生作品不建列表页，不传 viewAllHref。
## [2026-08-11] - Ticket #86（light）：全屏媒体查看器（缩放/翻页/三种退出）

### What was done:
- **`frontend/components/content/MediaViewer.tsx`（新）**：按 ui-spec §MediaViewer 实现。**portal 隔离**：`createPortal` 挂 `document.body`，脱离详情浮层 FLIP transform 祖先对 `position: fixed` 的约束；再以原生 `<dialog>` + `showModal()` 进入浏览器 top layer——普通 z-index 无法压过同为原生 dialog 的详情浮层（实测验证，ui-spec 的 `fixed inset-0 z-[60]` 类保留标注层级意图）。全屏 `inset-0 h-dvh w-full` 深色背景 `bg-black/90`；媒体 `object-contain` 不裁切，缩放用 `transform: translate() scale()` 不破坏布局。
- **缩放（AC2）**：滚轮（原生非 passive 监听，React 事件系统对 wheel 以 passive 注册 preventDefault 不生效）+ 双指捏合（pointer events 距离比）+ 放大/缩小/重置按钮（×1.5 步进，钳制 [1,5]）+ 双击复位；缩放态单指平移（`touch-action: none` + setPointerCapture，位移按舞台尺寸钳制）；视频项不缩放、直接渲染播放器（controls/autoPlay/poster）。
- **翻页（AC3）**：左右按钮（首末 disabled）+ 触摸滑动，仅未缩放态生效（手势优先级 缩放>平移>翻页）；翻页复位缩放与平移；底部沿用 MediaGallery 指示点视觉语言（透明圆=未浏览/实心=当前，`role=group` aria-label 位置）+ 顶部「第 X / 共 N」位置标签；不做内容级切换（AC5，查看器无任何上下文列表/连续浏览回调）。
- **三种退出（AC4）**：背板点击（pointerup 于空白舞台、位移 < 10px）、关闭按钮、Esc。Esc 用 window capture 阶段监听 + `stopPropagation` + `preventDefault` 自包含：capture 同步关闭后，浏览器对同一 keydown 的 dialog-cancel 默认动作会落到新的最顶层 dialog（详情浮层），`preventDefault` 阻止该默认动作，浮层不被连带关闭（浏览器实测发现并修复）。关闭后焦点还给触发媒体区（MediaGallery 侧 `requestAnimationFrame` + `tabIndex={-1}` 包装层）；打开期间锁定 html/body 滚动并恢复原值（与浮层锁幂等叠加）。错误占位 + 重试（`media.viewer.error.*`）。
- **`frontend/components/content/MediaGallery.tsx`（最小接入）**：仅新增内部 `viewerIndex` state + 触发元素 ref + `<MediaViewer>` 渲染；`handleMediaClick` 在 `onOpenViewer` 已由上层消费时转发（#88/#89 预留），未消费则内部自持状态（当前唯一路径）；不触碰既有翻页/指示点/几何稳定逻辑。
- **i18n**：`media.viewer.*`（title/position/close/previous/next/zoomIn/zoomOut/zoomReset/imageAlt/error.loadFailed/error.retry）zh/en 同步 append-only，未带其他键改动。

### Testing:
- `npm run lint`（tsc strict）/ `npm run lint:ui` / `npm run build` 全绿；`npm run test:unit` 335/335（新增 media-viewer.test.tsx 19 条：进入索引/位置/按钮+滚轮+双击缩放/钳制/捏合/平移/视频不缩放/触摸滑动与手势优先级/翻页复位/背板/拖拽不误关/关闭钮/Esc 捕获不传播/滚动锁/错误重试；media-gallery.test.tsx 同步更新 #86 前「无 handler 惰性」预期 + dialog stub）；`npm run test:contracts` 27/27。
- 浏览器实测（Playwright real :8080 + worktree 前端 :3001，zh）：20/20 断言——/original?category=sports 浮层内点媒体开查看器（从点击项开始，第 1 / 共 4）、查看器盖在浮层上方、放大 1.5×/滚轮/重置/双击、指示点 4 个、下一张翻页、Esc 只关查看器且浮层保持打开、焦点回触发媒体区、背板点击关闭、关闭按钮关闭、视频项播放器且无缩放控件、移动端进入/触摸滑动翻页/背板点击关闭、关闭后浮层仍在。截图 8 张 `screenshots/t86-viewer-*.png`（桌面 1280×800 + 移动 390×844，像素抽样验证非空白）。

### Notes:
- 关键决策：查看器必须用原生 dialog 而非纯 z-index 浮层——详情浮层经 `showModal()` 位于浏览器 top layer，`z-[60]` 的 fixed div 永远渲染在其下（浏览器实测：layer check）。原生 dialog 进入同一 top layer 且后开者在上，无需穿透。popover API 方案实测不可行：模态 dialog 打开时页面其余部分 inert，`showPopover()` 静默无效。
- 验证数据沿用 #85 fixture：content 27（4 图：横/竖/方/超高）+ 29（2 视频），`?category=sports` 使二者进入前 12 条避免深翻页；gallery SVG（g01-g05）为 #85 后缺失的 gitignored 资源，已在本 worktree 重建（不入库）。
- 未触碰 config/routes/migrations（无需 doc-validator）；未触碰 #88 划片文件（ContentDetail/Overlay*/ContentSidebar）与主 worktree。progress.txt 只更新本 worktree 副本。

## [2026-08-11] - Ticket #68（light）：内容详情浮窗共享元素转场 + 媒体加载（C2+C6 收敛 + FLIP/VT）

### What was done:
- **C2+C6 收敛**：删除 `ContentDetailClient`（无生产调用方：仅 fs-exists 守卫的源码扫描测试 + e2e mock 函数名；其第二套浏览历史记录器随之消失）；ContentDetail 数据/props 归一至 `lib/content.ts` canonical 类型（fanwork 详情页本地 ContentItem/Attachment/ContentResponse 重复类型删除）；`IP: ` 硬编码改 `content.ipLabel` zh/en i18n（无新增硬编码可见文案）；独立页与浮层侧栏收敛到唯一 `ContentSidebar` owner（relatedItems/onOpenRelatedItem/followAction/relatedFooterAction props），浮层内私有 AsideCard 侧栏删除；共享入口控制器 `use-content-detail-overlay.tsx` 接入推荐流/分区页/IP 页/Agent 引用/详情页宿主，入口只保留来源参数+触发 ref+上下文列表差异。
- **转场与媒体加载**（#67 原型 §5 契约）：FLIP 几何为核心，`document.startViewTransition` 特性检测渐进增强；路径选择 `reduced ? fade : (!source ? fallback : (vt ? vt : flip))`；300ms 开 / 240ms 关 / 共享缓动 / reduced-motion 100ms 纯 opacity；run token + cancelActive 中断处理；VT 快照唯一命名 + 清理，`t.finished` reject/同步 Abort → FLIP 兜底；入场保险定时器；外壳 opacity-0 起步。`ContentDetail` 新增 `coverSync`：加载态在最终封面几何内、正文/头部 invisible 保持布局、失败稳定占位仍显示可用详情；封面容器 w-full 与正文同水平边界，高度上限只裁内框。`ContentCard` 加 `card-cover` data-slot（FLIP source 锚点）。
- **测试**：新增 overlay-motion（15）、use-content-detail-overlay（3）、content-cover-sync（5，next/image 在 jsdom 下与测试错误互相作用会挂死 node:test runner，测试内 stub 为 plain img 规避——真实浏览器行为由 Playwright 覆盖）；focused 102/102、全量单测 295/295、tsc、ui-governance、`npm run build` 全绿。
- **浏览器验证**（Playwright real :3000 + :8080，Chromium 1223，zh）：21 项断言全过——VT 开合、FLIP 开（中段 matrix 几何）/关反向回归、离屏关闭降级居中缩淡（matrix(0.96...)）、reduced-motion 封面 transform 恒 none、封面加载态（skeleton+body hidden→reveal）、封面 404 占位+正文可用、分区页/IP 页返回文案、移动端滚动锁、Agent 引用（登录 seed-ui-001 + mock 流式引用）返回对话。截图 `screenshots/t68-*.png`（桌面 1440×900 + 移动 390×844）。
- **验证脚本**：`/tmp/t68-verify.mjs`（未入库，11 场景 21 断言；真实后端 + 局部 route mock：Agent 流式引用与 config/public——真实 config `web_agent_enabled=false` 且 LLM 凭证缺失，与 Web Agent Task 6 同阻塞源）。

### Testing:
- `npm run test:unit` 295/295；`npm run lint`（tsc）；`npm run lint:ui`；`npm run build` 全绿；浏览器 21/21 断言 + 11 张截图。

### Notes:
- commit `105d223`（motion core）→ `22e7209`（C2+C6 收敛）→ `ace2d7c`（转场+媒体加载）→ `screenshots+progress`。
- 会话中途 macOS Desktop TCC 权限吊销一度阻塞（已人工恢复）；期间完成的批次与证据见 ticket 评论。
- Agent 入口浏览器验证使用 route mock 的流式引用（真实 LLM provider 缺失）；#85/#88/#89 将在此基础上接入媒体集/双栏/连续浏览。

## [2026-08-11] - Ticket #85（light）：详情/浮层媒体区（MediaGallery）

### What was done:
- **`frontend/lib/content.ts`**：`AttachmentData` 扩展 `width/height/sort_order`（仅本票划片）；`normalizeAttachment` 归一化（`positiveInteger` 宽高 / 新增 `nonNegativeInteger` 支持 sort_order=0，蛇形+帕斯卡键，NULL/历史行保持 undefined）。
- **`frontend/components/content/MediaGallery.tsx`（新）**：按 ui-spec §MediaGallery 实现——contain 不裁切（image `<img>` object-contain / video `<video controls preload="metadata" poster>` 顺序播放器）；容器几何由首项宽高比决定（aspect-ratio style），会话内切换不跳版；超高图（height/width>2）限高 70vh + `overflow-y-auto` 内部滚动；宽高缺失防御性 3:4（私有常量）；底部低调指示点（透明圆=未浏览/实心=当前，`role=group` aria-label「第 X / 共 N」）；左右按钮（44px 触控目标、首末 disabled、i18n aria-label）+ touch swipe 翻页（仅水平位移>40px 且 |dx|>|dy|，不抢页面滚动）；末项上滑预留 `onReachEnd`（#89）；`onOpenViewer` 可选（#86 接入前媒体区不可点，video 点击排除 controls 条）；单媒体失败稳定占位（`media.gallery.error.loadFailed`）不阻断切换；首项 settle 回调 `onFirstMediaSettled` 驱动浮层 coverSync；`selectMediaItems` 导出：image/video 内容中 file_type=image/video 且有 URL 的附件进媒体集（保持后端稳定顺序不重排），其余进下载列表，其他内容类型全部维持下载列表（AC3）。
- **`frontend/components/content/ContentDetail.tsx`**：image/video 内容渲染 MediaGallery 替代 CoverImage（组件自带 `data-slot="detail-cover"`，浮层 FLIP/VT 转场锚点与封面同步合同保留；媒体集为空时回退 CoverImage，历史内容仍可见）；附件下载列表与 Download All 仅渲染非媒体集素材；sheet_music 语义不变。
- **i18n**：`media.gallery.*`（position/previous/next/imageAlt/error.loadFailed）仅新增键，zh/en 同步（parity 通过）。
- **测试**：`frontend/tests/media-gallery.test.tsx` 14 用例（selectMediaItems 拆分、normalizeAttachment 宽高/顺序、渲染顺序与 aria-current/hidden、首项几何与 3:4 默认、超高图限高滚动、指示点、按钮边界、touch 滑动（jsdom 手动构造 touches）、onReachEnd、视频 controls/poster、错误占位、onOpenViewer、几何稳定）；`runtime-test-helpers.tsx` 增补 `media.gallery` 测试消息。

### Testing:
- `npm run lint`（tsc strict）/ `npm run lint:ui` / `npm run build` 全绿；`npm run test:unit` 300/300；`bash scripts/verify-project.sh --full` 23 mocked contracts 全过（无 media gallery 契约改动）。
- 浏览器实测（Playwright real :3000+:8080，Chromium headless，zh）：本地 DB 手动应用 063 幂等 DDL + 插入内容 27（4 图：横/竖/方/超高，含 width/height/sort_order）、29（2 视频 + poster 内容封面）、30（历史 NULL 宽高 + pdf 附件）；断言全过——桌面容器 926×520.9 16:9 首项、翻页后几何不变、prev 首项 disabled、移动左/右滑动翻页、超高图 scrollH=1020>clientH=191 内部滚动、视频 controls/preload=metadata/poster、顺序播放器翻页、历史数据 0.75 默认比例、下载列表仅含 pdf；浮层（overlay）实测：卡片点击开共享浮层后画廊渲染、coverSync body reveal、浮层内翻页几何稳定、移动单列。截图 10 张 `screenshots/media-gallery-*.png`（含 overlay 桌面/移动）。

### Notes:
- 401 均为未登录 `/api/v1/auth/refresh` 全站既有行为，与本次改动无关。
- 本地 fixture 库迁移账本只到 004（表结构已存在），未跑全量迁移；验证数据用 063 幂等 SQL 手工补齐（`sort_order` 列 + 唯一索引），未改代码/迁移文件。
- 验证媒体资源 `frontend/public/seed-media/real/`（gitignored，从 main worktree 只读复制 + 新增 5 张 SVG 画廊测试图）不入库。
- 待办：#86 MediaViewer 将消费 `onOpenViewer`；#88 桌面双栏将向 MediaGallery 传高度约束；#89 连续浏览将接入 `onReachEnd`。

# OmniCraft 万象工坊 - 开发进度日志

> **轮换规则**：本文件仅保留最近约 30 天的条目；更早记录已归档至 `docs/archive/progress/`。超过 64KB 或每月轮换一次（上次轮换：2026-08-07）。完整历史见 `docs/archive/progress/2026-08-01-to-2026-08-07.md`。

## [2026-08-10] - 修复 #127 + #128（light）：config override 逐字段合并 + queue DLQ nil 兜底

### What was done:
- **#127**（LoadOverride 部分 section 覆盖清零兄弟字段）：`LoadOverride` 改为按 override 文件中实际出现的顶层 section 逐个 `UnmarshalKey`（`AllKeys` 收集 section + `mapstructure` tag 反射定位字段），只应用文件中出现的 key。回归测试锁定契约：部分 `queue` section 保留 `max_attempts`/`retry_backoff_sec`/`dlq_ttl_hours`/`worker_*`；同文件部分 `queue`+`rate_limit`；缺失文件 no-op。
- **#128**（queue sendToDLQ err=nil nil 解引用 panic）：`sendToDLQ` 对 `err==nil` 用 `"unknown error"` 兜底；`handleMessage` 在 `maxAttempts<=0` 时直接 ACK（不调 handler、不进 DLQ、不 panic），消费者 goroutine 存活。`:180` 的 `err.Error()`（ensureGroup）核实已有 nil 守卫，无需改动。
- 修复验证细节：对 viper v1.21.0 实测 10+ 种 override 形状（含 issue 所述精确形状与 ops/load/config-override.yaml）均无法复现 3→0；修复仍把「仅应用已出现 key」的保证固化到代码层并加测试锁定，不依赖 viper/mapstructure 解码语义。
- TDD：两个 #128 测试修复前均失败（panic / 消费者死亡消息滞留 pending），修复后通过。

### Testing:
- `go build ./... && go vet ./... && go test ./...` 全绿；gofmt 干净；doc-validator `--fix`（config 文档无变更）。
- PR #130 合并 main@fddb0db，4 workflow 全绿：CI 31388024033 / Security 31388881500（首次 dispatch 被用户取消后重跑）/ SBOM 31388029672 / Tauri 31388033042；#127（PR 自动关闭+证据评论 5240524409）、#128 已关闭附证据。

### Notes:
- 两 issue 无 checkbox，无需勾选计划文件；未占用注册表 DAG。下一可执行任务：#68 / #81。

## [2026-08-10] - Web Agent Task 6 完成：真实 Provider 发布验证（MiniMax M1）

### What was done:
- 用户提供 MiniMax API key，解除 Task 6 阻塞。按注册表从最新 main 重开 `web/agent-t6`，cherry-pick 旧分支 4 个实质提交（AutomaticEnv 移除 + MiniMax provider + 前端流式 CSRF + 验证修复/文档，冲突全部按 ours/incoming 解析）。
- **Step 1**：Go build/vet/test 24 包、前端 unit 246+26 / lint / lint:ui / build 全绿。
- **Step 2**：新增 `frontend/e2e/agent-workspace.mock.spec.ts`（9 用例）：gating/无 widget/无 Header Agent 模式、接地回答（delta+工具+引用）、no-evidence、stop 中止（浏览器原生流式 fetch mock 响应 AbortSignal）、429 错误卡、历史打开/删除确认与取消、320/375/414px 移动抽屉、键盘 Tab 聚焦 + reduced motion、引用打开 ContentDetailOverlay；`npm run test:contracts` 23/23。
- **Step 3**：真实 smoke 双级验证——provider 级 `agent-smoke` 4 场景（injection 显式拒绝）；Agent/API 级经真实后端 + 163 篇已嵌入内容 + MiniMax M1/embo-01：cited（工具执行 + 引用 content 26）、no_evidence、injection（未执行工具）、乱码（不编造），trace id 记录。
- **Step 4**：5 张证据截图由 mock 套件产出（gitignore）。
- **Step 5**：仓库默认 `web_agent_enabled=false` 保持；生产启用待人工裁决。
- 顺手修复：`config.go` 补 `SERVER_PORT` env 覆盖（AutomaticEnv 移除后的回归）；`gitignore` 补 frontend/test-results。

### Testing:
- 如上 Step 1/2/3/4 全部通过；`go test ./config` 通过。

### Notes:
- 新发现并开 issue：#127 LoadOverride 部分 section 覆盖清零兄弟字段（实测 queue.max_attempts 3→0）；#128 sendToDLQ err=nil nil 解引用 panic（max_attempts=0 触发）。均不在本任务修复面。
- smoke 用临时 SQL 用户 + dev 库，结束后已清理；MiniMax key 仅经 env 注入，未写入仓库。

## [2026-08-10] - 会话收尾：CI 全绿确认、#101 关闭、工作树清理

### What was done:
- 确认 main@27b89d3 的 4 个 workflow 全部 completed success（CI / Security / SBOM & Provenance / Tauri CI）；93f1855 上的失败系 TS2339 类型错误，已被 27b89d3 修复。
- 今晨 04:27 performance 调度 run 失败原因确认：该 run 跑在修复前 main（3546796 = PR #122 合并点，不含 #101 修复 796bbf7，后者 2026-08-10 14:06 才合入），Load gate Smoke 步骤失败为预期行为，非回归。
- 手动 dispatch performance workflow（run 31381037700，main@27b89d3）：Load gate 全绿（Contract + Smoke + Load + Stress 通过），#101 关闭条件满足，issue 已关闭并附证据。
- 恢复 `content-series.mock.spec.ts` 运行重写的 6 张基线截图（community-content-series-*.png，既有 spec 行为，按先例提交前恢复）；`git fetch --prune` 后确认无残留本地分支/worktree，工作树干净。

### Testing:
- gh run list / gh run watch 验证：4 workflow 绿 + performance dispatch run 绿；`git status` clean。

### Notes:
- 待办：#127 / #128（config LoadOverride 覆盖清零、sendToDLQ nil panic）仍 OPEN，未在任何计划内，可作独立 light 修复；#68/#81 为下一个可执行任务（ready-for-agent）。

## [2026-08-10] - Load gate 修复（#101 跟进）：k6 写路径退出媒体类型

### What was done:
- 定位 2026-08-10 04:27 调度 run 的 Load gate 失败：k6 `content create 200/201` 检查失败（1/13 请求），根因是 #83 媒体合同（063 migration）后 `image` 类型发布必须携带媒体集 + OSS upload grant（`ErrUploadGrantInvalid`），k6 旧 payload 无附件必然 400。
- 修复：`tests/load/k6/testdata.json` 的 `content_types` 收窄为 `["article"]`。媒体合同（含 grant 校验）由既有 Go/前端契约测试覆盖（content_upload_grant_test.go 等），k6 容量套件无 OSS 环境无法构造合法媒体发布；恢复容量测量本意。
- 本地复现与验证：迁移新建 `omnicraft_load` 库 → 以 `ops/load/config-override.yaml` + `SERVER_PORT=8081` 启动 → `run-load-tests.tests.sh` 契约全过 → Smoke（rate=0.00%，content create ✓）+ Load（rate=0.00%，content create ✓）双绿。

### Testing:
- `bash scripts/load/run-load-tests.tests.sh`：All run-load-tests contract cases passed.
- Smoke tier：`✓ content create 200/201`、`✓ rate<0.01`（rate=0.00%）；Load tier 同样双绿。
- curl 直连验证：article 创建 201；image 单/双附件无 grant 返回 400 UPLOAD_GRANT_INVALID（与修复方向一致）。

### Notes:
- 本地环境发现：8080 端口被一个 Java 进程（PID 50343）占用，OmniCraft dev/load server 需换端口（本批用 8081）；本机未装 psql，load runner 需用 `-SeedDb` transport。
- #101 关闭条件：下一次 performance 调度 run（周一）Load gate 绿。

## [2026-08-10] - CI 降本方案 A：全工作流移除 push 触发，仅按需 dispatch + 周调度

### What was done:
- 背景：方案 2 上线后 agent 仍直推 main（10:19/10:34/11:02 三次直推各触发全套 ≈ $0.25-0.30/次），且 #130 合并后 6 分钟有手动 dispatch 全套（12:26，CI/SBOM 跑完、Security 已取消）。用户决定升级为方案 A：**push 触发全移除，CI 只在 workflow_dispatch 手动按需跑**；验收完全本地化。
- 触发收敛：`ci.yml`/`security.yml`/`sbom.yml`/`tauri-ci.yml` 删除 `push: branches: [main]`；保留 workflow_dispatch（ci 保留 failure_probe 演练输入）、security 周一调度、performance 周一调度、sbom release 发布事件。performance.yml 本无 push 不变。
- retention 简化：无 push 后 `${{ github.event_name == 'push' && 90 || 30 }}` 死分支移除，ci/security/tauri 统一 `retention-days: 30`；sbom 改为 `release && 365 || 30`。
- 契约同步：`verify-workflows.sh` 断言改为 push/pull_request 均禁止 + workflow_dispatch 必含 + retention 必须 30 天；`verify-workflows.tests.sh` 新增 push 禁止 2 组（ci+tauri）、dispatch 必含 2 组，retention 变异用例改为 30→90 失败。TDD 红→绿：先在旧工作流上确认红（retention needle 不存在、push 触发仍在），改完转绿。
- 运行清理：12:26 dispatch 批次中 Security run（31388026980）已取消；CI（31388024033）与 SBOM（31388029672）在分析期间已跑完不可撤销。

### Testing:
- `bash scripts/ci/verify-workflows.tests.sh` 全绿；5 个工作流 on-block 结构核对无 push/pull_request 残留（rg 零匹配）。
- 未触发任何 GitHub Actions 运行做验证（本地契约测试即验证门，符合"尽量在本地验证"）。

### Notes:
- 效果：合并 PR / 直推 main 均零触发，月度花费趋近 0（仅周一 security ~13min + performance ~3min ≈ $0.02/周 + 手动按需）。验收 = 本地 `verify-project.sh` + UI 截图；需要 CI 证据时 `gh workflow run` 手动跑。
- 未改 config.go/routes/migrations，无需 doc-validator；并行任务（#127/#128 及后续）不受影响。

## [2026-08-10] - CI 降本（用户决定方案 2：GitHub Actions 仅 main 合并时跑）

### What was done:
- 背景：私有仓库全部计费（Linux $0.008/min），实测每次 push/PR ≈ 60 job-min（CI 35-45min + Security 13min + SBOM 11min），一天烧 ~$1.5。分支保护仅要求 project-gate。
- 用户选定方案 2：**PR 期完全靠本地验证（scripts/verify-project.sh + UI 截图），GH Actions 只在 push main + 手动/调度时跑**。
- 触发收敛：`ci.yml`/`security.yml`/`sbom.yml`/`tauri-ci.yml` 删除 `pull_request` 触发（保留 push main + workflow_dispatch；security 保留周一调度，performance.yml 保留周一调度 + dispatch，删除 PR paths 块及其 `!= 'pull_request'` 死条件；sbom.yml 删除 PR preview 步）。
- **project-gate 去重**：删除其完整重跑（checkout/go/node/npm ci/verify-project.sh/上传证据），只保留依赖结果聚合 + failure_probe 演练步（原每次重复跑 backend+frontend+docs 全套 ~12min，纯浪费）。ci.yml 155 行 → 155 行（缩减 48 行）。
- tauri-ci detect 步简化 `HEAD~1`（PR base sha 分支已成死代码）；注释同步。
- 契约同步：`scripts/ci/verify-workflows.sh` 断言改为「push 必含、pull_request 禁止、push 不得带 paths 过滤」；tauri-windows 跳过断言从旧 step 级 no-op 改为 job 级 `needs.detect.outputs.changed == 'true'`（修复 66944bb 换机制后契约过期、套件一直红的问题）；`verify-workflows.tests.sh` 对应翻转 3 组用例 + 新增 push-paths 禁止用例。TDD：先翻转测试确认红，再改实现转绿（"real workflows must satisfy the contract" 红 → 全绿）。
- **分支保护（gh api PUT）**：main 的 `required_status_checks`（project-gate, strict）置 null 移除——CI 不再在 PR 报告任何 check，保留 required 会永久阻塞合并；force-push/删除/创建保护保留。移除后 main 无自动化 required check，验收完全依赖本地验证门（AGENTS.md Step 4）。

### Testing:
- `bash scripts/ci/verify-workflows.tests.sh` 全绿（红→绿：先翻转断言在旧工作流上失败，改完通过）。
- 5 个 workflow YAML 结构校验通过；`gh api` 确认保护 JSON `required_status_checks: null`。

### Notes:
- 预估：合并前零消耗，每次 main merge ≈ 45 min（backend+frontend+docs+ops+security+sbom）≈ $0.36；若每天 1-2 次合并 ≈ $0.4-0.7/天，对比此前 ~$1.5/天。
- 遗留已结清：`tests/load/k6/lib/scenarios.js` 的并发修改为 2026-08-10 会话的 Load gate 排查（PR #125），最终修复落在 `testdata.json`（content_types 收窄为 article），scenarios.js 已还原为原始状态。
- 未改 config.go/routes/migrations，无需 doc-validator。

## [2026-08-10] - Ticket 04 (#84): 媒体集上传编排 UI

### What was done:
- `FileUploader` 增加媒体集模式：多选、图片/视频类型校验、九宫格预览、HTML5 拖拽排序、键盘/按钮排序、移除、首项封面标记、进度与单项错误状态；图片采集 natural width/height，视频首帧经 canvas 生成独立 poster 并复用 oss-token 上传合同。
- `PublishForm` 接入 public config 的媒体数量上下限，配置加载失败时阻止媒体发布；附件 payload 携带 `width`、`height`、`sort_order`，视频使用 `poster_grant_id`，图片继续按首项派生封面；旧附件模式保持兼容。
- 新增中英文媒体编排文案、public upload 类型、mock public config、媒体排序/移除/数量/类型校验测试，以及 mocked Playwright 合同（解除阻塞后已执行，见下）。
- 修正媒体客户端校验异常：内部错误只保留稳定错误码，展示文案统一经 `next-intl` 映射；移除失败项时清理整体错误提示。
- 修正 `PublishForm` 复用时媒体配置请求切换到非媒体类型后残留 loading 状态的问题。

### Testing:
- 前端项目门：`npm run test:unit` 243/243、`npm run lint`、`npm run lint:ui`、`npm run build` 均通过。
- 上传窄测试：`node --import tsx --test --test-concurrency=1 tests/content-upload-grant.test.tsx` 5/5 通过；`git diff --check` 通过。
- Go `build`/`vet` 通过；Go 全量测试 `go test ./...` 23 包全绿（docker postgres ephemeral + miniredis）。
- `npm run test:contracts`（mocked Playwright）：14/14 通过，含新增 `studio-publish-media.mock.spec.ts`（修正确认：内容类型按钮可访问名含描述文案，选择器由 `/^Image$/i` 放宽为 `/Image/i`）。
- 截图：`screenshots/omnicraft-media-uploader-desktop.png` / `-mobile.png` 已由 mock spec 生成（gitignore，未入仓）。

### Notes:
- 阻塞已于 2026-08-10 解除（Docker 恢复可用）：Go 全量测试与 mocked Playwright 补跑通过，本票验证闭环完成，提交合并。
 (Ticket 04 (#84): 发布端媒体编排 UI（多选/排序/宽高/poster）)

## [2026-08-10] - Ticket 03 (#83) 安全修复批次（P1-1/P1-2/P2-1/P2-2/P2-3/P2-4）

### What was done:
- **P1-1**：`NewContentHandler` 链式配置补 `.WithImageDimensionsResolver(ossSvc)`（content.go），真实视频发布路径可经 OSS 对象头解析 poster 尺寸。
- **P1-2**：`validatePosterContract` 视频只要求 `PosterGrantID`；客户端 `poster_width/poster_height` 降级为被忽略的兼容字段（不参与校验、不持久化），尺寸一律由服务端从对象头派生。补测试：无客户端宽高成功、错误客户端宽高仍用对象解析结果、image 内容携带 poster 宽高被忽略。
- **P2-1**：`imageinfo.Parse` 对 PNG/JPEG/WebP 强制 `width>0 && height>0`，否则 `ErrMalformedImage`；补零宽/零高/JPEG/WebP 测试。
- **P2-2**：图片上传 MIME 合同收敛到 parser 支持的 `image/png|image/jpeg|image/webp`（`imageinfo.IsSupportedMIME` 单一真源）。内容上传路径（`GeneratePresignUploadURL`，覆盖 poster/媒体）严格校验；发布期 poster/image 媒体 grant MIME 校验同步对齐。feedback 附件保留自身 gif 合同（走独立端点，不受影响）。补 presign 接受/拒绝矩阵 + 其他类型合同保持测试 + handler 路由 400 测试。
- **P2-3**：事务回滚清理日志改为从 `consumedGrants`（服务端真值）构建 `ossKeysToCleanup`，包含已消费 poster grant 与媒体附件 grant 的真实 OSSKey；不再使用客户端提交的（可伪造）OSSKey。补 slog JSON 捕获测试断言真实 key 存在、`forged` key 不存在。
- **P2-4**：`docs/reference/schema.md` 同步 `content_items.cover_width/cover_height`、`content_attachments.sort_order`（真源 063）。根因修复：doc-validator schema 生成器原先只解析 CREATE TABLE，`--fix` 会丢弃 ALTER TABLE ADD COLUMN 列；现 `parseAlterTableColumns` 合并 ALTER 列并补单测，`--fix` 幂等后 schema.md 同步全部 ALTER 列（含此前缺失的 036/040/041/048/050 等列，纯增量 29 行）。

### Testing:
- TDD 红→绿：imageinfo 零尺寸、service 无客户端宽高/错误宽高/清理日志、handler 接线与路由 400 先失败后通过。
- `gofmt -l`（全部改动 Go 文件）：干净。`git diff --check`：通过。
- `go build ./...`、`go vet ./...`、`go test ./...`（23 包）：全绿（PostgreSQL ephemeral + miniredis 本地可用，无外部依赖）。
- `tools/doc-validator`：`go test ./...` 通过；`--fix` 幂等；`--check --profile release` 仅剩 1 个 pre-existing warning（`docs/working/2026-08-08-overlay-transition-prototype-observations.md` 无到期日头，origin/main 同样失败，#67 文件，不在本票修改面）。
- 未改动前端/Tauri、config.yaml、routes.go、迁移编号与历史迁移语义；未提交前 PR #122 仍 Open。

### Notes:
- 环境：Docker 未在 compose 项目内运行但 5432/6379 端口已有监听（com.docker 代理），ephemeral postgres + miniredis 测试全部可用；未启动 dev server（纯后端修复，无需浏览器验证）。
- PR #122 的 GitHub Actions 因本月额度耗尽 3 秒内失败（非代码失败）；不得用 workflow/branch-protection 改动绕过，合并须等 P1 修复提交推送后人工复核真实证据。
- schema.md 的 ALTER 列同步同时修正了生成器 pre-existing 缺口（source_original_id 等此前从未出现在文档中），纯增量、`--fix` 幂等。

## [2026-08-09] - Ticket 03 (#83) 验证复核

### What was done:
- 复核媒体数量、纯净类型、连续 `sort_order`、poster grant、cover 尺寸和 public config 合同；保留显式 delivery domain，移除未签名 bucket endpoint fallback。
- 确认新 video 发布缺少 poster grant 时拒绝；旧的“服务端快照回退”说明仅为历史记录，不能作为当前合同。
- 额外收紧 poster/媒体 grant：仅允许 video 使用 poster 字段，并核验 image/video grant 的 MIME 前缀；移除未再调用的服务端视频快照回退函数。

### Testing:
- `go test ./config ./internal/handler -count=1`、`go test ./internal/service -count=1`、`go vet ./...`、`go build ./...`：通过（service 测试使用 loopback/miniredis）。
- `git diff --check`：通过。

### Notes:
- 私有 OSS 的完整“数据库相对 key + 读取时签名 URL”仍未在本票中闭合；poster 宽高仍由请求字段提供，尚未完成对象内容派生。未提交或合并。

## [2026-08-08] - Ticket 03 (#83): 媒体数据/发布合同（063 migration + 服务端校验）

### What was done:
- `backend/migrations/063_content_media_metadata.sql`（forward-only，未动历史迁移）：`content_items` 加可空 `cover_width/cover_height`；`content_attachments` 加可空 `sort_order` + 同内容部分唯一索引 `uq_content_attachments_item_sort_order`（`WHERE sort_order IS NOT NULL`，遗留 NULL 多行仍可读）。
- `config.yaml > upload` 与 `config.UploadConfig` 新增 `image_gallery_min_items=2/max_items=9`、`video_gallery_min_items=1/max_items=3`；public config（`/api/v1/config/public`）经新 `PublicUploadDTO` 只暴露这 4 个非敏感限制；前端不得复制常量（#84 消费）。
- 发布合同（`content_service.go`）：
  - `validateMediaGallery` 在任何 upload grant 消费与写库前校验数量上下限、纯净类型（`file_type` 与 `content_type` 一致、禁混排）、`sort_order` 从 0 连续且同内容唯一、宽高为正；image/video 拒绝客户端任意 `cover_image_url`。零配置回落到规格默认（2-9/1-3），非媒体类型（article 等）维持旧附件语义。
  - `validatePosterContract`：video 的 poster 必须带正宽高；image 拒绝 poster grant。
  - poster 安全合同：video 的 `poster_grant_id` 在事务内消费 + 核验属当前发布者且为 image，再经配置的 delivery domain 派生 cover URL 与 `cover_width/height`；私有 bucket 不再 fallback 为未签名 endpoint，失败回滚并 restore grant。签名读取/相对 key 的完整媒体 delivery contract 仍需单独收口。
  - 封面派生：image = `sort_order` 最小项（即 0）→ cover URL/宽高；`is_primary` 改为 `*bool` 只标记派生封面条目（video 无附件行携带，全部 false；遗留非媒体保持 DB 默认 true，下载端点已适配指针解引用）。
  - 修复回归：附件行原在 `CreateContent` 前以 `content.ID=0` 组装导致 FK 写 0（新测试抓出），现插入前回填 `ContentItemID`。
  - `GetAttachments` 固定 `sort_order ASC NULLS LAST, id ASC`（`content_repo.go`），列表零 join 由 `ContentItem.CoverWidth/Height` 提供。
- 测试：service 错误路径（数量/混排/宽高/顺序/任意 cover URL/校验先于 grant 消费/视频缺 poster）、正常路径（image 首项派生 + is_primary 唯一 + 顺序持久化、video poster 派生 + 双 grant 消费、poster 非 image 拒绝后恢复、image 拒绝 poster）、`TestPersistentObjectURL`（delivery domain/私有 endpoint 不 fallback/空输入）；handler 路由错误路径（400 MEDIA_SET_INVALID，`MEDIA_SET_INVALID` safe message）；model 迁移双跑幂等 + 唯一索引 + NULL 兜底顺序；migration gap fixture（061 缺口 + 063 后到 + 补缺 runner 语义，不补 061、不改历史 fixture）；public config 恰好 4 键并归一化默认限额。
- `newContentGrantPublishService` 加 `SetMaxOpenConns(1)`（sqlite :memory: 读写回查，沿用 browse_history/admin_notification_broadcast 先例）。

### Testing:
- TDD 失败证据（HEAD 仅测试文件）：model 迁移测试 `no such file: 063...`；service 编译失败（`ErrMediaSetInvalid`/`SortOrder` 未定义）；handler 编译失败（`UploadConfig` 字段未定义）；gap fixture 在 HEAD 即绿（覆盖既有 runner 语义）。实现后转绿。
- `go build ./...`、`go vet ./...`、`go test ./...`（22 包）全绿。
- 迁移双路径：空库 + 历史 fixture 升级（model ephemeral postgres + migration runner fixture）通过。
- `cd tools/doc-validator && go run . --fix`（同步 `docs/reference/config.md`）后 `--check --profile release` 绿。
- 真实 server curl（8097 端口）：`/api/v1/config/public` 返回 4 个 upload 限制；注册+登录后 POST 发布——单图太少/混排/重复顺序/负顺序/任意 `cover_image_url` 均 400 `MEDIA_SET_INVALID`；article 走旧 grant 路径（`UPLOAD_GRANT_INVALID`，证明非媒体语义未受影响）。正常路径因无 OSS 凭证由 handler/service 测试覆盖。
- 期间 Docker daemon 意外退出，已重启 omnicraft-postgres/redis 恢复环境，测试复跑全绿。

### Notes:
- 两个非阻塞 concern：video 无 poster grant 时保留既有服务端快照回退（强制要求会破坏 #84 之前的前端；#84 将始终提交 poster grant）；`docs/reference/schema.md` 未列新列（doc-validator schema sync 为权威，报告无漂移，migrations 为真源）。
- 未触碰：`backend/internal/pkg/llm/*`、`backend/cmd/agent-smoke/main.go`、`config.yaml agent:` 段、frontend/、063 以外迁移与历史 checksum fixture。
- **阻塞（2026-08-08 18:20，仅剩 GitHub 发布步）**：实现/测试/commit 全部完成，但本机 GitHub 连通性中断（gh 报 `dial tcp 127.0.0.1:443: connection refused`；系统代理 127.0.0.1:7892 无监听进程；直连 api.github.com 失败）。无法执行 `gh issue edit 83 --body-file` 勾选 AC 与 `gh issue close 83`。已备好 `/tmp/t83-body.md`（8 个 AC 已改 [x]）与关闭说明。解除后运行：
  `gh issue edit 83 --body-file /tmp/t83-body.md && gh issue close 83 --comment "完成说明见下"`（gh token 在 macOS keyring，必要时 `export GH_TOKEN=<keyring 中 gho_ 开头 token>`）。

## [2026-08-09] - Ticket 02 (#82) 验证修正

### What was done:
- 保留加载失败时的内联错误与全局 Toast；add/remove/create 仍只显示弹窗内 notice。
- 修正测试对重复错误文案的查询，明确断言同时存在两处文案和 `alert` Toast。

### Testing:
- `npm run test:unit`：234/234 通过。
- `npx tsc --noEmit --incremental false`、`npm run build`：通过。

### Notes:
- 当前仅验证和修复 worktree，未提交或合并。

## [2026-08-08] - Ticket 01 (#65): 对齐 P-01 重叠边界与 UI 权威契约
## [2026-08-09] - #101 Load gate 修复（新规则试运行首轮）

### What was done:
- 复现：干净库（omnicraft_loadtest，61 迁移）+ `-SeedDb` 后端复现 Smoke `detail 200` 失败；历史 run 日志已过期（404）不可查。
- 根因：seed 只建 load-test 身份不建内容，干净环境无 `published` 内容，详情路径解析不到 CONTENT_ID。
- 修复 `scripts/load/run-load-tests.sh`：新增 `seed_content()`，幂等插入 1 条 published 内容（title/category/content_type 取自 testdata.json，作者为 load-test-001，zone=original）；新增 `-SeedDbName` 参数（默认 omnicraft），`DB_RUN` 不再硬编码库名（CI `-DbDsn` 路径不受影响）。
- `scripts/load/run-load-tests.tests.sh` 补 `-SeedDbName` 契约用例。
- 同时作为开发流程优化计划（docs/working/2026-08-09-development-flow-optimization-plan.md）的试运行任务：无外部依赖，仅启动本任务依赖的 postgres/redis/后端（8090，8080 被本机 java 进程占用），scoped 验证。

### Testing:
- 契约测试 `bash scripts/load/run-load-tests.tests.sh`：PASS。
- 真实 Smoke（clean env，`-SeedDbName omnicraft_loadtest`）：seed 身份 5 个 + 内容 1 条 → `resolved content id 1`；health 3/3、search 2/2、detail 4/4、login 1/1、auth/me 1/1 全过；p(95)=32ms、p(99)=47.84ms、error rate 0%；阈值全部 ✓；cleanup 正常。
- `bash -n` 语法检查通过。

### Notes:
- 本地验证环境与 CI 的差异处理：CI 用 `-DbDsn`（psql），库名在 DSN 内；本地 `-SeedDb` 容器路径补 `-SeedDbName` 对齐。CI workflow 无需改动。
- 提交与合并：#101 的 PR 推送后等 required `project-gate`，绿后合入 main 再关闭 issue。



## [2026-08-08] - 分支/计划审查收口：合并文档批次并修正 #71→#69→#72 依赖

### What was done:
- 将已通过 PR #102 检查的 `docs/gate-recovery-closure` 三提交批次合入本地 `main`，保留 3.6 GiB 部署档、IP history/favorites heavy 计划、迁移预留和活动计划审计修正。
- 核对本地/远程分支、工作树、PR 与 #64/#80/#96/#97 tickets；确认 `origin/docs/media-experience-spec` 是已合并前的旧媒体规格，`origin/fix/publish-freeze-key` 的运行时代码已由 #79 合入，均不重复合并；归档 `codex/archive/unreachable-*` 分支不属于当前 Web-only 任务来源。
- 将 GitHub 原生依赖补齐为 `#97 → #71 → #69 → #72 → #73`，并同步 `AGENTS.md` 与 `docs/working/2026-08-08-active-plan-audit.md`，避免共享文件被并行修改。

### Testing:
- `git diff --check`
- `cd tools/doc-validator && go run . --check`
- `bash scripts/release/deployment-contract.tests.sh`

### Notes:
- 当前唯一注册工作树保持干净；未发现任何工作树中的未提交文件。远程 Dependabot PR 仍需各自 CI/兼容性审查，不在本次规划合并范围内。

## [2026-08-08] - 3.6 GiB 面试服务器部署档裁决（含 Prometheus）

### What was done:
- 复核 Compose 真实服务清单：根目录本地集成 compose 为 12 服务，单服务器生产模板为 17 服务；`architecture.md`/`README.md` 原 6 服务清单仅覆盖 Web 核心，不能代表当前发布合同。
- 核算单服务器模板声明上限：完整常驻服务约 5.7 GiB；6 个 Web 核心加现有 Prometheus 约 3.9 GiB，均未计 Ubuntu/Docker/页缓存，因此不适合在 3.6 GiB 主机上按原上限全量常驻。
- 将资源受限面试档写入 `README.md`、`architecture.md` 与单服务器 runbook：保留 6 个 Web 核心、一次性 `migrate` 和 backend-only 精简 Prometheus；完整 exporter/Alertmanager、cAdvisor/Blackbox/node-exporter 与 Loki/Alloy/loki-gate 在该主机暂缓。
- 明确精简档仍须保留不可变镜像、Redis/PgBouncer 安全合同、迁移账本、仅 Nginx 对外、结构化日志/轮转、健康/就绪检查及备份恢复，不把“少启动容器”表述为完整生产观测已通过。

### Testing:
- `git diff --check` 通过。
- `bash scripts/verify-project.sh -Scope Docs -ReportDir /tmp/omnicraft-interview-profile-docs` 通过：doc-validator Go tests 与 release profile check 全绿。
- 本地 PostgreSQL、Redis 容器已运行且 health 为 healthy；本轮未启动或修改服务器服务。

### Notes:
- 当前完整 `ops/observability/prometheus.yml` 依赖 Alertmanager、各 exporter、cAdvisor、Blackbox 与 node-exporter。服务器切换前仍需单独实现并验证 backend-only Prometheus 配置及 compose override/profile；本轮按用户范围只更新本地文档，没有改生产 compose 或服务器。
- 3.6 GiB 档是 Web-only 面试展示档，不替代资源充足环境下的 17 服务完整生产观测档，也不解除真实告警投递/外部 heartbeat 的发布证据要求。

## [2026-08-08] - Task #92 closure revalidation: transient Security gate failure

### What was done:
- 核对 `main` 最新提交 `079d2a4` 的 Security run `31225081492`：首次运行因 runner 下载模块时 `proxy.golang.org` 返回 403，且 Trivy filesystem/image 扫描未产出完整结果而失败；未发现漏洞 finding 或 secret finding。
- 在隔离的 `origin/main` 临时副本重跑 Go、Trivy filesystem、Trivy image 三门，全部通过；随后只重跑失败的 GitHub Security job，attempt 2 以 job `93038361451` 在 8m41s 通过。
- 复核 `main` 的 CI/SBOM/Tauri 与 Security 全部成功；#93/#94/#95 与 #92 已关闭，当前不需要修改安全规则、allowlist 或生产代码。

### Testing:
- 本地 Docker 授权环境：`verify-alerts.tests.sh`、`alert-drill.tests.sh`、`generate-sbom.tests.sh`、`verify-security.tests.sh`、`verify-project.tests.sh` 全部通过。
- `cd tools/doc-validator && go run . --check --profile release`、`deployment-contract.tests.sh` 全部通过。
- 隔离副本：`bash scripts/security/verify-security.sh -Gates go,trivy-fs,trivy-image -BuildImages ...` 的 summary 为 `go:0`、`trivy-fs:0`、`trivy-image:0`、`verdict:0`。

### Notes:
- 本条只记录远端瞬态失败与重跑证据；未改安全门强度、未提交代码、未覆盖工作区已有未提交计划审计改动。
- 独立的 performance `Load gate` 仍因 smoke 数据集没有 approved content 失败（run `31220493995`）；该问题不属于 #92，本轮未修改其 seed/fixture。

## [2026-08-08] - CI gate recovery 三票完成（#93/#94/#95）与 #92 闭门准备

### What was done:
- **G1 (#93) Ops alerting 根因定案**：CI 诊断步证实 runner 上无 ripgrep（`NO RG: 1`）——`verify-alerts.sh`/`alert-drill.sh` 及两个 contract tests 中的 `rg -q/-n` 在无 rg 环境静默失败，触发 "alertmanager.yml must keep receiver values as placeholders" 假阳性；macOS 本地因 Homebrew rg 存在而通过。手记中 "docker pull TLS 超时" 理论被证伪（runner 位于 westus3，拉取正常；pre-fix 的 ~12s 失败 = 拉镜像耗时 + 随后 rg 假失败）。修复：4 个 ops 脚本的 `rg` 全部替换为 POSIX `grep -qE/-nE`；同时把 prom/prometheus、prom/alertmanager 固定为 sha256 digest（服务器侧腾讯 mirror 交叉验证）+ 显式 `docker pull` 带重试 + 失败时输出真实 docker stderr（满足 #92 "失败诊断输出不再被吞" 验收）。新增 pinned-tooling 合同测试。
- **G2 (#94) SBOM 修复落地**：保留上一会话未提交的 `generate-sbom.sh` 修改（`--user $(id -u):$(id -g)` + `HOME=/work` + `TMPDIR=/work/tmp` + mkdir tmp），新增 Linux bind-mount seam 静态断言测试（macOS 无法复现 root-owned 语义，故用契约固定）；uid 语义用 alpine 实测确认（容器文件归宿主 uid，宿主 python 可重写）。
- **G3 (#95) gitleaks 收窄**：`.gitleaks.toml` allowlist 增加 3 个 release-gate 测试夹具脚本路径 + 归档文档双路径（`docs/superpowers/plans/` 历史路径 + `docs/archive/plans/` 现路径，前者覆盖 history scan）；本地 tree+history 扫描 0 findings，"fake secret fails the gitleaks gate" 正向检出用例保持通过。
- **Docs gate 修复**：`docs/working/2026-08-08-architecture-fix-backlog.md` 与 `active-plan-audit.md` 补 `**预计失效日期**:` 半角冒号头部（doc-validator 正则要求）。
- 以 `docs/superpowers/plans/2026-08-08-omnicraft-ci-gate-recovery.md` 的 G1/G2/G3 勾选 + 本条目 + 子票关闭作为各任务收口；PR #98（合并 60d4b31）为普通合并，未用 admin merge。

### Testing:
- 本地全绿：`verify-alerts.tests.sh`、`alert-drill.tests.sh`、`generate-sbom.tests.sh`、`verify-pinned-actions.tests.sh`、`verify-security.tests.sh`、doc-validator release profile。
- 服务器（Ubuntu 24.04，无 rg）复现验证：`verify-alerts.sh -ConfigDir ops/observability` 由 `ruby: command not found`（服务器无 ruby，非 CI 环境差异）→ 与 CI 一致失败点确认；grep 替换后本地通过。
- CI 首轮全绿：Backend/Frontend/Docs/Ops alerting/SBOM/Security/project-gate/Desktop 全部 pass（run 31216862012/41/88）。

### Notes:
- #92 关闭条件（main 连续两次全绿 + 后续普通 PR 不经 admin merge 通过 required checks）尚差 main 上的连续绿 run 验证，保持 OPEN。#93/#94/#95 子票待关闭。
- 工作区存在另一 agent 的未提交 docs 改动（AGENTS.md/社区 plans/specs/progress 等），本会话未触碰其文件面；progress.txt 与其条目共存。
- 剩余生产项（不属 #92）：`GREEN_CALLBACK_ALLOWED_IPS` 需阿里云控制台人工获取；compose 镜像 digest 固化按 §1③ 服务器侧解析；服务器 compose 与仓库脱节、staging drill 未执行（需用户决策）。

## [2026-08-08] - 活计划/GitHub Issues 全量审计与执行图收口

### What was done:
- 审计 AGENTS 注册表、constitution/templates、#64/#80 specs、社区 plans、架构 backlog、当前 migration/route/model 真实状态和 GitHub issues #1/#64~#76/#80~#98；报告更新于 `docs/working/2026-08-08-active-plan-audit.md`。
- 将 CI gate recovery 登记为 priority 0 heavy 计划；#93~#95 挂为 #92 原生子票并由 PR #98 承载。Constitution 3.0.0 已用 AGENTS registry + GitHub dependencies + light/heavy lanes 取代退休的 `task.json` 工作流。
- 统一迁移预留为 media `063` → source-linkage `064` → collaboration-invites `065` → IP history `066` → favorites drop `067`；同步社区 spec/plans、媒体 spec、协调/backlog 和 release contract fixture。
- 交换媒体 T03/T04 职责：#83 为 heavy 数据/发布合同（`sort_order`、cover 宽高、config/public config、服务端媒体/poster grant 校验），#84 为依赖 #83/#65 的 light 上传 UI；补齐 #85/#87/#90 owner 与 fixed similar-content source。
- GitHub 原生 DAG 回读验证：#80 挂 #81~#90；#96 等待媒体 T01~T09 terminal tickets，#90 等 #96，#97 等 #96/#90；Web light chain 之后才进入 #73(066)→#74→#75→#76(067)。#76 改 `ready-for-human`，tracking parents #64/#80 移除 AFK-ready label；已归档完成的 #1 关闭。
- 新增 #73 IP visit history 和 #76 favorites cutover 两份 heavy implementation plans，锁定 HTTP/时间戳/batch/部分失效项合同，以及 #75 compatibility release→日志/backup restore 人工门→067 drop→forward-fix 的生产边界。#68 验收纳入 C2+C6 收敛。

### Testing:
- `git diff --check` 通过；旧迁移预留 grep（排除 archive）无残留。
- `bash scripts/release/deployment-contract.tests.sh` 全部通过。
- `cd tools/doc-validator && go run . --check`：release profile all checks passed。
- GitHub API 回读确认 #80/#92 原生子票、#65~#97 blocked-by 图和 #76 label 与本地注册表一致。

### Notes:
- 未勾选任何功能 ticket/plan checkbox，未宣称 #92 完成。PR #98 当前 SBOM gate 已绿，但 Ops alerting gate 仍失败且其他 checks 尚在运行；#92 必须继续保持开放，直到 main 连续两次全绿并有普通 PR 验证。
- 本轮只补计划、tracker 和直接相关 release fixture；未开始 #65~#76/#81~#90/#96/#97 的功能实现，也未恢复 Desktop 范围。

## [2026-08-08] - 架构审查裁决落盘（backlog 文档 + 注册表交错序 + #84 验证门）

### What was done:
- 合并收尾：PR #91（媒体计划注册，文档）与 PR #79（C1 冻结键修复）经 admin-merge 合入 main（`4aea507` / `733a58d`；progress.txt 冲突已调和）；#78 随 #79 自动关闭；分支已清理。
- 查明三个 pre-existing 全红 CI gate（Ops alerting 自 Ops-04 引入即从未绿过、SBOM /tmp 权限、Security gitleaks 夹具误报），建 heavy 车道 issue #92 修复；修复前合并需 admin-merge。
- 落盘三处决策：① AGENTS.md 注册表 #64 行与媒体行改为 2026-08-08 确认的交错执行序（T03/T04 提前、媒体 T05~T10 在其后、source-linkage 之后回归）；② 新建 `docs/working/2026-08-08-architecture-fix-backlog.md`（C1~C8 全部裁决与时机、次要项不建 issue 记录、gate #92）；③ issue #84 验收项增加非连续 migration fixture 验证门（C7 前移）。

### Testing:
- 纯文档改动；未改 config/routes/migrations，doc-validator 不适用。

### Notes:
- 未勾选任何计划 checkbox；注册表行修改仅反映已确认的执行序裁决。
- 下一步执行建议：C2+C6 批（须在 #64 T03/T04 与媒体 T05 前）；或按注册表先行 #64 T01/T02/T06 与媒体 T01/T02/T03。

## [2026-08-08] - C1: 发布冻结键分叉修复（P1 bug，issue #78）

### What was done:
- 修复 P1 bug（issue #78）：`publishGuard` 中间件读取 `user:publish_freeze:%d` 而写入方使用 `publish:freeze:%d`，生产拦截静默失效。
- 新增共享 key builder `internal/pkg/rediskeys.PublishFreezeKey(userID)`（纯 stdlib，返回规范键 `publish:freeze:<id>`，与存量 Redis 数据兼容），middleware 守卫（interaction.go:88）、review_service 写入（删除 `buildPublishFreezeKey`）、content_service 读取（content_service.go:115）三处统一复用。
- 测试：新增 `rediskeys` 单测（键格式精确断言）；middleware 夹具改用 canonical key 并强化 `PUBLISH_FROZEN` 响应码断言；新增跨 seam 测试（以 review_service 相同方式写 canonical key，守卫 403 `PUBLISH_FROZEN`）；handler 层新增 `POST /api/v1/contents` 端到端 seam 测试（403 `PUBLISH_FROZEN`，policy 对齐生产 `publishingInteractionPolicy`）；auth_test.go:299 夹具改用 canonical key，`TestLoginInteractionCapabilityMatrix` 行为断言不变。
- 从 `git status` 发现的无关未提交改动 `docs/GLOSSARY.md`（媒体集/附件术语条目）未纳入本次提交，保持未提交状态。

### Testing:
- `gofmt -l`：改动文件全部干净（仅存量未格式化文件 pre-existing）；`go build ./...` / `go vet`（四个相关包）通过。
- `go test -count=1` `internal/pkg/rediskeys` / `internal/middleware` / `internal/service` / `internal/handler` 全部通过；全量 `go test ./...` 无失败（Postgres/Redis 容器已运行）。

### Notes:
- 分支 `fix/publish-freeze-key`，单 commit，PR 以 `Fixes #78` 开头；未改 config/routes/migrations，未运行 doc-validator（非必需）；未勾选任何计划 checkbox，未改 AGENTS.md。

## [2026-08-08] - 媒体体验设计会话（/grilling + domain-modeling + to-spec + to-tickets）

### What was done:
- 用户实测报告三问题：原创区筛选不生效、详情封面被裁切成横向矩形、多图/多视频上传与画廊缺失。定位根因：前端 `original/page.tsx` 死代码（`search.sort || "hot"` 永不为空，分类选中仍发 `sort=recommended`）+ 后端 `handleRecommended` 无视筛选（curl 验证 recommended+category 返回混合分类）；封面裁剪为 `ContentCard` 强制 3:4/16:9 与 `ContentDetail` 强制 16:9（违背 ui-spec 自适应高度）；`content_attachments` 的 width/height 从不写入。
- /grilling 逐项确认 14 项决策（含收藏集弹窗两处补充发现：背板/Esc 不关闭、toast 被浮层变暗遮盖）：媒体集 vs 附件语义拆分（同表不拆）、媒体集纯净（image 2~9 / video 1~3 不混排可混纵横比）、首图即封面 + 视频首帧 poster（前端 canvas 截帧）、老数据不兜底重建、contain+几何稳定+指示点、全屏媒体查看器、自然比例瀑布流、桌面双栏+移动单列、移动端连续浏览（上下文列表上滑）、桌面相关内容块不做自动加载、上传九宫格编排、筛选 bug 前后端修法、收藏集弹窗关闭行为 + 弹窗内通知。
- 产出：spec 文档 `docs/superpowers/specs/2026-08-08-omnicraft-media-experience-design.md`；ADR `docs/adr/0004-media-experience-paradigm.md`；GLOSSARY 新增 6 术语（媒体集/附件/媒体查看器/连续浏览/相关内容/首图即封面）；CONTEXT.md 内容浏览域导航更新；GitHub spec issue #80 + tickets #81~#90（全部 ready-for-agent，原生阻塞边 83→84→85、86←85、87←84、88←85、89←85/88、90←88）；AGENTS.md 注册表新增优先 2 计划行（与 #64 串行规则、迁移 067 钉名）。

### Testing:
- 无实现代码改动；本轮为设计/规格/票据产出，验证门不适用。
- 事实核查使用 curl 实测后端筛选行为（recommended+category 混分类 vs hot+category 正确）+ 代码阅读（ui-spec、ContentCard/ContentDetail/FileUploader/CollectionPicker/ConfirmModal、后端 handler/service/repo）。

### Notes:
- 未勾选任何计划 checkbox；注册表更新仅新增媒体体验计划行（#80/#81~#90），未动 #64 及既有计划描述。
- 下一执行建议：按注册表优先 1（#64 T01/T02/T06）先行，或按文件预留协调直接开始媒体体验 T01/T02/T03（无阻塞，light）。

## [2026-08-08] - 审计收口：证据、文档与本地发布配置

### What was done:
- 重新审阅 Ops-05/Ops-06/Ops-08 的原始日志并完成 redaction 检查；在最终 reviewed `HEAD` 上使用仓库 finalizer 重新计算 evidence SHA-256 并绑定三个 summary。
- 收口 Ops-08 状态、RPO/RTO、runbook、Web 体验规格、生产 readiness 汇总、协调计划和 CI Go 版本文档；没有启动任何新开发任务。
- 将 `.env`、`.env.production`、`frontend/.env.local` 权限收紧为 `0600`。`.env.production` 仍因包含 `your-domain.com`、空回调 IP 和本地真实密钥而不可作为生产输入；真实值不写入仓库，发布使用 `.env.production.example` 和外部 secret 管理。
- 生产 Compose 不再给 PgBouncer 提供 `latest` 默认值，服务器必须显式提供 registry-verified `@sha256` 引用；本机 registry 查询不可用时不伪造 digest。
- 只读查看所有 unreachable commits 的提交摘要和父子关系；未删除、未 prune、未恢复任何对象。

### Testing:
- 运行证据 schema/finalizer 校验、文档 validator、生产 Compose/发布契约门和最终 Git 状态复核。

### Notes:
- unreachable commits 的逐项摘要仅作为本次审计交付信息，不改变活计划来源；在用户审核前不处理这些对象。
- Web Agent Task 6、Web 体验 Ticket 01 及其他活计划均未开始。

## [2026-08-07] - 审查修正收尾：Ops-08 解除阻塞前记录

> 本条记录发生在下方同日 Ops-08 Step 5 完成记录之前；当前状态以完成记录和本次 2026-08-08 证据收口记录为准。

### What was done:
- 接管并复核 ChatGPT/opencode 审查会话留下的未提交修复；确认当前 `main` 工作树只有本轮发布门、Agent、Reaction、配置、文档和测试改动。
- 当时保持 `release/recovery-objectives.json` 为 `baseline_only`，生产 readiness 注册表与 Ops-08 Step 5 均诚实标记为 blocked；未用历史 Release 摘要替代可复核的 staging/OSS/off-site 原始证据。
- Agent 工具预算改为纯配置驱动，缺失配置在配额/SSE 前返回 `AGENT_CONFIG_INVALID`；客户端取消流的历史清理使用独立超时上下文，避免请求取消留下孤儿会话。
- 真实发布演练补齐 smoke URL 输入门；归档器使用 OSS AES256 元数据并在成功前校验远端保留元数据、过期字段与归档 commit；进度日志按 64KB 规则完成轮换。

### Testing:
- Go：`go test ./...`、`go vet ./...`、`go build ./...` 全部通过（使用临时 GOCACHE/GOMODCACHE；测试夹具本地 Redis 监听使用受控权限）。
- 前端：`npm run lint`、`npm run lint:ui`、`npm run test:unit`（227/227）、`npm run build` 全部通过；生产构建固定为已验证的 `next build --webpack`，避免本机 Turbopack 构建无输出挂起。
- 发布/文档：`go run . --fix` 无漂移；preflight、deployment-contract、staging-drill、archive-release-evidence 契约测试全部通过；PostgreSQL/Redis 容器 healthy。
- 浏览器：推荐卡片打开/返回共享 ContentDetailOverlay、匿名 ReactionBar 禁用态已验证；截图保存至 `screenshots/audit/recommend-content-detail-overlay.png`。
- 聚合门：`GOCACHE=/private/tmp/omnicraft-go-build-cache GOMODCACHE=/private/tmp/omnicraft-go-mod-cache bash scripts/verify-project.sh --full` 全部通过（含 8 个 mocked Playwright contracts）。
- `npm install` 与 `go mod tidy` 完成；Go 首次默认缓存受沙箱权限限制，改用临时缓存复验。

### Notes:
- 当时 Ops-08 Step 5 仍需人工提供并实际执行真实 staging deploy/rollback、OSS 版本恢复、off-site 双目的地归档、approved 且 commit-bound 的 RPO/RTO 与原始测量证据。
- Web experience corrections 的 12 个 tickets、Task 6 真实 Provider smoke 和桌面能力不属于本轮收尾，不提前开发或勾选。

## [2026-08-07] - 审查反馈清理：死代码/类型契约/推荐流 SWR 重构

### What was done:
- 删除空目录 `frontend/app/__u02a-test`、`frontend/app/u02a-test`（空目录不进入 Git，仅本地清理）。
- 删除 `frontend/components/agent/AgentChatWidget.tsx` 死代码（零挂载，Root Layout 挂载已移除；agent-workspace 测试仅断言 layout 不含该组件，不受影响）。
- 生产代码 `as unknown as` 类型契约清理两处：`search/page.tsx` 的 `handleSearch` 改为接收 `SearchAgentInput` 真实类型 `Record<string, unknown>[]` 并走 `normalizeContentList`（与 doSearch 路径一致）；`UserProfileClient.tsx` 讨论分支改为显式类型化映射 `toDiscussionCardData`（行为等价，保留卡片渲染字段）。
- `/recommend` 推荐流按 ui-spec 重构为 SWR `useSWRInfinite`（page=2,3... 追加）：SSR 首页数据经 ref 注入 fetcher 避免挂载重复请求；`initialSize: 0` + 空数组 fallbackData 保持 SSR 失败时错误态即时渲染；`revalidateFirstPage: false` 防止 setSize 时重取第 1 页；issue #73/#76 正文分别钉入迁移文件名 `064_ip_visit_history.sql` / `065_drop_legacy_favorites.sql`。
- `config.yaml` agent.llm_api_key 注释更新为指向已实现的 `AGENT_LLM_API_KEY` env 注入（config.go:500），doc-validator `--fix` 无漂移。
- `artifacts/ops-07`、`ops-08` 本地证据目录：核验确认无真实 staging/OSS/off-site 输入，不伪造证据，Ops-08 Step 5 保持 blocked（需人工介入）。

### Testing:
- Go：`go build ./...`、`go vet ./...`、`go test ./...` 全部通过。
- 前端：`npm run lint`（tsc --noEmit）、`npm run lint:ui`、`npm run test:unit`（227/227，含 RecommendFeedClient 4 用例，新增每用例独立 SWR 缓存避免跨用例污染）、`npm run build` 全部通过。
- 浏览器（ego-browser）：/recommend SSR 首屏 16 卡 + 无限滚动追加至 98 卡 + 共享 ContentDetailOverlay 打开/Escape 关闭；/search 关键词搜索返回 9 卡（handleSearch 修复路径）；/user/16 讨论 tab 空态与临时种子讨论卡片渲染（标题/作者/互动数，验证后已删除）；截图保存至 screenshots/（目录被 .gitignore 忽略）。

### Notes:
- artifacts 证据目录与 Ops-08 Step 5 保持 blocked；T09/T12 迁移文件尚未创建，issue 正文仅钉名待执行任务使用。

## [2026-08-07] - Ops-08 Step 5 解除阻塞并完成：真实 staging drill + 双目的地归档

### What was done:
- 用户批准宽松档 RPO/RTO（postgres_rpo 1440 / postgres_rto 30 / object_restore_rto 60 / service_rpo 0 / service_rto 30，单位分钟），批准记录 issue #77（GitHub API 可核验），`release/recovery-objectives.json` 从 baseline_only 切换为 approved。
- 真实恢复测量（原始记录含 SHA-256，绑定 candidate commit 0e13aa29）：PostgreSQL pg_dump 0.553s + pg_restore 1.199s（55 表全恢复，RTO 0.02min）；真实 OSS bucket omnicraft（versioning Enabled）版本化对象 上传→删除→按 versionId 移除删除标记恢复 0.081s（RTO 0.0014min，对象完整性验证后清理）；compose 服务恢复 0.603s（RTO 0.01min）。
- 从目标 commit（0e13aa29 / f01fc378）重建 candidate/previous 镜像（label 绑定 commit），重写 manifest 为真实可复算 digest（此前 ~/.config 中 manifest 的 digest 与本地镜像不一致，不可复核）。
- 真实 staging drill 跑通：preflight → deploy candidate（真实 compose、61 迁移、pg 备份、readiness、smoke）→ rollback previous → redeploy candidate → RPO/RTO 机器比较 all_met。证据 18 文件在 artifacts/ops-08/（backup 两份 .custom dump、history、durations、comparison 等）。
- 归档：`archive-release-evidence.sh` 18 对象 × 3 目的地（GitHub Release v0.1.0-rc.1 资产 + 本地镜像 + off-site OSS `oss://omnicraft-ops-archive/ops-evidence`，SSE-AES256 + retention 365d 元数据验证通过，receipt 生成）。
- 顺带修复真实模式才暴露的 4 个发布脚本 bug（含契约测试防回归）：staging-drill.sh bash3.2 空数组 unbound（DRILL_ARGS 改字符串）；3 处把 JSON 当 python 脚本执行；compose-release.sh `build: null` 被 Compose v5 拒绝改 `!reset null` + 本地 digest 镜像 pull 改 best-effort；archive-release-evidence.sh ossutil cp 加 -f 强制覆盖（旧对象元数据错误被覆盖修正）。用户补 RAM 权限 oss:GetObjectMeta + oss:GetObjectAcl 后 stat 验证通过。
- 验收门：preflight/deployment-contract/staging-drill 契约测试全绿；doc-validator --fix/--check --profile release 通过；`verify-project.sh --release` 全绿（26+227 测试、5 Playwright cross-stack e2e——新增 omnicraft_test 库并迁移 61 个迁移）；`validate-evidence.sh` summary 校验通过。

### Testing:
- Go/前端全套：verify-project.sh --release 通过（OMNICRAFT_TEST_DB_DSN 指向新建 omnicraft_test 库、OMNICRAFT_TEST_COMPOSE_ROOT 为仓库根）。
- 浏览器：drill 后 backend healthz（localhost:18080）与 frontend（localhost:13000）smoke 通过（脚本内 curl 验证）。
- 证据：artifacts/ops-08/summary.json 通过 ops-evidence.schema 校验。

### Notes:
- artifacts/ 目录被 .gitignore 忽略，本地留证 + GitHub Release/OSS 双目的地归档；drill 输入（env/manifest/measured）保留在 ~/.config/omnicraft/ops-08-staging/（含密钥，不入库）。
- 桌面范围（Ops-09）与 Web Agent Task 6 不属本轮，维持原阻塞。

## [2026-08-07] - 文档一致性修复：Ops-08 完成状态收口 + source-linkage 迁移重编号 061→066

### What was done:
- 全面审计（git 干净、doc-validator release 全绿、Ops-08 证据真实可复核）后修复 3 处文档与实际情况不符的问题：
  1. **Ops-08 完成状态收口**：AGENTS.md 活计划注册表移除 Ops-08 Step 5 行（已完成），暂缓表 Ops-09 行恢复条件更新为「Ops-00~Ops-08 已全部完成」；production-readiness 计划 Status 头更新，审查修正记录 blockquote 追加「Step 5 完成（2026-08-07）」条目；working 协调文档 1.1/1.2/Phase 0/B1 同步更新为完成态。
  2. **source-linkage 迁移重编号 `061`→`066`**：`061` 因 `062_notification_broadcast_idempotency.sql` 已先入库而不可回填（回填会造成新装库与既有库应用顺序分歧），`063`（collab）/`064`（IP history #64 T09）/`065`（favorites #64 T12）已钉名，取下一可用号 `066`。同步更新：AGENTS.md、source-linkage 计划（5 处）、collaboration-invites 计划（3 处）、community-features-design 规格（2 处）、deployment-contract.tests.sh 夹具（3 组 4 处）、working 协调文档（2 处）。
  3. **issue #77 收口**：追加完成注释并关闭（作为 Ops-08 Step 5 approved RPO/RTO 的 API 可核验证据长期保留，recovery-objectives.json 仍引用其 URL）。

### Testing:
- `bash scripts/release/deployment-contract.tests.sh`：全部通过（夹具迁移编号更新为 062→066 无回归）。
- `go run . --check`（tools/doc-validator，release profile）：All checks passed。
- 全库 grep：无残留 `061_add_source_fanwork` / `Step 5 remains blocked` 引用（archive 历史记录除外，属历史日志不修改）。

### Notes:
- 纯文档/测试夹具改动，无运行时代码变更；未勾选任何计划 checkbox；下一步仍按注册表执行 #64 Ticket 01（issue #65）。
- issue #77 关闭不影响证据有效性：GitHub issue 关闭后仍可访问，`release/recovery-objectives.json` 的 `approval.ref` 不受影响。

## [2026-08-08] - Ticket 06 (#70): 补齐 IP 讨论入口与关注状态反馈

### What was done:
- `FollowButton.tsx`：状态契约修复（ui-spec FollowButton 章节 / #64 决策 20 / 审计问题 10）——未关注 primary（default variant，`--primary` 实底），已关注 outline；已关注态 hover/focus 文本切换「取消关注」（group-hover/group-focus-visible 双 span），并加 destructive 边框/文字（`hover:border-destructive!`/`hover:text-destructive!` 防 outline variant 的 `hover:text-foreground` 按编译顺序覆盖）；互动受限（封禁/邮箱未验证/信誉分不足/不可用）时 disabled + title 展示 denial 原因（复用 `interactionDenialKey` 合同，与 ReactionBar/CommentSection 一致）；匿名点击跳 `/login` 不变。
- `DiscussionBoard.tsx`（#64 决策 19 / 审计问题 8）：compact 模式（IP 详情页）补「发起讨论」入口——有讨论时 header 入口，无讨论时空态 CTA（EmptyState action，避免双份入口）；入口仅对 `user && capabilities.can_interact` 的登录用户为可用链接；封禁/信誉分不足等受限状态为 disabled 按钮 + 可见 denial 文案（title + caption）；匿名为跳 `/login` 按钮（title「请登录后发起讨论」）；非 compact 全量模式入口与空态同步收口到 canStartDiscussion（不承诺不可用操作）。
- `zh.json`/`en.json`：新增 `social.unfollow`（取消关注/Unfollow）与 `discussion.loginToStart`（请登录后发起讨论/Log in to start a discussion），denial 文案复用既有 `capabilities.denied*`。
- 未新增后端接口，API 合同零变化；未触碰 backend/*（web/agent-t6 WIP 面）、Header/Sidebar/ContentDetailOverlay 等非 owner 文件。

### Testing:
- `npm run build` ✓、`npm run lint`（tsc --noEmit）✓。
- 浏览器实测（Playwright，前端 :3001 → 后端 :8090 独立实例 + 测试用户 t70-eligible/lowrep/banned@test.local，bcrypt 密码 TestPass#70）：20/20 通过——匿名（IP7 有讨论 header 入口、IP8 空态 CTA）点击均跳 /login；eligible 登录用户 IP7 header 入口 + IP8 空态 CTA 均为可用链接；关注按钮未关注 primary（rgb(79,70,229)=`--primary`）、点击后已关注 outline（rgb(255,255,255)）、hover/focus 可见文本变「取消关注」（含 a11y 名随 hover 切换）、Enter 取消关注还原；lowrep（reputation=1 < min_score_for_interaction=3）发起讨论与关注均 disabled + 「信誉分不足」反馈；banned 真实流（登录后 DB 置 ban + reload）= 会话终止回匿名（既有后端 hard-denial 合同，/auth/me 503 → 前端清会话），deniedBanned UI 分支经网络 mock /auth/me（can_interact=false + USER_BANNED）验证渲染 disabled 入口 + 封禁文案；暗色模式（html.dark + body rgb(13,17,23) 验证）。
- 截图 13 张存 `screenshots/t70-*.png`（明暗 × 桌面/移动 × 空态/受限态/关注各态，含 hover/focus 特写）。

### Notes:
- 与 #109 串行边界：仅改 DiscussionBoard 文案/入口面与 FollowButton，未动文本审核能力面，按 ownership matrix §6 预约边界保持清晰。
- 顺带发现（记录不修，backend 面外）：FollowIP 成功但 follow 通知插入 FK 失败（notifications.user_id=0，`follow_repo.go` 关联 creator 缺失）——后端缺陷，与 #70 UI 面无关，留待后端票据。
- issue #70 五条 AC 全部勾选并关闭；light 车道分批 commit 3 个。
## [2026-08-08] - Ticket 03 (#67): 验证浮窗共享元素转场原型

### What was done:
- 产出 throwaway 原型 `prototypes/overlay-transition/index.html`（自包含静态 HTML + vanilla JS，file:// 可用，零依赖）：FLIP 几何为核心（开 300ms / 关 240ms、共享缓动 cubic-bezier(0.22,0.61,0.36,1)）、View Transition API 渐进增强（命名唯一性换名、rAF 挂起规避、Abort 兜底）、居中 scale(0.96)+fade 降级（无 source/离屏/detached/display:none）、reduced-motion 100ms 纯透明度（原生 matchMedia + 强制开关双通道）、run token 中断恢复、滚动锁、focus 回归、执行日志面板。
- 自动化验证驱动 `prototypes/overlay-transition/verify-prototype.mjs`（playwright-core，主仓库 node_modules 只读回退），17/17 断言通过。
- 观察记录 `docs/working/2026-08-08-overlay-transition-prototype-observations.md`：各场景实测观察、兼容性（Firefox 无 VT 即真实 FLIP 环境）、9 条限制（VT 快照命名唯一性、VT 期间 rAF 挂起、getBoundingClientRect 时序、关闭必须重测 source、中断处理、reduced-motion 双通道等）、给 #68 的正式实现契约与复现步骤。

### Testing:
- `node prototypes/overlay-transition/verify-prototype.mjs`：17/17 PASS（FLIP 开/关中段变换断言、VT 路径开合、无 source/离屏/已卸载/无法测量四种降级、关闭时 source 离屏降级、reduced-motion 无 transform、连点中断恢复、focus 回归）。
- 截图 12 张落 `screenshots/overlay-transition-*.png`（FLIP/VT 中段与落定、降级中段/落定、关闭降级、reduced-motion、中断恢复、baseline）。
- 独立静态原型不参与 Next 构建面；未改动任何生产文件（矩阵 §6：#67 不共享生产文件）。

### Notes:
- 原型不替代 ui-spec/design-system 的数值权威（scale 0.96 等为原型实测值）；#68 接入时沿用共享 overlay hook 预约顺序（#67 → #68）。
## [2026-08-08] - Ticket 02 (#66): 统一导航壳、品牌入口与筛选状态

### What was done:
- Header（`frontend/components/layout/Header.tsx`）：内层 `max-w-7xl` → `max-w-[1280px]` + `px-4 md:px-6`（page-shell 宽度/gutter 契约，桌面 24px/移动 16px）；桌面 Logo 与移动抽屉内品牌入口 `href="/"` → `/recommend`（决策 1：推荐流为唯一品牌落点，二创区 `/` 保持分区语义）。
- Sidebar（`Sidebar.tsx`）：公开侧边栏背景 `bg-canvas-subtle` → `bg-canvas-default`，与相邻页面表面同一背景契约（决策 3）。
- 页面壳统一：`/`（HomePageClient）、`/original` 主容器 `max-w-[1440px]` → `max-w-[1280px]`；`/ips` 保留 1440px 已批准例外（P-01 方案 B）；`/recommend` 维持 1280px + `bg-canvas-subtle` 权威契约。
- 筛选选中态统一（决策 4）：`/`（HomePageClient 内容类型）、`/original`（分类 Tab）与 `/ips`（IP 分类）均采用 IP 库彩色药丸 `rounded-full border-accent-emphasis bg-accent-subtle text-accent-emphasis font-semibold` + `aria-pressed`（选中不只靠颜色）；/ips 顺带 `border-primary` → `border-accent-emphasis` 对齐。新建共享组件 `frontend/components/original/CategoryTabs.tsx`（客户端，`role=tab` + `aria-selected` + `aria-pressed`，保留滚动位置与 sort=recommended 归一化语义），`original/page.tsx` 内联 Tab 迁入。
- 修复 `frontend/e2e/helpers/mock-api-guard.ts`：`escapeRegex` 字符类缺失 `*`，导致 `**/api/v1/**` 等通配 pattern 编译非法正则（`Nothing to repeat`），mocked 契约全部在首个 API 请求即崩溃（自 6dea58b 引入，main 上 `--full` 梯队一直受影响）；补回 `*` 后 9 个依赖 `mockPublicApis` 的 spec 恢复可运行。
- 新增契约测试：`frontend/e2e/page-shell-contract.mock.spec.ts`（品牌入口桌面/移动路由、1280px 壳 + 24px gutter 几何、侧边栏与页面表面明暗双模式背景一致、三处 pill 选中态 CSS + aria-pressed、Tab 键盘 Enter 导航、375px 四路由无横向溢出）与 `frontend/e2e/t66-shell-screenshots.spec.ts`（截图证据生成器，`zh-CN` locale，桌面 light/dark + 移动）；`frontend/tests/navigation-shells.test.tsx` 增加源码级契约断言。

### Testing:
- `npm run build`（next build --webpack）通过；`npm run lint`（tsc --noEmit）通过；`npm run test:unit` 229/229 通过（含新增 navigation-shells 用例）。
- Playwright（dev 服务用 `next dev --webpack` 规避 Turbopack 在内存压力下的 CSS 编译损坏 flake）：page-shell-contract.mock.spec 5/5 + t66-shell-screenshots 3/3 全绿（多次重复运行稳定）；截图 7 张落盘 `screenshots/t66-*.png`（desktop light：home/original/ips/recommend；desktop dark：original；mobile：original + 品牌入口路由后 recommend），像素采样验证：light sidebar=body=#ffffff、dark sidebar=body=#0d1117、mobile original 工具栏命中 pill 色 #eef2ff、recommend 面 #f5f5f5（canvas-subtle 权威）。
- 后端真实服务（docker 18080）确认存活；真实数据截图方案因 CORS（allowed_origins 仅 `http://localhost:3000`，我无法改 backend）放弃，改用 mockPublicApis + `zh-CN` locale 生成证据。
- 环境注意：本机 16GB 内存长期高占用（opencode/Codex/ego-lite/多 worktree dev server 并发），Turbopack dev 偶发 CSS 产物损坏（`Parsing CSS source code failed`，字节级乱码，非本票代码引起——A/B 测试与全量还原后仍复现）；`next build`（webpack）与 `next dev --webpack` 均稳定。CI mocked 配置（plain `npm run dev`）遇 Turbopack flake 时建议清缓存重试。

### Notes:
- `zh.json`/`en.json` 无需新增键（复用 `nav.siteName`/`content.originalZone`/`home.*` 既有键）。
- 未触碰 #67/#70/#82/#83 文件面与 backend/；未改 config/routes/migrations，无需 doc-validator。
- 遗留（非本票范围）：mock-public-apis 修复后暴露的既有 spec 问题（如 messages-notifications 严格模式重复文本、admin/collections/history 首编译超时）为 6dea58b 守卫崩溃掩盖下的 pre-existing 问题，留待对应票处理。
## [2026-08-08] - GitHub issue #82（媒体体验 T02）：CollectionPicker 关闭行为与浮窗内通知

### What was done:
- 按 #65 更新的 ui-spec 权威修复 CollectionPicker：
  - 背板点击与 Esc 关闭（对齐 ConfirmModal 既有模式），弹窗内点击不关闭；busy/creating（添加/移除/创建请求在途）时背板、Esc、关闭按钮均阻止关闭。
  - 打开时保存触发元素焦点并落到关闭按钮，关闭后恢复触发焦点；Tab 焦点陷阱（首尾循环）复用 ConfirmModal 的 getFocusableElements 实现风格。
  - 加/移/创建成功与失败改为 footer 上方绝对定位浮动小条（pointer-events-none、半透明绿/玫瑰标签底、1800ms 显示 + 200ms 淡出 ≈ 2s、不引起布局跳动）；移除全局 useToast。
  - 行内「已添加」徽标保留为持久状态；i18n：zh/en `collections.picker.toast.*` → `collections.picker.notice.*`（added/removed/created 文案微调 + addFailed/removeFailed/createFailed 新增）。
- 测试（frontend/tests/collection-picker.test.tsx）：新增背板/Esc 关闭、busy 阻止关闭、notice 出现/淡出/失败路径、无全局 toast（源码断言）、焦点陷阱与恢复共 6 组用例；harness 由动态 import 改为静态 import（消除冷模块加载竞态），api mock 增加 deleteShouldFail。

### Testing:
- `npm run test:unit`：26 native + 233 typed 全绿（其中 collection-picker 22 用例 ×3 次稳定通过）。
- `npm run lint`（tsc --noEmit）、`npm run lint:ui`、`npm run build` 全绿。
- 浏览器实测（真实 backend + worktree 前端 :3001，测试账号 T82验证用户/156 + 4 收藏集）：27/27 断言通过——背板/Esc 关闭、弹窗内点击不关、busy 时背板/Esc/关闭钮全阻止（route 挂起模拟慢请求）、加成功/加失败/移除成功/内联创建成功 notice 均在弹窗内出现并约 2s 淡出、全程无全局 toast、「已添加」徽标在 notice 消失后保留、焦点打开落关闭钮/Tab 循环/关闭回触发钮；桌面 1280×800 与移动 390×844 截图共 7 张存于 screenshots/（community-collections-picker-*.png）。

### Notes:
- 测试环境发现 waitFor（@testing-library）在本仓库 node:test 环境下 interval/timeout 不触发（仅 MutationObserver 路径有效），淡出断言改用有界手动轮询（window.setTimeout）替代。
- 本票不改 config/routes/migrations，无需 doc-validator；未触碰 #66/#67/#70/#83 与 backend/* 文件面。

## [2026-08-11] - #81 原创区分类筛选修复（light，worktree media/t81）

### What was done:
- 前端：`frontend/lib/search-filters.ts` 新增 `resolveDefaultSort`（排序默认值语义：无分类且未显式选排序 → recommended；有分类 → hot；显式排序原样保留）；`frontend/app/(public)/original/page.tsx` 用它替换死代码表达式 `!search.category && search.sort === "recommended" ? "recommended" : (search.sort || "hot")`，并把 `search.sort` 默认从 "recommended" 改为 ""（不再混淆"未选择"与"recommended"）。CategoryTabs 接线本身已正确（点击写 ?category=<slug>、删显式 sort=recommended），未改动。SortSelect/#72 共享面未触碰。
- 后端：`backend/internal/service/content_service.go` `handleRecommended` 增加防御：`recommendedSortHasFilters` 检测 category/content_type(s)/tags/author_id/ip_id/source_original_id/time_range(非 all) 任一筛选条件时降级为 hot 并输出结构化 slog.Warn JSON（含各筛选字段）；无筛选 recommended 语义不变。
- 测试：handler seam（recommended 无筛选保留 + recommended+category 降级 hot 且仅含该分类）+ service 集成缝（结构化日志断言 + 无筛选不打日志 + 7 类筛选条件表驱动降级）。

### Testing:
- 后端 RED→GREEN：新测试先失败（total=3 混合分类泄露 / 无降级日志）后通过；`go build/vet/test ./...` 全绿。
- 前端：`npm run lint`（tsc）、`lint:ui`、`npm run test:unit`（282 tests）、`npm run build` 全绿。
- 浏览器（Playwright 1.60 + 日志代理 :8081 观察 SSR 请求）：/original 无分类 → `sort=recommended` 混合分类；点击 Film & TV → URL `?category=film_tv` + `sort=hot&category=film_tv` 且响应仅 film_tv；深链 `?category=film_tv&sort=recommended` → 后端降级、响应仅 film_tv；后端日志出现 JSON WARN "content list recommended sort degraded to hot"。截图 `screenshots/t81-original-{before-tab-click,after-tab-click,stale-deeplink}.png`。

### Notes:
- 二创首页（固定 sort=hot）与 /recommend 未改动；routes.go/config/migrations 未触碰，无需 doc-validator。已接管 :8080（停掉 #68 进程后启动本 worktree 后端）与 :3001 前端。测试期间暂用 NEXT_PUBLIC_API_URL=http://localhost:8081 指向日志代理，验证完成后恢复默认。
- 2026-08-11 收尾：TCC 桌面访问已重新授权。复核全量 `npm run test:unit` = 26 native + 256 typed 全绿（此前 12 fail 确认系 TCC EPERM 环境所致）；`npm run lint` / `lint:ui` / `build` 与 `go build/vet/test ./...` 复跑全绿；Playwright t81 spec 重跑通过（日志代理观测：无分类 sort=recommended 混合流 / 点 Tab sort=hot+category=film_tv 仅该分类 / 深链 sort=recommended+category=film_tv 后端降级仅该分类）。

## [2026-08-11] - #87 列表卡片自然比例瀑布流（light，worktree media/t87）

### What was done:
- `frontend/lib/content.ts`：`normalizeContentItem` 拾取 #83 合同 `cover_width`/`cover_height`（snake/Pascal 双兼容，经 positiveInteger 过滤非法值 → undefined 走默认比例）。仅扩展卡片归一化，未动 `AttachmentData`/`normalizeAttachment`（#85 划片）。
- `frontend/components/content/ContentCard.tsx`：
  - `ContentCardData` 新增 `cover_width?`/`cover_height?`；组件私有常量 `COVER_DEFAULT_ASPECT_RATIO="3 / 4"`、`COVER_EXTREME_RATIO_THRESHOLD=2`、`COVER_EXTREME_MAX_HEIGHT_PX=400`（不新建共享 lib）。
  - 封面容器改为数据驱动 `aspect-ratio`（image=媒体集首项 / video=poster 尺寸；NULL → 3:4），`object-contain` 不裁切（替代旧 video→16:9 / 其他→3:4 的 object-cover）；极端比例（max(w/h,h/w) > 2）内联 `max-height:400px` 限高 contain；新增 `data-slot="card-cover-aspect"` 供测试定位。原创/二创共用同一组件与规则（AC3）。
- i18n：无新增文案（纯 CSS/数据逻辑）。

### Testing:
- `npm run lint`（tsc）、`lint:ui`、`npm run build` 全绿。
- `npm run test:unit` 294 tests 全绿（新增 12 条：3:4 默认、数据驱动比例+object-contain 不裁切、极端横/竖限高 400px、2:1/1:2 边界不触发、video poster 比例、二创共用、键盘可聚焦、归一化 snake/Pascal/非法值/列表保持）。
- `npm run test:contracts` 27 passed（新增 t87-natural-ratio.mock.spec.ts 4 条：桌面 4 列自然比例+极端限高+无卡片溢出 / 移动 2 列+限高 / Tab 键盘聚焦卡片 / 搜索页二创+原创同源卡片）。
- 浏览器截图 4 张：`screenshots/t87-original-mixed-ratio-desktop.png`、`t87-original-mixed-ratio-mobile.png`、`t87-original-keyboard-focus.png`、`t87-search-mixed-zones-shared-card.png`（内容经像素抽样验证非空白）。

### Notes:
- 移动端 390px 视口存在既有 26px 页面级水平溢出（原创区页头描述行 + 分类 Tab 行，未涉及卡片）；已用 stash 对照 main 复现确认与本票无关，溢出断言按 AC 范围收敛到卡片区。
- 未触碰 config/routes/migrations（无需 doc-validator）；未触碰 #85 的 ContentDetail/MediaGallery/overlay 组件与主 worktree。progress.txt 只更新本 worktree 副本。

## [2026-08-11] - #88 桌面双栏详情浮层（light，worktree media/t88）

### What was done:
- `frontend/components/content/ContentDetail.tsx`：新增 `mediaSlot?: "inline" | "split"` 与 `coverReady?: boolean`。split 模式下行内媒体区包 `min-[1100px]:hidden`（<1100px 单列兜底保留，display:none 不加载图、不触发落定事件）；正文 reveal 判定扩展为「行内落定 || 行外 coverReady 落定」（任一路径 settled 即显示，错误态同 reveal，符合 ui-spec:2411）。
- `frontend/components/content/ContentDetailOverlayLayer.tsx`：数据落定后按「content_type ∈ image/video 且媒体集非空」判定 split-media 并回报 `onLayoutChange(index, layout)`。split 布局 = `min-[1100px]:grid-cols-[3fr_2fr]`：左媒体列（`hidden min-[1100px]:grid place-items-center`，aspect 盒 `height:100%` + `aspect-ratio` 首项比例 + `max-width:100%`，有翻页控件时 `max-height: calc(100% - 64px)` 预留控件位；宽度按比例自适应、列内居中 contain 不裁切，MediaGallery 不改组件仅包容器）；右信息列 = `data-slot="layer-scroller"`，`min-[1100px]:overflow-y-auto` 独立滚动，ContentDetail(mediaSlot="split", coverReady) + 创作者栏/相关列表下置于信息列末尾（维持 ui-spec:2402 相关推荐与 :2438 创作者栏，ContentSidebar 组件本身未改）。单列布局与侧栏不变。
- `frontend/components/content/ContentDetailOverlay.tsx`：
  - 新增 `layerLayouts` state（每层回报布局）+ `resolveActiveScroller()`：顶层 split-media 且视口 ≥1100px（`matchMedia("(min-width: 1100px)")`，jsdom 无 matchMedia 时安全降级）时唯一滚动目标 = 顶层可见 `layer-scroller`（跳过 display:none 底层），否则 overlay-scroller；pushLayer 滚动捕获与 topKey 恢复均走该目标（焦点/滚动按层记忆不变）。
  - overlay-scroller 在 split-media 顶层时加 `min-[1100px]:h-full min-[1100px]:overflow-hidden`（不滚动，ui-spec:2447 唯一滚动目标）。
  - `getTopCover` 改为跳过 display:none 的 detail-cover（split 模式下行内媒体区与左栏媒体列共存，取可见者，FLIP/VT 锚点仍正确）。
- i18n：无新增文案（布局纯 CSS/逻辑，未触碰 messages）。ContentSidebar/Host/use-content-detail-overlay 未改（Host 全页维持单栏，ui-spec:2395 双栏定义于浮层章节）。

### Testing:
- `npm run lint`（tsc）、`lint:ui`、`npm run build` 全绿；`npm run test:unit` 317 tests 全绿（新增 `tests/content-detail-split-layout.test.tsx` 4 条：媒体集内容出 split 布局（layer-scroller + 双 detail-cover 锚点 + aspect 盒 1600/900 比例）、split 滚动记忆路由到 layer-scroller 并在 pop 后恢复（matchMedia stub ≥1100px）、历史无媒体集 image 内容保持单列、源码契约断言）。
- `bash scripts/verify-project.sh -Scope Frontend`：default verification passed。
- 浏览器（Playwright + worktree :3001 dev + 共享 :8080 后端，内容 30「15 分钟营养早餐碗」3:4 防御比例 + 2 图控件位）：
  - 1440×900：`screenshots/t88-desktop-dual.png` —— 双栏几何 580×773（3:4 比例、高度达上限 773+64=837≈视口可用），信息列独立滚动（scrollTop 400，媒体区 top 恒定 148，overlay-scroller 0）；`t88-desktop-dual-scrolled.png` 滚动后；Esc 关闭、0 pageerror。
  - 900×900：`t88-tablet-single.png` —— 单列全宽 contain（868 宽），无溢出。
  - 390×844：`t88-mobile-single.png` —— 全屏单列（382 宽），信息下滚不回归。
  - 平板 701-1100 判定：≤1100 保持单列（含 1024-1100 现 lg 布局），>1100 才双栏——PR 已写明。
- 桌面 FLIP 锚点：visibleCovers=1（隐藏行内 cover 被跳过，取左栏媒体列）。

### Notes:
- 双栏仅作用于 image/video 且媒体集非空的内容；历史 image/video 无媒体集（行内 CoverImage）维持单栏。
- 端口：:3001 为 t88 worktree 前端 dev（:3000 属 t85 worktree）；后端 :8080 共享。截图所需 `/seed-media/real/gallery/*.svg` 系 t85 worktree 的本地未提交素材，已复制到本 worktree 供验证，**不随本 PR 提交**（与 #85 一样未入库）。
- 未触碰 config/routes/migrations（无需 doc-validator）；未触碰 MediaGallery/ContentCard/MasonryGrid/主 worktree；未 close issue。

## [2026-08-11] - Ticket #89（light）：移动端连续浏览（上下文列表+上滑前进）

### What was done:
- **入口贯通（AC1）**：`use-content-detail-overlay.tsx` 把 `contextList`/`contextIndex` 从 hook options 移到每次 `open()` 的 `OverlayOpenEntry`（一次 hook 实例可开不同上下文），贯通所有网格入口：`OverlayMasonryGrid.tsx`（分区页/IP 页共用）与 `RecommendFeedClient.tsx`（推荐流）在 `openDetail` 时按网格顺序传入 `contextList` + 当前卡索引；浮层内关联内容/Agent 引用等非网格入口不传（不参与连续浏览）。`ContentDetailOverlay` 首层初始化把 context 字段带进 `OverlayEntry`。
- **上滑切篇（AC2）**：`ContentDetailOverlayLayer` 新增 `handleReachEnd`（仅 `isMobileViewport()` <1100px 时生效；无 contextList 或索引越界早退）→ 请求 `onSwitchNext`；`ContentDetailOverlay.switchTopLayer` 原地替换顶层 entry（不压栈、不写 history、栈深不变），`key` 含 contentId 驱动层 remount——媒体区（viewerIndex/加载态/指示点）与信息区（滚动位置、coverSync 状态）全部重置为新内容初始态，顶层 scrollTop 置 0。接线：`MediaGallery.onReachEnd`（#85 已有钩子，仅接线；查看器不参与，未改 `MediaViewer.tsx`）→ 行内画廊与 #88 左栏媒体列两条路径都接同一 handler。上滑判定只在媒体集最后一项（MediaGallery 内已按 #85 实现），与集内翻页/页面滚动无冲突。
- **到底提示（AC3）**：`atContextEnd`（contextIndex ≥ length-1）→ `ContentDetail` 新增 `galleryEndHint` prop，媒体区下方渲染 `media.continue.endReached`（en「You've reached the end」/zh「已经到底了」），随行内媒体区在桌面双栏下隐藏；到底后 `handleReachEnd` 早退不切篇。
- **桌面不回归（AC4/AC5）**：`isMobileViewport` 与 #88 双栏判定同一 1100px 断点；桌面左栏媒体列上滑不切篇（handler 早退）；浮层关闭恢复滚动/焦点走既有 `restoreRef`/`finalizeClose` 契约（未改动）；`resolveActiveScroller`/#88 滚动模型未破坏。
- **ContentDetail.tsx 改动理由**：移动端可见媒体区是行内画廊（split 左栏 <1100px 隐藏），连续浏览钩子与到底提示必须接在行内路径上（`onGalleryReachEnd`/`galleryEndHint` 两个新 prop + 提示块）；未触碰双栏几何逻辑。

### Testing:
- `npm run lint` / `npm run lint:ui` / `npm run build` 全绿；`npm run test:unit` 344/344（新增 `tests/content-detail-continuous-browse.test.tsx` 5 条：AC1 入口贯通、AC2 切篇+媒体/滚动/加载态重置+不重复取数、AC3 到底提示且不切篇、AC4 桌面不触发、源码契约）；`npm run test:contracts` 27/27。
- 浏览器实测（Playwright，mock API :18080 + worktree 前端 :3002，zh）：12/12 断言——390×844 移动：打开单列浮层、滑到末媒体、上滑切篇（标题/媒体区重置/信息区滚动回 0）、连续切篇、列表最后一项上滑显示「已经到底了」且不再切换、Esc 关闭后焦点回到触发卡片；1280×800 桌面：双栏浮层可见、左栏媒体列上滑不切篇。截图 8 张 `screenshots/t89-*.png`（t89-mobile-open/last-media/switched/switched-2/end-hint/restored + t89-desktop-dual/no-switch）。
- 验证环境：真实 Go 后端以 `SERVER_PORT=8091` 起（8080 被 health-agent 的 Spring Boot 占用，未杀他人进程）+ 确定性 mock（3 篇 image×2 媒体，CORS 回显 origin）；验证脚本与 mock 在 /tmp 未入库。

### Notes:
- 验证中发现切篇后若合成触摸仍有惯性滚动残留，顶层 `topKey` effect 会先重置 0 再被浏览器惯性补滚——真实手势（touchEnd 即抬指）无此问题，实测在 touchEnd 前留 80ms 停顿后稳定回 0；产品切篇重置逻辑本身正确（单元测试覆盖 scrollTop=80→切篇→0）。
- 契约测试套件会再生成 `screenshots/community-content-series-*.png`（渲染不确定），已 restore 不纳入本票；tsconfig.json 的 `.next-playwright-t89` 条目为临时 dev 自动追加，已还原并删除临时 dist。
- 未触碰 config/routes/migrations（无需 doc-validator）；未触碰 `MediaViewer.tsx`；未触碰主 worktree。

## [2026-08-11] - Task 1（heavy）：source-linkage 064 迁移与模型字段（#96）

### What was done:
- **Step 1**: `ls backend/migrations/ | sort | tail -10` 确认 064 未被占用（061 空置、062/063 已用），无需换号。
- **Step 2（TDD 红）**: `backend/internal/model/content_migration_test.go` 新增 `TestContentSourceMigration`（复用同包 `requireContentItemsBaseTable` / `assertSeriesForeignKey` / `indexDefinition` 先例），断言：`source_fanwork_id` 为 bigint nullable、FK 引用 `content_items(id)` 且 `ON DELETE SET NULL`、`idx_content_items_source_fanwork` 存在且为 partial（`source_fanwork_id IS NOT NULL`）。迁移文件应用两次验证幂等（沿用 063 先例）。失败证据：
  ```
  content_migration_test.go:45: read migration ../../migrations/064_add_source_fanwork_id.sql: open ../../migrations/064_add_source_fanwork_id.sql: no such file or directory
  --- FAIL: TestContentSourceMigration
  ```
- **Step 3（绿）**: 新建 `backend/migrations/064_add_source_fanwork_id.sql`，DDL 与计划一致（ADD COLUMN IF NOT EXISTS + FK ON DELETE SET NULL；partial index `(source_fanwork_id, status, created_at DESC)`），含 `-- ROLLBACK:` 注释块（本地测试库专用，DROP INDEX + DROP COLUMN，共享环境优先 forward fix）。036/063 风格，063 同款 forward-only 头注释。
- **Step 4**: `ContentItem` 新增 `SourceFanworkID *int64`（`gorm:"index"`）与 `SourceFanwork *ContentItem`（`gorm:"foreignKey:SourceFanworkID"`），JSON 字段名与计划一致，对齐既有 `SourceOriginalID/SourceOriginal` 写法。
- 未触碰 config/routes；`go test ./...` 无 schema 快照报错，故按计划不跑 doc-validator（Task 8 统一处理）。未触碰 main worktree/frontend。

### Testing:
- 红→绿：`go test ./internal/model -run '^TestContentSourceMigration$' -v` FAIL（迁移不存在）→ 实现后 PASS。
- `go test ./internal/model -run TestContentSourceMigration -v`：两个匹配用例均 PASS。
- 全量 `go build ./... && go vet ./... && go test ./...`：全部 ok，无 FAIL。
- `gofmt -l` 干净（改动的 2 个文件均未列出）。

### Notes:
- 单一 commit（heavy 纪律），PR 标题含「Closes #96（Task 1）」；不 close issue #96（Task 8 收口）。改 progress.txt 仅限本 worktree 副本。

## [2026-08-11] - Task 2（light）：source-linkage 显式来源校验错误码（#96）

### What was done:
- **Step 1（TDD 红）**: `backend/internal/service/content_source_test.go` 以 `TestValidateSource` 表驱动测试替换旧的 `TestValidateSourceOriginalLink`（旧用例「fanwork 无来源可发布」与新业务规则 FANWORK_SOURCE_REQUIRED 直接冲突，属计划明确替换范围），9 用例与计划一一对应：zone=original + source_original_id / + source_fanwork_id → `ErrSourceNotAllowedForOriginal`；fanwork 无 ip/source → `ErrFanworkSourceRequired`；双内容来源 → `ErrMultipleSourceConflict`；来源原创非 original/published → `ErrSourceOriginalUnavailable`；来源二创非 fanwork/published → `ErrSourceFanworkUnavailable`；IP-only / 原创来源 / 二创来源三成功路径。失败证据：
  ```
  internal/service/content_source_test.go:26:20: undefined: ErrSourceNotAllowedForOriginal
  ...
  internal/service/content_source_test.go:77:11: undefined: validateSourceLink
  FAIL omnicraft/backend/internal/service [build failed]
  ```
- **Step 2**: `go test ./internal/service -run TestValidateSource -v` 确认红（上述 build failed）。
- **Step 3**: `PublishContentInput` 新增 `SourceFanworkID *int64 json:"source_fanwork_id"`（紧邻 `SourceOriginalID`）。
- **Step 4**: 单一创建期校验 helper `validateSourceLink(zone, ipID, sourceOriginal, sourceFanwork)`：zone 非 fanwork 携带任一来源 → SOURCE_NOT_ALLOWED_FOR_ORIGINAL；fanwork 三空 → FANWORK_SOURCE_REQUIRED；双来源 → MULTIPLE_SOURCE_CONFLICT；按 zone+status 校验两个来源的可用性（顺序严格按 spec §发布时校验规则）。删除旧 `validateSourceOriginalLink` 与 `ErrInvalidSourceOriginal`。来源加载逻辑改为：任一来源 ID 存在即 `FindByID`（zone=original 也加载，保证 helper 看到来源并优先返回 NOT_ALLOWED_FOR_ORIGINAL；不存在 → 对应 UNAVAILABLE）。不做递归 CTE（创建期来源必为已存在内容，无环）。
- **PATCH SOURCE_IMMUTABLE（handler 配套改动，见 Notes）**: `UpdateContent` 请求体结构新增 `ip_id`/`source_original_id`/`source_fanwork_id` 探测字段，任一非 nil 立即返回 `400 SOURCE_IMMUTABLE`（`ErrSourceImmutable`），不再静默忽略；无来源字段的既有更新路径不受影响。
- **Step 5**: 创建时持久化 `ContentItem.SourceFanworkID = input.SourceFanworkID`。
- **Step 6**: `go test ./internal/service -run TestValidateSource -v` 转绿（9/9 PASS）。

### Testing:
- 红→绿：`go test ./internal/service -run TestValidateSource -v` build failed（未定义符号）→ 实现后 9/9 PASS。
- 全量 `go build ./... && go vet ./... && go test ./...`：全部 ok，无 FAIL。
- `gofmt -l` 干净（本任务改动的 5 个文件均未列出；仓库既有未格式化文件 ip.go/version.go 等为存量问题，未触碰）。

### Notes:
- handler 配套改动（计划 Task 2 Files 清单之外的 3 个文件，按执行指令「如 handler 需要配套改动请一并完成并在报告中说明」）：`handler/content.go`（CreateContent 旧 `INVALID_SOURCE_ORIGINAL` 映射替换为 5 个新精确码 400 映射 + UpdateContent SOURCE_IMMUTABLE 探测）、`handler/content_publish_route_test.go`（`TestCreateContentRouteRejectsInvalidSourceOriginal` 改名 `TestCreateContentRouteRejectsInvalidSourceLink`，3 用例断言改为各自精确码：original+source → SOURCE_NOT_ALLOWED_FOR_ORIGINAL、fanwork+非原创/未发布来源 → SOURCE_ORIGINAL_UNAVAILABLE）、`pkg/response/safe_error.go`（删除 `INVALID_SOURCE_ORIGINAL`，新增 6 个新码 safeMessages，供 SafeErrorResponse 消息映射）。理由：服务语义变更后全量 `go test ./...` 必须保持绿，旧码已无生产路径。
- 已知边界（报告中说明）：fanwork 同时携带两个来源 ID 且其一不存在时，加载阶段先返回 UNAVAILABLE 而非 CONFLICT（helper 内部 CONFLICT 优先仅对已加载来源成立）；两个 ID 均存在时按 spec 顺序返回 MULTIPLE_SOURCE_CONFLICT。Task 3 路由测试覆盖真实路径。
- 未触碰 config/routes/migrations（无需 doc-validator）；未触碰 main worktree/frontend；progress.txt 只更新本 worktree 副本。

## [2026-08-11] - Task 3（light）：发布与内容详情契约（#96）

### What was done:
- **Step 1（TDD 红）**: `backend/internal/handler/content_publish_route_test.go` 扩展 7 项断言：① 新测试 `TestCreateContentRoutePublishesFanworkWithSourceFanwork`——fanwork 仅带 `source_fanwork_id` 可发布（201 + 返回 source_fanwork_id），且 `GET /contents/:id` 可见时返回 `source_fanwork: {id, title, zone:"fanwork"}`；② 既有 `TestCreateContentRoutePublishesFanworkAndReadsBackSourceRelation` GET 断言补 `zone:"original"`；③ `TestCreateContentRouteRejectsInvalidSourceLink` 新增 3 用例：original+source_fanwork_id → SOURCE_NOT_ALLOWED_FOR_ORIGINAL、fanwork 无 IP/来源 → FANWORK_SOURCE_REQUIRED、双来源 → MULTIPLE_SOURCE_CONFLICT（Task 2 已实现，直接验证端到端）；④ 新测试 `TestUpdateContentRouteRejectsSourceImmutable`——PATCH ip_id/source_original_id/source_fanwork_id 各返回 400 SOURCE_IMMUTABLE 且 DB 中已存归因（source_fanwork_id 原值）保留、其余来源字段仍为 nil。测试 router 补 `PATCH /api/v1/contents/:id`（authReq + handler.UpdateContent）。
- **Step 2**: `go test ./internal/handler -run "TestCreateContentRoute|TestUpdateContent.*SourceImmutable" -v` 红：`source_original zone = ""`（缺 zone）与 `source_fanwork = {ID:0,...}`（摘要缺失）。`TestUpdateContentRouteRejectsSourceImmutable` 因 Task 2 已实现探测而直接绿（行为本已正确，测试补契约）。注意：PATCH 测试命名按计划 Step 2 正则 `TestUpdateContent.*SourceImmutable` 调整。
- **Step 3**: 核对 6 个精确码映射（`handler/content.go`）：SOURCE_NOT_ALLOWED_FOR_ORIGINAL/FANWORK_SOURCE_REQUIRED/MULTIPLE_SOURCE_CONFLICT/SOURCE_ORIGINAL_UNAVAILABLE/SOURCE_FANWORK_UNAVAILABLE 均已映射 400（:209-228，Task 2 完成），SOURCE_IMMUTABLE 更新时 400（:420-423）。全部就位，无需改动。
- **Step 4**: `GetContent` 详情路径：新增 `source_fanwork` 摘要 `{id, title, zone:"fanwork"}`（`SourceFanworkID` 非空且来源可取时）；`source_original` 摘要补 `zone:"original"`。来源不可取时省略摘要，`source_fanwork_id` 由 content 对象 json tag 自动保留（前端渲染「内容已下架」）。
- **Step 5**: `frontend/lib/content.ts`：`ContentDetailData` 加 `source_fanwork_id`；`normalizeContentItem` 归一化 `source_fanwork_id`；`normalizeSourceSummary` 要求 id/title/zone 三字段齐备才产出 `SourceSummary`（`zone: "original" | "fanwork"`），缺一即省略——无有效三字段摘要不得成为可点击卡片（ui-spec SourceAttribution 契约），`source_*_id` 单独存在时前端仍可渲染下架文案。
- **Step 6**: `go test ./internal/handler -run TestCreateContentRoute -v` 转绿；`TestUpdateContent.*SourceImmutable` 亦绿。

### Testing:
- 红→绿：`content_publish_route_test.go:94: source_original zone = "", want original`、`:159: source_fanwork = {ID:0, Title:"", Zone:""}, want id=1 title="Published fanwork source"` → 实现后 7 用例全 PASS。
- 全量 `go build ./... && go vet ./... && go test ./...` 全绿（handler/model/service 等全 ok）。
- `gofmt -l` 仅列出存量未格式化文件（admin_audit.go/config_public.go/feedback.go/version.go，未触碰）；本任务 3 个文件均干净。
- 前端：`npm run lint`（tsc --noEmit）绿、`npm run build` 绿、`npm run test:unit` 344/344（含 content-series-normalizer.test.ts）。
- 未改 config/routes/migrations → 无需 doc-validator。

### Notes:
- 中途误用 `go fmt` 顺带格式化 5 个存量未格式化文件（ip.go/version.go/admin_audit_service_test.go/category_service.go/verification_service.go），已 `git checkout` 还原，未混入提交。
- 未触碰 main worktree；progress.txt 仅更新本 worktree 副本。

## [2026-08-12] - Task 4（light）：增强 Related Fanworks API（#96）

### What was done:
- **Step 1（TDD 红）**: 新建 `backend/internal/handler/content_related_fanworks_route_test.go`，11 个用例覆盖计划 10 项测试清单 + 1 项来源可见性：original 源按 `source_original_id` 返回（并断言 `source_content_id`/`source_zone` 新响应合同）；fanwork 源按 `source_fanwork_id` 返回衍生作品（不得再 400 NOT_ORIGINAL，且按 `source_original_id` 指向 fanwork 的行不得泄漏）；非可见子项排除（pending/under_review/软删/作者 banned/is_public=false 全部排除，仅 published+public 可见）；`content_type` 单值与逗号多值 allowlist IN；非法/空条目（`bogus`/`article,`/`,`/`article,,prompt`/`text`）→ 400 INVALID_CONTENT_TYPE；默认 sort 按 hot_score 降序；`sort=new` 按 created_at 降序；`limit` 别名 page_size；`page_size` 优先于 `limit`；软删来源 → 404 NOT_FOUND。测试 seed 用显式 UPDATE 落 `is_public=false`（`default:true` 标签使 GORM 零值 false 被 INSERT 省略，初版实测 is_public 恒 true 已修复）。
- **Step 2**: `go test ./internal/handler -run TestListRelatedFanworks -v` 红（5 FAIL/4 PASS，另 2 项来源可见性/页大小语义本就绿）：
  ```
  --- FAIL: TestListRelatedFanworksFanworkReturnsDerivativesBySourceFanworkID
      status = 400, want 200 ... body = {"code":"NOT_ORIGINAL",...}
  --- FAIL: TestListRelatedFanworksExcludesNonVisibleChildren
      total = 5, want 1 ...（banned 作者/private/pending 子项全泄漏）
  --- FAIL: TestListRelatedFanworksRejectsInvalidContentType
      content_type=bogus: status = 200, want 400
  --- FAIL: TestListRelatedFanworksSortHotIsDefault
      default sort = [low-hot high-hot mid-hot], want hot order
  --- FAIL: TestListRelatedFanworksLimitAliasesPageSize
      page_size = 20, want 1（limit 别名未解析）
  ```
- **Step 3**: `ListContentsFilter` 新增 `SourceFanworkID *int64` 与 `ViewerID int64`；`ListContents` 在携带任一来源过滤时**复用集中可见性 scope**（`ApplyContentVisibilityScope(q, f.ViewerID)`：published + 非软删 + 作者非 banned/删 + IP 非 banned + is_public 或作者本人），不复制局部 `status='published'` 谓词；无来源过滤的既有路径（含 admin under_review 显式 Status、user/IP 列表）行为不变。`ContentService`：hot Redis 路径守卫补 `SourceOriginalID==nil && SourceFanworkID==nil`（否则 sort=hot 默认下 related-fanworks 会命中全局热度 rank 泄漏跨源内容）；`recommendedSortHasFilters` 补 SourceFanworkID。新增 `GetVisibleContent(id, viewerID)`——经集中 scope 的单行解析（显式绕过详情缓存，防可见性经缓存泄漏），供来源查找。
- **Step 4**: `ListRelatedFanworks` 重写：来源查找改用 `GetVisibleContent`（zone=original → SourceOriginalID；zone=fanwork → SourceFanworkID；其余 zone 返回空表，模型约束下不可达）；**删除旧 NOT_ORIGINAL 400**；`content_type` 逗号解析走 allowlist（image/article/video/audio/template/sheet_music/mod/prompt/other，与 browse-history 同枚举）参数化 `IN`，非法/空条目 400 `INVALID_CONTENT_TYPE`；sort 默认 `hot`、`new`/`newest` → 最新、`most_views`/`best_rated` 透传、未知回退最新；page_size 默认 24 上限 100，`limit` 为别名且 page_size 优先（复用 `parsePositiveInt`）；响应对齐 `docs/reference/api.md` 快照合同：`source_content_id` + `source_zone`（原 `source_original_id` 键对 fanwork 源语义错误，已按 api.md 收敛；前端仅消费 contents/total 不受影响）。
- **Step 5**: `go test ./internal/handler -run TestListRelatedFanworks -v` 11/11 全绿。

### Testing:
- 红→绿证据见上；`go build ./... && go vet ./... && go test ./...` 全量全绿（handler/service/repository 等全 ok）。
- `gofmt -l`：本任务 4 个文件均干净（仓库存量未格式化文件 ip.go/version.go 等未触碰）。
- 未改 config/routes/migrations → 无需 doc-validator。

### Notes:
- 已知前端协调点（本任务后端先行，不碰 frontend）：`frontend/app/(public)/original/[contentId]/fanworks/page.tsx` 的 TYPE_FILTERS 仍发送 `text,article,prompt`（`text` 不在标准枚举内），严格 allowlist 生效后该页「All/Text」过滤将 400 回退空列表；计划后续前端任务已列入修改该页（`frontend/app/(public)/original/[contentId]/fanworks/page.tsx` 在计划文件清单），届时须将 `text` 从过滤值中去掉。
- 未触碰 main worktree；progress.txt 仅更新本 worktree 副本。

## [2026-08-12] - Source-linkage Task 5（light）：确认发布 UI 规格并实现 SourceContentPicker（#96）

### What was done:
- **Step 1 ui-spec 确认**：`design/ui-spec.md` 存在 `## Component: SourceContentPicker`（L4828）与 `## Page: /studio/publish/fanwork`（L5034）章节，满足全部要求——loading/empty/validation/error/disabled/prefilled 状态变体、a11y（label+combobox/listbox、aria-activedescendant、44px 触控）、i18n namespace `sourceContentPicker.*`、query prefill（`?source_original_id` / `?source_fanwork_id`）、Playwright 截图检查点（`screenshots/community-source-picker-{desktop,mobile}.png`）；且明确 `SourceContentPicker` 拥有灵感来源选择、`CollabUserPicker` 留给后续 collaboration-invites 计划（L5040/5096）。无缺失，无需记录 issue。
- **`frontend/components/studio/SourceContentPicker.tsx`（新）**：按计划原文 props（`sourceKind/selected/disabled/onSelect`）。搜索调用 `GET /api/v1/contents/search?zone=<sourceKind>&q=<query>&limit=8`（URLSearchParams 顺序 zone→q→limit），300ms debounce；**防御性可选题过滤**（`isSelectableSource`）：要求数字 id>0、非空 title、`zone === sourceKind`、无 `deleted_at`、`status` 为 published 或缺失、作者无 `deleted_at`/`is_banned`——banned/审核中/软删/作者删除行永不渲染；结果归一为 `{id,title,zone}` 摘要再 emit。combobox/listbox 语义（label+htmlFor、`aria-expanded`/`aria-controls`/`aria-activedescendant`、option `aria-selected`）、ArrowDown/Up 高亮 + Enter 选择 + Esc 关闭、loading（Spinner+文案）、empty、error（内联错误+重试按钮，不清除已选）、已选 summary row + 清除按钮（`onSelect(undefined)`）、disabled 态禁用输入与清除。
- **`SourceOriginalPicker.tsx` 迁移为兼容包装**：保留旧 props 接口（value/onChange/placeholder/searchLabel/loadingLabel），内部渲染 `<SourceContentPicker sourceKind="original">` 并做 `{id,title}` ↔ `{id,title,zone}` 映射；PublishForm.tsx 无需改动仍可编译（Step 4 要求改 PublishForm 前测试先绿，表单集成属 Task 6）。
- **i18n**：新增顶层 `sourceContentPicker.*` namespace（original/fanwork label+placeholder、search.loading/empty、error.searchFailed/retry、selected.label/clear、a11y.results*）zh/en 同步（ui-governance i18n parity 通过）。
- **e2e mock 修正**：`studio-publish-fanwork.spec.ts` 两处 contents/search mock items 补 `zone`/`status`（严格 zone 过滤下旧 mock 行不渲染）；新增 `e2e/t5-source-picker.mock.spec.ts`（mocked config 契约）实测搜索/过滤/选中/清除/错误/空态并截图。
- 未改后端/config/routes/migrations（无需 doc-validator）；page.tsx 无 Task 5 改动（query prefill 与来源区集成属 Task 6）。

### Testing:
- 红→绿：9 条 focused 测试先以 `ERR_MODULE_NOT_FOUND` 红，实现后 `node --import tsx --test tests/studio-publish-fanwork.test.tsx` 9/9 绿（original/fanwork URL 合同精确断言 `zone=original|fanwork&q=<query>&limit=8`、匿名与登录子集一致、banned/审核中/软删/作者删除/封禁作者不渲染、id/title/zone 校验、选中 emit 摘要、清除 emit undefined、loading/empty/error 本地化文案、disabled）。
- `npm run lint`（tsc strict）、`npm run lint:ui`（i18n parity 等全 ok）、`npm run build`、`npm run test:unit` 353/353 全绿。
- 浏览器实测（Playwright mocked config，webServer 自动起 dev :3001）：t5-source-picker.mock.spec.ts 通过——默认态、搜索出结果且 Banned Draft/Under Review Draft 不可见、选中出 summary+清除、清除回归、500 错误态+Retry、空态。截图 6 张 `screenshots/t5-publish-source-picker-{default,results,selected,cleared,error,empty}.png`。
- `npm run test:contracts`：16 过 / 4 失败 + 8 未跑——4 个失败（content-series/contract-smoke/page-shell/t87-natural-ratio）在 pristine origin/main 上以相同集合复现（pre-existing，非本任务引入；对应注册表已知 #101 load-gate 与 #66/#68 共享文件批次）。

### Notes:
- 关键实现决策：结果行按 ui-spec「id+title+zone 匹配」做严格 zone 过滤；@testing-library/dom 10.4.1 的 `fireEvent.click` 不派发 mousedown，option 选中改挂 `onClick`（mousedown 仅 preventDefault 保焦点，键盘 Enter 路径独立覆盖）；测试 harness 需像真实父级一样 lift selection state 才会出现 summary row。
- 未触碰 main worktree；progress.txt 仅更新本 worktree 副本。

## [2026-08-12] - Source-linkage Task 6（light）：更新 fanwork 发布流（#96）

### What was done:
- **Step 1 失败测试（10 项 + 1 子项）**：`frontend/tests/studio-publish-fanwork.test.tsx` 追加页面级集成测试——仅 IP 发布（payload 含 ip_id 无来源 id）、仅原创来源发布、仅二创来源发布、无 IP/来源提交在 API 调用前被拦截（disabled 提交钮 + 本地化校验文案）、选原创来源清二创来源、选二创来源清原创来源、`?source_original_id=77` 预填加载摘要、`?source_fanwork_id=88` 预填加载摘要、双 ID 预填保留 source_original_id 清 source_fanwork_id 并显示本地化警告（断言只 fetch `/api/v1/contents/77`）、非法预填 id 显示本地化非阻塞警告且 picker 为空（断言 0 次 API 调用）+ 预填来源加载失败显示可关闭内联错误（404 子项）。红→绿证据：实现前 11 新测试全红（9 旧 picker 测试绿），实现后 20/20 全绿。测试需 stub `next/navigation`/`AuthContext`/`MarkdownEditor`/`.css`（沿用 original-category-tabs + admin-notifications 的 Module._load 拦截模式，页面模块动态 import）。
- **Step 2 校验文案**：删除 `studio.publish.ipRequired` 的「IP 必选」逻辑与 `ipError` 状态；fanwork 三源（IP/原创来源/二创来源）均空时提交按钮 disabled，来源区显示 `studio.publish.fanwork.validation.sourceRequired`（"发布二创需要 IP 或灵感来源"），fieldset `aria-describedby` 关联（ui-spec a11y）。旧键 `ipRequired`/`sourceOriginalOptional`/`searchOriginalPlaceholder` 从 zh/en 移除（仅 PublishForm 引用，已确认无其他消费者）。
- **Step 3 接入 SourceContentPicker**：PublishForm 移除 `SourceOriginalPicker` 兼容包装使用（包装文件本身未动），改渲染两个 `<SourceContentPicker>`（sourceKind="original"→`source_original_id`、sourceKind="fanwork"→`source_fanwork_id`），共用既有 `GET /api/v1/contents/search?zone=&q=&limit=8`（无新增搜索 API）；来源区改为 `fieldset+legend`（`studio.publish.fanwork.source.legend`/`ipOptional`），IP 标签去 `*` 改可选；选择互斥：`handleSourceOriginalSelect`/`handleSourceFanworkSelect` 选中即清除另一来源。
- **Step 4 payload 合同**：提交 payload 至多含 `source_original_id` 或 `source_fanwork_id` 之一（互斥由选择器状态保证），可带 `ip_id` 与任一来源；与后端 `validateSourceLink`（仅一个来源、zone 匹配 + published）对齐。
- **query prefill**：`page.tsx` 用 `useSearchParams`（Suspense 包裹，login 页模式）解析 `source_original_id`/`source_fanwork_id`（`/^[1-9]\d*$/` 校验），双 ID 时保留 original 清 fanwork 并加 `bothSources` 警告，非法 id 加 `invalidId` 警告（可关闭，role="status"）；PublishForm 新增 props `prefillSourceOriginalId`/`prefillSourceFanworkId`/`prefillWarnings`，mount 时 fetch `GET /api/v1/contents/:id` → `normalizeContentDetailResponse` → 校验 zone 匹配 + status published 后预填摘要（prefill-loading 骨架行、prefill-error 内联可关闭，均 role="status"）。
- **Step 5 i18n**：`studio.publish.fanwork.{source,validation,prefill,a11y}`（legend/ipOptional/sourceRequired/bothSources/invalidId/loadFailed/dismissWarning）zh/en 同步（i18n parity 通过）。
- **Step 6 Playwright**：`studio-publish-fanwork.spec.ts` 两处 source option 定位从 `getByRole("button")` 改为 `getByRole("option")`（SourceContentPicker 结果行为 li/option）；test 2 改写为当前 picker 契约（编辑查询清空已选来源 → input 置空、summary 消失）；新增 `e2e/t6-publish-flow.mock.spec.ts`：三源发布 payload、无源 disabled+互斥、预填摘要、双 ID 警告、非法预填警告、6 张截图。
- 未改后端/config/routes/migrations（无需 doc-validator）；未触碰 SourceContentPicker.tsx 与 main worktree。

### Testing:
- 红→绿：11 条新 focused 测试先全红（20 中 9 过 11 败），实现后 `node --import tsx --test tests/studio-publish-fanwork.test.tsx` 20/20 绿。
- `npm run lint`（tsc strict）、`npm run lint:ui`（i18n parity 等全 ok）、`npm run build`、`npm run test:unit` 364/364 全绿。
- Playwright：`npx playwright test e2e/studio-publish-fanwork.spec.ts` 2/2 绿 + `e2e/t6-publish-flow.mock.spec.ts` 6/6 绿（合计 8/8；worktree dev server 在 :3001，:3000 被另一 agent worktree t89 占用，用等价 baseURL 配置跑同一批 spec）。截图 8 张 `screenshots/t6-publish-fanwork-{ip-only,original-source,fanwork-source,mutual-exclusion,no-source-validation,prefill-original,prefill-both-warning,prefill-invalid-warning}.png`。

### Notes:
- 双 ID 优先级规则：两参数同时出现时（original 合法）保留 original 清 fanwork；参数非法时不发任何 prefill 请求、显示可关闭警告；预填来源 404/zone 不匹配/非 published 视为 prefill-error 不落选。
- `@testing-library/dom` v16 已移除 `getByPlaceholder`（新 API `getByPlaceholderText`，测试内修正）；提交门 titleRequired 先于来源校验（预填测试需先填标题）。
- 未触碰 main worktree；progress.txt 仅更新本 worktree 副本。

## [2026-08-12] - Task 1（heavy）：配置、迁移与模型（collaboration-invites #97）

### What was done:
- **TDD 红**（先失败测试）：
  - `backend/internal/model/collab_invite_migration_test.go`（新）`TestCollaborationInviteMigration`：先跑失败——`read migration ../../migrations/065_collaboration_invites.sql: no such file or directory`；随后两次断言修正（`accept_collab_invites`/`msg_type`/`metadata` 为 NOT NULL，非 nullable；metadata 默认值改 `::text` 扫描避免 GORM map 扫描歧义）。
  - `backend/config/config_test.go` `TestCollaborationConfig`：先跑失败——`cfg.Collaboration undefined (type *Config has no field or method Collaboration)`。
  - `backend/internal/handler/public_config_test.go`：`TestPublicConfigExposesOnlyCollaborationMaxInviteesPerPublish` + `TestPublicConfigAllowlist` 扩展先跑失败——`response must contain 'collaboration' object`。
- **实现（Step 3/4）**：
  - `backend/migrations/065_collaboration_invites.sql`（新）：`collaboration_invites` 表（status CHECK pending/accepted/rejected/expired）+ `idx_collab_invites_invitee` + **partial unique** `idx_collab_invites_active ON (content_id, invitee_id) WHERE status IN ('pending','accepted')`（非普通 UNIQUE，过期可重邀）；`users.accept_collab_invites BOOLEAN NOT NULL DEFAULT TRUE`；`messages.msg_type VARCHAR(20) NOT NULL DEFAULT 'text' CHECK (text/collab_invite)`；`messages.metadata JSONB NOT NULL DEFAULT '{}'`；`-- ROLLBACK:` 注释块覆盖三处，共享环境不自动 drop。
  - `backend/internal/model/collab_invite.go`（新）：`CollabInvite` + 4 个 status 常量；`user.go` 加 `AcceptCollabInvites`（`gorm:"not null;default:true"`）；`notification.go` Message 加 `MsgType`（default 'text'）+ `Metadata`（`JSONMap` `gorm:"type:jsonb;not null;default:'{}'"`，复用 RehabCourse 先例）。
  - `backend/config/config.go`：`CollaborationConfig` 4 字段（invite_daily_limit=20/invite_expire_days=7/max_invitees_per_publish=5/max_contributors_per_item=10）挂 `Config.Collaboration`；**未加入 `SaveOverride`**（config.go:597 白名单不变）；`config.yaml` 加 collaboration 段默认值。
  - `backend/internal/handler/public_config.go`：`PublicCollaborationDTO{MaxInviteesPerPublish}` + `PublicConfigResponse.Collaboration`；仅暴露 `collaboration.max_invitees_per_publish`。

### Testing:
- 三目标测试全绿：`go test ./internal/model -run TestCollaborationInviteMigration -v`、`./config -run TestCollaborationConfig -v`、`./internal/handler -run TestPublicConfig -v`（含 allowlist 精确 key 断言：collaboration 仅 1 键、无 invite_daily_limit/invite_expire_days/max_contributors_per_item/inviter 泄漏）。
- `cd backend && go build ./... && go vet ./... && go test ./...` 全量全绿（约 30 包）；改动文件 `gofmt -l` 干净（全库残留未格式化文件为既有，未触碰）。
- doc-validator `--fix` 两轮幂等：`docs/reference/config.md`（+4 配置行）、`docs/reference/schema.md`（+collaboration_invites 表、messages 两列、users 一列）；生成物 diff 已核对仅含本次改动。

### Notes:
- 未碰：main worktree、frontend、routes.go。Task 2（repo/service 层）待本 PR 合并后开新 worktree。

## [2026-08-12] - Task 1（heavy）审查修复（PR #166 DONE_WITH_CONCERNS 4 项）

### What was done:
- **① message_id 列**：`065_collaboration_invites.sql` CREATE TABLE 补 `message_id BIGINT REFERENCES messages(id) ON DELETE SET NULL`（设计输入 line 834 + 计划 Task 2 Step 1/5 回填要求；迁移未合并直接改 065 本体）；`collab_invite.go` 补 `MessageID *int64`（`json:"message_id,omitempty"`）；测试新增 message_id 列（bigint nullable）+ FK→messages 断言。
- **② inviter 索引**：065 补 `idx_collab_invites_inviter ON collaboration_invites (inviter_id)`（设计输入 line 840，inviter_id 为 ON DELETE CASCADE FK 需显式索引）；测试断言存在。
- **③ UNIQUE 断言强化**：新增 helper `indexIsUnique`（pg_index.indisunique）断言 `idx_collab_invites_active` 是 UNIQUE；partial 谓词断言（content_id/invitee_id/status/pending/accepted）保持。
- **④ status 统一 declined**：065 CHECK 改 `('pending','accepted','declined','expired')`、`CollabInviteStatusRejected` → `CollabInviteStatusDeclined = "declined"`；测试新增 status CHECK 断言（含 4 值、不含 rejected）；迁移注释同步；全库残留 rejected 均为无关表（tag_suggestions/appeals）。

### Testing:
- `go test ./internal/model -run TestCollaborationInviteMigration -v` PASS；`cd backend && go build ./... && go vet ./... && go test ./...` 24 包全绿无 FAIL；改动文件 gofmt 干净。
- doc-validator `--fix` 两轮幂等：schema.md 同步 `collaboration_invites.message_id` 列（+1 行）；config/route 无变化。
- 提交：`fix(collab): address Task 1 review concerns - message_id, inviter index, unique assertion, declined status (#97)`（追加 commit，不 amend）。

### Notes:
- 未碰 main worktree/frontend/routes.go；issue #97 不 close。

## [2026-08-12] - Task 5 [light]: Build Invite Card And Settings UI

### What was done:
- **Step 1（UI spec 确认）**：`rg -n "## Component: CollabInviteCard|## Component: CollabUserPicker|## Page: /settings|## Page: /messages" design/ui-spec.md` → 全部存在（4029/4892/1064/1567），/settings 描述 accept_collab_invites 开关、保存状态、i18n keys 与截图检查点。TagBadge spec 2191。
- **Step 2（RED 测试）**：新建 `frontend/tests/collab-invite-card.test.tsx`（12 个测试：pending 按钮+状态文本+role=group/aria-labelledby、accepted/declined/expired 只读 + expired bg-canvas-subtle、accept/decline POST 端点 + returned invite.status 状态更新、失败保持 pending + 行内错误 + toast、非接收方 waiting 无按钮、aria-busy + 另一按钮 disabled、按钮 aria-label 含标题 + min-h-11、ChatWindow 集成卡片 + 普通气泡回归、invalid metadata 回退摘要）与 `frontend/tests/settings-collab-invites.test.tsx`（6 个测试：初始化 aria-checked、PATCH body deepEqual、成功 refreshUser + toast、失败回滚 + toast + 行内错误、保存中密码/注销控件不受影响、显式可见 label）。初次运行 RED：`Cannot find module '@/components/social/CollabInviteCard'` + `Unable to find role="switch"`，0/18 通过。
- **后端 DTO 前置（研究 gotcha 1）**：`backend/internal/handler/message.go` MessageDTO 增加 `MsgType string json:"msg_type"` + `Metadata model.JSONMap json:"metadata,omitempty"` 并在 `messageDTO()` 填充——否则 ChatWindow 的 collab_invite 分支是死代码。无 migration/route 改动 → 不跑 doc-validator。
- **Step 3（卡片）**：新建 `frontend/components/social/CollabInviteCard.tsx`——按 ui-spec 精确 props 接口（id/status/contentId/contentTitle/inviterUsername + isCurrentUserInvitee + onAccept/onDecline）；`max-w-full md:max-w-[min(420px,80%)] lg:max-w-[420px] border border-border-default rounded-lg bg-canvas-default p-4`；类型行 → 邀请描述 → 内容链接（/content/:id）→ TagBadge 状态（pending blue/accepted green/declined rose/expired `bg-canvas-subtle text-fg-muted` 自定义 gray，twMerge 覆盖生效，绝不只靠颜色）→ 仅 `isCurrentUserInvitee && pending` 显示操作区；本地 status state + per-button busy LoaderCircle + 另一按钮 disabled + aria-busy；失败恢复 + 行内错误 + toast；全部文案走 `useTranslations()` `collabInviteCard.*`。`ChatWindow.tsx`：Message 接口加 `metadata?`，`msg_type === "collab_invite"` 分支渲染卡片（snake_case metadata → camelCase props，`isCurrentUserInvitee = sender_id !== user?.id`），invalid metadata（缺 invite_id/content_id/content_title/inviter_username）回退安全摘要文本；普通气泡不变；ConversationList 无需改动（collabInviteSummary 分支已存在，DTO 修复后自然生效，metadata 无 status 可追加）。
- **Step 4/5（设置页）**：`AuthContext.tsx` User 接口加 `accept_collab_invites: boolean`；`app/(protected)/settings/page.tsx` 个人信息卡与安全设置之间新增独立「联合创作邀请」分组卡（标题 + 描述 + 显式 `<label>` 包裹 Switch + help 文本 + 行内错误），own `collabBusy/collabError/collabAccept` 状态（绝不用页面级 busy/error）；乐观 PATCH `/api/v1/users/:id` body 仅 `{accept_collab_invites}` → 成功 `refreshUser()` + success toast，失败回滚到 pre-save 值 + error toast + 行内错误；保存中仅开关行 disabled + 局部 saving 指示，密码/注销控件不受影响；Switch 为既有 `@/components/ui/switch`（无 id prop → 外层 label）。
- **i18n**：zh.json + en.json 镜像新增 `collabInviteCard.*`（type/invitation/status/actions/errors/summary/a11y，占位符 {inviter}/{title} 一致）与 `settings.collaboration.*`（title/description/acceptInvites.label/help/toast.saved|failed/error.save/a11y.acceptInvites）；复用既有 `messages.chat.collabInviteSummary`/`messages.conversations.collabInviteSummary`，不重复。
- **浏览器验证（mock 合同）**：新建 `frontend/e2e/t97-collab-invites.mock.spec.ts`（9 个测试，precedent：messages-notifications.spec.ts + mock-api-guard + NEXT_LOCALE=zh cookie）：pending 卡片（含接受/拒绝/气泡回归）、accept→accepted 只读、decline→declined 只读、accept 返回 expired → 灰色只读、失败 409 → 行内错误+toast+保持 pending、移动端按钮 ≥44px（boundingBox 断言）、设置开关初始化/保存/回滚、移动端设置。截图 7 张：`community-collab-invite-pending/states-accepted/states-declined/states-expired/mobile.png` + `community-collab-settings-desktop/mobile.png`（screenshots/ gitignored，`git add -f`）。

### Testing:
- 红→绿：RED 0/18 → 实现后聚焦 18/18（`node --import tsx --test tests/collab-invite-card.test.tsx tests/settings-collab-invites.test.tsx`）。
- 修复两个测试自身问题：(1) toast 与行内错误同文案 → `getByText` 双匹配，改 `querySelector('[role="alert"]')`；(2) 设置开关测试与 AuthProvider fetchMe 竞态（switch 先于 user 渲染，`!user` 早退）→ `awaitLoadedUser` 等 username input 值出现再点击。
- 全量门：`npm run test:unit` 403/403、`npm run lint`（tsc --noEmit）干净、`npm run lint:ui` 全 gate ok、`npm run build` ok；后端 DTO 改动后 `go build ./...`/`go vet ./...`/`go test ./...`（24 包）全绿；Playwright mock 合同 9/9（`npx playwright test --config=playwright.mocked.config.ts t97-collab-invites.mock.spec.ts`）。
- 截图保存至 `screenshots/community-collab-*.png` 共 7 张。

### Notes:
- 偏差：(1) 卡片 metadata 有效性判定要求 invite_id+content_id+content_title+inviter_username 四项齐全（ui-spec 只列前三个，但描述文案需要 inviter，四项更保守安全）；(2) e2e expired 状态用 mock accept 响应返回 status=expired 演示（后端对过期邀请实际返回 409 错误，卡片对任何 returned status 都应用，截图合同仅用于视觉验证）；(3) 状态截图分三张（accepted/declined/expired），非单张合成图；(4) ConversationList 未追加状态文本（last_message metadata 无 status 字段可读）。
- 遗留：(1) 后端 MessageDTO 新增字段无 handler 测试断言（研究 §6 gotcha 1 的补测点），建议 Task 7 或后续补 `TestMessageDTO`；(2) ChatWindow 初始 status 恒为 pending（metadata 不含 status，已接受邀请重进会话仍显示可操作，直到点击后按返回 DTO 更新；属既有合同限制，不在本任务范围）。
- 提交序列（light 分批）：tests → backend DTO → card → chatwindow → auth/settings → i18n → e2e+screenshots → docs（progress.txt + 计划勾选）。
